<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Android之ViewModel</title>
    <url>/posts/2023/10/17/bcdb2252/</url>
    <content><![CDATA[<h2 id="ViewModel是什么"><a href="#ViewModel是什么" class="headerlink" title="ViewModel是什么"></a>ViewModel是什么</h2><p>官网上的介绍是这样的</p>
<blockquote>
<p>The ViewModel class is a business logic or screen level state holder. It exposes state to the UI and encapsulates related business logic. Its principal advantage is that it caches state and persists it through configuration changes. This means that your UI doesn’t have to fetch data again when navigating between activities, or following configuration changes, such as when rotating the screen.</p>
</blockquote>
<p>ChatGPT总结以下3点</p>
<ul>
<li><strong>生命周期感知</strong> 感知与其关联的生命周期，适当的时候清理资源，避免内存泄漏</li>
<li><strong>配置更改时保留数据</strong> ViewModel生命周期超出了单个Activity/Fragment，所以配置更改时不会丢失数据</li>
<li><strong>分离业务逻辑</strong> 将 UI 相关的业务逻辑和数据管理从界面控制器中分离，使得代码更易于维护和测试</li>
</ul>
<h2 id="ViewModel使用"><a href="#ViewModel使用" class="headerlink" title="ViewModel使用"></a>ViewModel使用</h2><p>作为例子说明，我们需要准备一个类继承自ViewModel，暂且命名为<code>MainViewModel</code></p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">class MainViewModel: ViewModel() { 
    ...
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
<p>接下来是在Activity/Fragment/可组合项中使用ViewModel，创建ViewModel对象</p>
<ul>
<li><h3 id="不需要依赖注入"><a href="#不需要依赖注入" class="headerlink" title="不需要依赖注入"></a>不需要依赖注入</h3></li>
</ul>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">// 使用扩展函数
private val mainViewModel by viewModels&lt;MainViewModel&gt;()
// 或者直接点 使用ViewModelProvider
private val mainViewModel: MainViewModel = ViewModelProvider(this).get()<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>
<ul>
<li><h3 id="使用hilt依赖注入"><a href="#使用hilt依赖注入" class="headerlink" title="使用hilt依赖注入"></a>使用hilt依赖注入</h3></li>
</ul>
<p>当你使用hilt依赖时，MainViewModel需要给类和主构造函数分别添加<code>@HiltViewModel</code>和<code>@Inject</code>两个注解</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@HiltViewModel
class MainViewModel @Inject constructor(): ViewModel() {
    ...
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>
<p>接下来调用函数<code>hiltViewModel()</code>拿到ViewModel对象</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">val mainViewModel: MainViewModel = hiltViewModel()<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<ul>
<li><h3 id="使用koin依赖注入"><a href="#使用koin依赖注入" class="headerlink" title="使用koin依赖注入"></a>使用koin依赖注入</h3></li>
</ul>
<p>使用koin注入时，首先需要在module中注入对象</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">module {
    viewModel {
        MainViewModel()
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>然后直接使用即可</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">val mainViewModel: MainViewModel = koinViewModel()<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>以上便是常见的创建ViewModel的几种方式，主要取决于你项目使用依赖库。不过不管是哪种，其内部都是经过ViewModelProvider通过Factory来实现的。</p>
<h2 id="ViewModel的生命周期"><a href="#ViewModel的生命周期" class="headerlink" title="ViewModel的生命周期"></a>ViewModel的生命周期</h2><p>在关于<a href="https://developer.android.com/topic/libraries/architecture/viewmodel">ViewModel的介绍</a>中，ViewModel是有生命周期的，如下图所示</p>
<img src="/posts/2023/10/17/bcdb2252/viewmodel-lifecycle.png" class="" title="viewmodel-lifecycle">

<p>那ViewModel如何实现生命周期管理的呢？<br>前文提到， <strong>ViewModel</strong>对象通过<code>ViewModelProvider(this).get()</code>返回，那先看看<code>get()</code>方法</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@MainThread
public inline fun &lt;reified VM : ViewModel&gt; ViewModelProvider.get(): VM = get(VM::class.java)

public open class ViewModelProvider @JvmOverloads constructor(
    private val store: ViewModelStore,
    private val factory: Factory,
    private val defaultCreationExtras: CreationExtras = CreationExtras.Empty,
) {
    @MainThread
    public open operator fun &lt;T : ViewModel&gt; get(modelClass: Class&lt;T&gt;): T {
        val canonicalName = modelClass.canonicalName
            ?: throw IllegalArgumentException("Local and anonymous classes can not be ViewModels")
        return get("$DEFAULT_KEY:$canonicalName", modelClass)
    }

    @Suppress("UNCHECKED_CAST")
    @MainThread
    public open operator fun &lt;T : ViewModel&gt; get(key: String, modelClass: Class&lt;T&gt;): T {
        val viewModel = store[key]
        if (modelClass.isInstance(viewModel)) {
            (factory as? OnRequeryFactory)?.onRequery(viewModel!!)
            return viewModel as T
        } else {
            @Suppress("ControlFlowWithEmptyBody")
            if (viewModel != null) {
                // TODO: log a warning.
            }
        }
        val extras = MutableCreationExtras(defaultCreationExtras)
        extras[VIEW_MODEL_KEY] = key
        // AGP has some desugaring issues associated with compileOnly dependencies so we need to
        // fall back to the other create method to keep from crashing.
        return try {
            factory.create(modelClass, extras)
        } catch (e: AbstractMethodError) {
            factory.create(modelClass)
        }.also { store.put(key, it) }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>概括下上面的代码，通过一个key去store中找，如果存在，则直接返回，否则通过factory创建<strong>ViewModel</strong>对象并调用store的put方法保留到store中，而这个store就是构造函数中的<strong>ViewModelStore</strong>。那么<strong>ViewModelStore</strong>是什么呢？继续往下看</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">public open class ViewModelProvider @JvmOverloads constructor(
    private val store: ViewModelStore,
    private val factory: Factory,
    private val defaultCreationExtras: CreationExtras = CreationExtras.Empty,
) {
  public constructor(
      owner: ViewModelStoreOwner
  ) : this(owner.viewModelStore, defaultFactory(owner), defaultCreationExtras(owner))
}

public class ComponentActivity extends androidx.core.app.ComponentActivity implements
        ContextAware,
        LifecycleOwner,
        ViewModelStoreOwner,
        ...
        FullyDrawnReporterOwner {
    @Override
    public ViewModelStore getViewModelStore() {
        if (getApplication() == null) {
            throw new IllegalStateException("Your activity is not yet attached to the "
                    + "Application instance. You can't request ViewModel before onCreate call.");
        }
        ensureViewModelStore();
        return mViewModelStore;
    }

    @SuppressWarnings("WeakerAccess") /* synthetic access */
    void ensureViewModelStore() {
        if (mViewModelStore == null) {
            NonConfigurationInstances nc =
                    (NonConfigurationInstances) getLastNonConfigurationInstance();
            if (nc != null) {
                // Restore the ViewModelStore from NonConfigurationInstances
                mViewModelStore = nc.viewModelStore;
            }
            if (mViewModelStore == null) {
                mViewModelStore = new ViewModelStore();
            }
        }
    }
}

open class ViewModelStore {
    private val map = mutableMapOf&lt;String, ViewModel&gt;()
    ...
    fun clear() {
        for (vm in map.values) {
            vm.clear()
        }
        map.clear()
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>简单解释下，创建<strong>ViewModelProvider</strong>时，传入this即<strong>ComponentActivity</strong>，而<strong>ComponentActivity</strong>实现了<strong>ViewModelStoreOwner</strong>接口提供<strong>ViewModelStore</strong>对象，<code>ensureViewModelStore()</code>是为了保证<strong>ViewModelStore</strong>不为空。<strong>ViewModelStore</strong>内部通过一个<strong>LinkedHashMap</strong>管理和存储<strong>ViewModel</strong>对象的。<br>目前，我们了解了<strong>ComponentActivity</strong>、<strong>ViewModelStore</strong>、<strong>ViewModel</strong>之间的关系了，总结一下：<strong>ComponentActivity</strong>实现了<code>ViewModelStoreOwner</code>接口从而提供<strong>ViewModelStore</strong>对象，<strong>ViewModelStore</strong>用来管理<strong>ViewModel</strong>对象的。当调用<strong>ViewModelProvider</strong>的<code>get()</code>方法获取<strong>ViewModel</strong>对象时，首先会看<strong>ViewModelStore</strong>是否已经存在该<strong>ViewModel</strong>对象，如果存在，则直接返回，否则先创建<strong>ViewModel</strong>对象并put到<strong>ViewModelStore</strong>中。不过还没谈到<code>Lifecycle</code>呀。</p>
<p>要谈生命周期，自然想到<code>Lifecycle</code>，接下来请看<strong>ComponentActivity</strong>中的这块代码</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">public ComponentActivity() {
    Lifecycle lifecycle = getLifecycle();
    getLifecycle().addObserver(new LifecycleEventObserver() {
        @Override
        public void onStateChanged(@NonNull LifecycleOwner source,
                @NonNull Lifecycle.Event event) {
            if (event == Lifecycle.Event.ON_DESTROY) {
                ...
                if (!isChangingConfigurations()) {
                    getViewModelStore().clear();
                }
                ...
            }
        }
    });
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>结合生命周期的配图，是不是一目了然？在Activity经过旋转屏幕导致Activity重建时，<code>!isChangingConfigurations()</code>不成立，<strong>ViewModelStore</strong>中还保留着原来的<strong>ViewModel</strong>对象，在恢复过程中继续可以通过<strong>ViewModelStore</strong>得到原来的对象和数据。但如果不是因为配置变更导致onDestory时，则清空<strong>ViewModelStore</strong>，并逐一执行<strong>ViewModel</strong>对象的clear方法，生命周期结束。</p>
<h2 id="如何保证数据在配置更改期间的一致性"><a href="#如何保证数据在配置更改期间的一致性" class="headerlink" title="如何保证数据在配置更改期间的一致性"></a>如何保证数据在配置更改期间的一致性</h2><p>在JVM中，想要保证数据，必须内存中有其备份。所以这个问题可以理解成，在配置更改时，<strong>ViewModel</strong>会被保存在哪里而避免在<code>Activity</code>重建过程中被销毁回收？<br>通过前文，了解到<strong>ViewModel</strong>保存在<strong>ViewModelStore</strong>中管理，其中有一个方法<code>ensureViewModelStore()</code>保证了<strong>ViewModelStore</strong>不为空。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@SuppressWarnings("WeakerAccess") /* synthetic access */
void ensureViewModelStore() {
    if (mViewModelStore == null) {
        NonConfigurationInstances nc =
                (NonConfigurationInstances) getLastNonConfigurationInstance();
        if (nc != null) {
            // Restore the ViewModelStore from NonConfigurationInstances
            mViewModelStore = nc.viewModelStore;
        }
        if (mViewModelStore == null) {
            mViewModelStore = new ViewModelStore();
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p><em>TODO 待完成</em> 结合前文，加上<code>NonConfigurationInstances</code>解释即可</p>
<h2 id="ViewModel-VS-onSaveInstanceState"><a href="#ViewModel-VS-onSaveInstanceState" class="headerlink" title="ViewModel VS onSaveInstanceState"></a>ViewModel VS onSaveInstanceState</h2><p><em>TODO 待完成</em> 主要差异在于ViewModel没有数据大小限制，而onSaveInstanceState是基于Bundle实现的，Bundle又有内存映射时的限制所以无法保留大数据</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><em>TODO 待完成</em></p>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://proandroiddev.com/how-viewmodel-works-under-the-hood-52a4f1ff64cf">How Android ViewModel works under the hood to survive to configuration change
</a></p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Android</category>
      </categories>
      <tags>
        <tag>ViewModel</tag>
      </tags>
  </entry>
  <entry>
    <title>Android14中《此应用专为旧版 Android 系统打造...》弹窗</title>
    <url>/posts/2023/12/29/9734dc48/</url>
    <content><![CDATA[<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>测试机升级到Android14后，当打开应用时，会出现一个弹窗，弹窗信息如下图：<br><img src="/posts/2023/12/29/9734dc48/app_warning_dialog.png"><br>刚开始点击确认之后，后面并不会出现也就没去管，然而，在我执行<code>benchmark</code>的Startup测试的时候，每次都会提醒而影响测试。这事必须得解决。</p>
<h2 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h2><p>通过搜索，很多文章都提到一个方法<code>showDeprecatedTargetDialogIfNeeded()</code>。可以根据这个方法展开去了解。</p>
<h3 id="定位"><a href="#定位" class="headerlink" title="定位"></a>定位</h3><p>这是一个简单的定位过程：</p>
<ol>
<li>打开<a href="https://github.com/aosp-mirror/platform_frameworks_base">platform_frameworks_base</a>这个仓库。</li>
<li>作为Android开发者，都知道硬编码的字符串是存在res目录下的strings.xml文件中，笔者系统默认英文，直接找默认的文件即可，目录为<code>platform_frameworks_base/core/res/res/values/strings.xml</code>，不同语言根据不同目录去找。</li>
<li>在strings.xml文件中找对对应字符串的那行 <code>&lt;string name="deprecated_target_sdk_message"&gt;This app was built for an older version...</code>，从而得知key为deprecated_target_sdk_message</li>
<li>使用Github Command Palette查找key，找到对应使用的代码。搜索结果右边有个Paths的section可以看到只有一个是java代码的，点击它便可看到调用的代码。<img src="/posts/2023/12/29/9734dc48/search_result.jpg" class="" title="search_result"></li>
<li>这时候你应该看到<code>DeprecatedTargetSdkVersionDialog.java</code>文件了，双击该类名，右侧可以看到<code>References(引用)</code>，点击调用方法进入对应代码块。</li>
<li>这时候你应该是在<code>AppWarnings.java</code>文件中，继续根据引用链，最后也一样找到<code>showDeprecatedTargetDialogIfNeeded()</code>方法</li>
</ol>
<h3 id="为什么会出现这个弹窗"><a href="#为什么会出现这个弹窗" class="headerlink" title="为什么会出现这个弹窗"></a>为什么会出现这个弹窗</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java"> public void showDeprecatedTargetDialogIfNeeded(ActivityRecord r) {
    if (r.info.applicationInfo.targetSdkVersion &lt; Build.VERSION.MIN_SUPPORTED_TARGET_SDK_INT) {
        mUiHandler.showDeprecatedTargetDialog(r);
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>根据<code>applicationInfo.targetSdkVersion</code>和<code>Build.VERSION.MIN_SUPPORTED_TARGET_SDK_INT</code>判断是否执行警告弹窗代码，那这两个玩意是什么呢？</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">// 来自
/**
 * The minimum SDK version this application targets.  It may run on earlier
 * versions, but it knows how to work with any new behavior added at this
 * version.  Will be {@link android.os.Build.VERSION_CODES#CUR_DEVELOPMENT}
 * if this is a development build and the app is targeting that.  You should
 * compare that this number is &gt;= the SDK version number at which your
 * behavior was introduced.
 */
public int targetSdkVersion;

// 来自Build.java
public static final int MIN_SUPPORTED_TARGET_SDK_INT = SystemProperties.getInt(
                "ro.build.version.min_supported_target_sdk", 0);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>注释很清楚，<code>applicationInfo.targetSdkVersion</code>就是我们配置的min SDK。而<code>MIN_SUPPORTED_TARGET_SDK_INT</code>是一个System配置中key为<code>ro.build.version.min_supported_target_sdk</code>的属性，其中Android14中配置为28<br><a href="https://android.googlesource.com/platform/prebuilts/fullsdk/platforms/+/refs/heads/main/android-34/build.prop">ro.build.version.min_supported_target_sdk in line42</a></p>
<h3 id="为什么只提醒一次"><a href="#为什么只提醒一次" class="headerlink" title="为什么只提醒一次"></a>为什么只提醒一次</h3><p>以下是两个相关代码块，注释部分则可以表示同一个包只会出现一次弹窗的原因</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">DeprecatedTargetSdkVersionDialog(final AppWarnings manager, Context context,
            ApplicationInfo appInfo) {
    super(manager, appInfo.packageName);
    ...
    final CharSequence message = context.getString(R.string.deprecated_target_sdk_message);

    final AlertDialog.Builder builder = new AlertDialog.Builder(context)
            .setPositiveButton(R.string.ok, (dialog, which) -&gt;
            // 设置 package flag
                manager.setPackageFlag(
                        mPackageName, AppWarnings.FLAG_HIDE_DEPRECATED_SDK, true))
            .setMessage(message)
            .setTitle(label);

    ...
}

@UiThread
private void showDeprecatedTargetSdkDialogUiThread(ActivityRecord ar) {
    if (mDeprecatedTargetSdkVersionDialog != null) {
        mDeprecatedTargetSdkVersionDialog.dismiss();
        mDeprecatedTargetSdkVersionDialog = null;
    }
    // 根据package flag 判断是否弹出
    if (ar != null &amp;&amp; !hasPackageFlag(
            ar.packageName, FLAG_HIDE_DEPRECATED_SDK)) {
        mDeprecatedTargetSdkVersionDialog = new DeprecatedTargetSdkVersionDialog(
                AppWarnings.this, mUiContext, ar.info.applicationInfo);
        mDeprecatedTargetSdkVersionDialog.show();
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<h2 id="解决问题"><a href="#解决问题" class="headerlink" title="解决问题"></a>解决问题</h2><p>我们知道这个弹窗的流程并了解其判断条件了，解决问题自然简单，简单把我们min SDK改成28或更高就可以了。再执行benchmark测试，没有再出现这个弹窗，问题解决。</p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android14</tag>
      </tags>
  </entry>
  <entry>
    <title>Coroutines + Retrofit + MVVM 简单实现</title>
    <url>/posts/2021/09/11/5c71264a/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><ul>
<li>这里基本不会深入讨论各个知识点，如需了解更多可以参考 <a href="https://www.bennyhuo.com/">Benny’s Blog</a> 和 <a href="https://kotlinlang.org/docs/reference/coroutines/coroutines-guide.html">Kotlin官方文档</a></li>
</ul>
<h2 id="为什么使用协程"><a href="#为什么使用协程" class="headerlink" title="为什么使用协程"></a>为什么使用协程</h2><ul>
<li>协程比线程小<blockquote>
<p>我： 为什么已经有Rx了还要在这里用协程？<br><br>某同事： 因为协程比线程小，可以开很多个。<br><br>我：…… 为什么比线程小？</p>
</blockquote>
</li>
</ul>
<p>  我维基了一下，确实有说比线程更小。<br><br>  但是看了一些源码，也是线程池 + 线程实现的，这时就开始有了疑惑，为什么同样是线程，怎么就说是比线程小的东西呢？<br><br>  直到看到了Benny大佬的文章 <a href="https://www.bennyhuo.com/2019/10/19/coroutine-why-so-called-lightweight-thread/">协程为什么被称为『轻量级线程』</a>解释，我清晰了。通过测验，确实启动成千上万个协程也不会出现OOM或者其他问题。</p>
<ul>
<li>当然最主要的还是代码上的体验<br>现在Kotlin越来越普遍，各种inline函数，操作符也都基本可以替换Rx的常用操作符了。所以在写代码上体验还是相对比较好的。<br><br><font color="#f00" size="3"><em>（备注：个人觉得协程小不小对于Android开发真的没多大区别，最主要还是写代码和代码美观性）</em></font></li>
</ul>
<h2 id="一、添加依赖"><a href="#一、添加依赖" class="headerlink" title="一、添加依赖"></a>一、添加依赖</h2><pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">implementation"com.squareup.retrofit2:retrofit:2.6.2"
implementation"com.squareup.retrofit2:converter-gson:2.6.2"

// Coroutines 
implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-core:1.3.2'
implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.3.2'<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p><font color="#f00" size="3"><em>记得使用retrofit 2.6.0 或以上</em></font></p>
<h2 id="二、准备网络请求接口"><a href="#二、准备网络请求接口" class="headerlink" title="二、准备网络请求接口"></a>二、准备网络请求接口</h2><ul>
<li>创建接口<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">interface GitApi {
    @GET("/users/{username}/{module}")
    suspend fun repos(
        @Path("username") username: String,
        @Path("module") module: String,
        @Query("page") currPage: Int
    ): List&lt;RepoInfo&gt;

    @GET("/search/repositories")
    suspend fun searchRepos(
        @Query("q") key: String,
        @Query("sort") sort: String? = "updated",
        @Query("order") order: String? = "desc",
        @Query("page") currPage: Int
    ): SearchResponse
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
<li>创建Retrofit实例<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">fun buildRetrofit(): Retrofit {
     builder.addInterceptor(HttpLoggingInterceptor(object : HttpLoggingInterceptor.Logger {
         override fun log(message: String) {
             Timber.d(message)
         }
     }).apply {
         level = HttpLoggingInterceptor.Level.BODY
     }).addInterceptor(object : Interceptor {
         override fun intercept(chain: Interceptor.Chain): Response {
             val userCredentials = "$username:$password"
             val basicAuth =
                 "Basic ${String(Base64.encode(userCredentials.toByteArray(), Base64.DEFAULT))}"
             val original = chain.request()
             val requestBuilder = original.newBuilder()
                 .header("Authorization", basicAuth.trim { it &lt;= ' ' })
             val request = requestBuilder.build()
             return chain.proceed(request)
         }
     })
     return Retrofit.Builder()
         .client(builder.build())
         .baseUrl(BASE_URL)
         .addConverterFactory(GsonConverterFactory.create())
         .build()
 }<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
</ul>
<h2 id="三、-在ViewModel中使用"><a href="#三、-在ViewModel中使用" class="headerlink" title="三、 在ViewModel中使用"></a>三、 在ViewModel中使用</h2><p> <code>kotlinx.coroutines.CoroutineScope</code> 接口中的注释有这么一句话<br><br><img src="https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2019/12/30/16f561eaea6d630b~tplv-t2oaga2asx-image.image" alt="image.png"><br>所以启动一个协程一定需要一个<code>Scope</code>，也就是说每个suspend函数都有一个<code>CoroutineScope</code>，如果没有一定会报错。而Android的ViewModel中有扩展了一个<code>viewModelScope</code>，并且跟<code>lifecycle</code>绑定了，所以在ViewModel的onCleared方法上会自动帮我们cancel掉这个viewModelScope的所有Jobs。<br><img src="https://p1-jj.byteimg.com/tos-cn-i-t2oaga2asx/gold-user-assets/2019/12/30/16f561eae8114f24~tplv-t2oaga2asx-image.image" alt="图1"><br>为了方便使用，封装了一个BaseViewModel</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">open class BaseViewModel : ViewModel() {
    fun &lt;T&gt; request(
        onError: (error: Throwable) -&gt; Unit = {}, // 不需要处理Error可以不传
        execute: suspend CoroutineScope.() -&gt; T
    ) {
        viewModelScope.launch(errorHandler { onError.invoke(it) }) {
            launch(Dispatchers.IO) {
                execute()
            }
        }
    }

    private fun errorHandler(onError: (error: Throwable) -&gt; Unit): CoroutineExceptionHandler {
        return CoroutineExceptionHandler { _, throwable -&gt;
            Timber.d(throwable)
            onError.invoke(throwable)
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>使用继承 <code>BaseViewModel</code> 调用 <code>request</code> 方法即可。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">class RepoViewModel(
    private val userRepo: UserDataSource,
    private val gitApi: GitApi
) : BaseViewModel() {
    private val _reposResult = BaseLiveData&lt;List&lt;RepoInfo&gt;&gt;()
    val repoResult: BaseLiveData&lt;List&lt;RepoInfo&gt;&gt;
        get() = _reposResult

    fun fetchRepos(module: String) {
        request {
            userRepo.currUser()?.let {
                val result = gitApi.repos(it.nickname, module, 1)
                _reposResult.update(result)
            }
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>OR</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">fun fetchRepos(module: String) {
    request(
        onError = {
            // handle error
        },
        execute = {
            userRepo.currUser()?.let {
                val result = gitApi.repos(it.nickname, module, 1)
                _reposResult.update(result)
            }
        }
    )
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>剩下的基本是LiveData和Fragment之间的订阅上的逻辑实现了。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>最近在学习，了解也不是很深，欢迎评论补充和提建议。 学习项目地址 <a href="https://github.com/BreakZero/Dithub">Dithub</a>。</p>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>MVVM</tag>
        <tag>Kotlin Coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>Android中跨进程通信之AIDL</title>
    <url>/posts/2023/03/20/ba1c720c/</url>
    <content><![CDATA[<blockquote>
<p>Android 跨进程通信的方式一般有：</p>
<ol>
<li>Binder</li>
<li>内存共享</li>
<li>Socket</li>
</ol>
</blockquote>
<p>其中Binder在Android中使用比较多，比如AMS<br>在Android上，一个进程通常是不能获取到其他进程的内存访问权限的，但是有时我们需要在多个进程之间进行数据处理，因此Android提供了Android Interface Definition Language（AIDL）来做处理<br>其实AIDL是文件会通过编译生成一个Sub类并实现IBinder接口的，这种方式可以说是基于Binder实现的。</p>
<h2 id="配置新项目"><a href="#配置新项目" class="headerlink" title="配置新项目"></a>配置新项目</h2><p>首先创建一个项目，有两个Phone Module，分别为Client和Server端。对于新的项目打开aidl feature支持</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">buildFeatures {
    compose true
    aidl true
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>
<span id="more"></span>
<h2 id="Server端："><a href="#Server端：" class="headerlink" title="Server端："></a>Server端：</h2><p>然后创建aidl文件</p>
<pre class="line-numbers language-aidl" data-language="aidl"><code class="language-aidl">interface IRemoteAidlInterface {
    /**
    * Demonstrates some basic types that you can use as parameters
    * and return values in AIDL.
    */
    int currPid();
    void basicTypes(int anInt, long aLong, boolean aBoolean, float aFloat,
    double aDouble, String aString);
    void addUser(inout User user);
    User theFirstUser();
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>以上加了User不是基本类型，所以需要做一些特殊处理，<br>首先添加一个aidl文件声明下这个类</p>
<pre class="line-numbers language-aidl" data-language="aidl"><code class="language-aidl">package com.easy.aidlserver;

// Declare any non-default types here with import statements

parcelable User;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>然后创建一个data class, 并添加<code>readFromParcel</code>方法</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Parcelize
data class User(
var name: String? = "",
var age: Int = 0
): Parcelable {
    fun readFromParcel(reply: Parcel) {
        this.name = reply.readString().toString()
        this.age = reply.readInt()
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>Build下项目生成对应的java类，<br>声明并注册一个Service，提供数据处理能力</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">class RemoteService : Service() {
    override fun onBind(intent: Intent?): IBinder {
        Log.d("Service Side", "onBind");
        return binder;
    }

    override fun onUnbind(intent: Intent?): Boolean {
        Log.d("Service Side", "onUnBind")
        return super.onUnbind(intent)
    }

    private val users = mutableListOf&lt;User&gt;()

    .....
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>并在manifest种注册</p>
<pre class="line-numbers language-markup" data-language="markup"><code class="language-markup"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>service</span>
    <span class="token attr-name"><span class="token namespace">android:</span>name</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>.RemoteService<span class="token punctuation">"</span></span>
    <span class="token attr-name"><span class="token namespace">android:</span>exported</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>true<span class="token punctuation">"</span></span>
    <span class="token attr-name"><span class="token namespace">tools:</span>ignore</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>ExportedService<span class="token punctuation">"</span></span><span class="token punctuation">&gt;</span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>intent-filter</span><span class="token punctuation">&gt;</span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>action</span> <span class="token attr-name"><span class="token namespace">android:</span>name</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>com.aidl.server<span class="token punctuation">"</span></span> <span class="token punctuation">/&gt;</span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>category</span> <span class="token attr-name"><span class="token namespace">android:</span>name</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>android.intent.category.DEFAULT<span class="token punctuation">"</span></span> <span class="token punctuation">/&gt;</span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>intent-filter</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>service</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>这里Server端代码完成了</p>
<h2 id="Client端"><a href="#Client端" class="headerlink" title="Client端"></a>Client端</h2><p>copy aidl文件到client端，注意要相同的包名和内容，还有对应的data class <code>User</code><br>定义connection并通过Binder拿到AIDL Interface，拿到interface就可以通过调用aidl提供的方法，并通过Proxy和服务端的service进行数据传输和处理</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">private val sConnection = object : ServiceConnection {
    override fun onServiceConnected(name: ComponentName?, service: IBinder?) {
        iRemoteService = IRemoteAidlInterface.Stub.asInterface(service)
        iRemoteService?.let {
            try {
                val pid = it.currPid()
                val myPid = Process.myPid()
                Log.i("Client", "Service Pid: $pid, Client Pid: $myPid")
                it.basicTypes(1, 2, true, 3.0f, 5.0, "Hello AIDL Server")
            } catch (e: Exception) {
                e.printStackTrace()
            }
        }
    }

    override fun onServiceDisconnected(name: ComponentName?) {
        Log.d("Client", "onDisconnect")
        iRemoteService = null
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>遇到的问题或疑惑：</p>
<ol>
<li><p>通过API30 的x86 虚拟机可以正常唤醒service并数据正常传输，但是在API33的真机上无法bind service<br>通过查阅资料，发现在30或以上的API level后存在<a href="https://developer.android.google.cn/training/package-visibility?hl=en">Package visibility filtering on Android</a> 这么一个说法。<br>大概就是为了更高的安全性而加入了过滤行为，从而导致应用无法检测到当前设备上所有安装的应用程序。解决方法加入 <code>queries</code></p>
<pre class="line-numbers language-markup" data-language="markup"><code class="language-markup"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>queries</span><span class="token punctuation">&gt;</span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>package</span> <span class="token attr-name"><span class="token namespace">android:</span>name</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>com.easy.aidlserver<span class="token punctuation">"</span></span> <span class="token punctuation">/&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>queries</span><span class="token punctuation">&gt;</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>已经操作了unbindService，但是仍然能进行数据操作，而且日志走了onUnbind却不走onServiceDisconnected<br>其实onServiceDisconnected在正常unbind的时候是不会触发执行的，而是在服务丢失的时候执行。<br><img src="/images/codes/aidl_1.jpg" alt="When to excute onServiceDisconnected" title="AIDL"></p>
</li>
<li><p>传输大数据的时候<br>因为AIDL 是通过IBinder进行数据传输的，而IBinder对数据的拷贝是经过mmap（内存映射），在我们app启动的时候，会去申请一个 1M - 8K大小的内存给mmap，所以如果<br>传输1M或以上的数据就会报错。<br>将User修改一下，加一个ByteArray</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Parcelize
data class User(
    var name: String? = "",
    var age: Int = 0,
    var byteArray: ByteArray = ByteArray(1)
)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>使用的时候</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">// _1M = 1024 * 1024
iRemoteService?.addUser(
    User(
        "Dougie ${Random.nextInt(1, 100)}",
        Random.nextInt(18, 24),
        byteArray = ByteArray(_1M)
    )
)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>你会收到报错信息：<br><img src="/images/codes/aidl_error.jpg" alt="android.os.TransactionTooLargeException: data parcel size 1048712 bytes" title="AIDL Error"></p>
</li>
</ol>
<h2 id="代码地址-AidlSample"><a href="#代码地址-AidlSample" class="headerlink" title="代码地址 AidlSample"></a>代码地址 <a href="https://github.com/BreakZero/AidlSample">AidlSample</a></h2>]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>AIDL</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>优雅实现Ktor Client请求异常处理</title>
    <url>/posts/2024/01/18/36ef9a4a/</url>
    <content><![CDATA[<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><p>参考官方文档简单处理特殊 Status Code 响应错误，最简单的方式则是打印日志，当然可以根据自己的情况来包装。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">private suspend fun ResponseException.debugLogging() {
    when (this) {
        is RedirectResponseException -&gt; {
            // Status codes 3XX
            val errorString = response.body&lt;String&gt;()
            println("===== $errorString")
        }

        is ClientRequestException -&gt; {
            // status codes 4XX
            println("===== status codes 4XX")
        }

        is ServerResponseException -&gt; {
            // status codes 5XX
            println("===== status codes 5XX")
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>整合模板代码实现如下</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">internal suspend inline fun &lt;reified T&gt; HttpClient.doGetWithCatch(
    urlString: String,
    isThrows: Boolean = false,
    block: HttpRequestBuilder.() -&gt; Unit = {}
): T? {
    return try {
        get(urlString, block).body&lt;T&gt;()
    } catch (exception: ResponseException) {
        exception.debugLogging()
        if (isThrows) throw exception else null
    } catch (ioException: IOException) {
        if (isThrows) throw ioException else null
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>上面代码是适配get请求的，post一样，就不拷贝出来了。 加了一个参数<code>isThrows</code>作为是否将异常往上抛让调用者进行处理的标识。</p>
<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">override suspend fun getCoinsMarkets(
    currency: String,
    page: Int,
    numCoinsPerPage: Int,
    order: String,
    includeSparkline7dData: Boolean,
    priceChangePercentageIntervals: String,
    coinIds: String?
): List&lt;CoinGeckoMarketsDto&gt; {
    return httpClient.doGetWithCatch&lt;List&lt;CoinGeckoMarketsDto&gt;&gt;("coins/markets") {
        parameter("vs_currency", currency)
        parameter("page", page)
        parameter("per_page", numCoinsPerPage)
        parameter("order", order)
        parameter("sparkline", includeSparkline7dData)
        parameter("price_change_percentage", priceChangePercentageIntervals)
        parameter("ids", coinIds)
    } ?: emptyList()
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>是不是看起来舒服多了？特别是在接口需要实现的方法多的时候，修改之后特有满足感</p>
]]></content>
      <categories>
        <category>随记</category>
      </categories>
      <tags>
        <tag>Ktor</tag>
      </tags>
  </entry>
  <entry>
    <title>是否应该将Compose中的状态拆分</title>
    <url>/posts/2023/07/10/6ae1f469/</url>
    <content><![CDATA[<h2 id="疑问"><a href="#疑问" class="headerlink" title="疑问"></a>疑问</h2><blockquote>
<p>Compose中单个页面是否应该使用单个状态进行管理？</p>
</blockquote>
<p>为什么会有以上这种疑问呢？难道不应该就是这样的吗？<br>其实不然，我们知道Compose是通过重组来刷新UI的，当一个<code>State</code>发生改变时，其对应的可重组函数的Lambda会重新执行，为了方便，我们通常会会直接使用一个完整的<code>State</code>来管理一个界面的显示。<br>所以我们在更新<code>State</code>时，而这个<code>State</code>的可重组范围是整个界面的Compose函数，基本会让整个界面都发生重组而不会跳过任何可重组项。<br>下面是一个简单的例子：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">// 一个整合的State
data class XModelState(
    val name: String,
    val age: Int,
    val intro: String
    ...
)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<span id="more"></span>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">Column(
    modifier = Modifier
    .fillMaxSize()
    .padding(16.dp)
) {
    
    println("Column")

    Wrapper {
        Text(text = "Name is ${modelState.name}").also {
            println("Name changed")
        }
    }
    Wrapper {
        Text(text = "Age is ${modelState.age}").also {
            println("Age changed")
        }
    }
    Wrapper {
        Text(text = "Intro is ${modelState.intro}").also {
            println("Intro changed")
        }
    }

    Button(
        modifier = Modifier.fillMaxWidth(),
        onClick = {
            modelState = modelState.copy(
                name = "Dougie ${Random.nextInt(0, 10)}",
                age = age + 1
            )
        }
    ) {
        Text(text = "Update Model")
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>以上代码中，当点击Update Model按钮来修改modelState时，3个Wrapper全部都发生重组。</p>
<pre class="mermaid">flowchart LR
A(Init State) --&gt;|Click Button| B(State changed) --&gt; |Trigger| C(re-compose)
</pre>

<p>但假设界面内容比较复杂，而这个State也很大很多属性，在更新其中某个或某些属性时，发生重组时，重组了整个可重组项，这不是我们想要的结果。我们想要的是，在某个或某些属性发生更新时，只重组使用到该属性的可重组项，而其他的则跳过（skip re-compose）.</p>
<p>为了避免这个情况，目前Compose也没提供相关的api，好像也只能通过分割状态了，对应文章的标题。</p>
<h2 id="拆分方式"><a href="#拆分方式" class="headerlink" title="拆分方式"></a>拆分方式</h2><h3 id="1-ViewModel层保留多个公开的State"><a href="#1-ViewModel层保留多个公开的State" class="headerlink" title="1. ViewModel层保留多个公开的State"></a>1. ViewModel层保留多个公开的State</h3><p>在ViewModel combine的时候就将State分割并提供多个State叫个UI层使用，而在操作的时候，我们也只会改变其对应的小State，其他State不会变化，<br>所以Compose上也只会重组其对应的可重组Lambda，而其他的则会跳过重组。</p>
<h3 id="2-使用-derivedStateOf"><a href="#2-使用-derivedStateOf" class="headerlink" title="2. 使用 derivedStateOf"></a>2. 使用 derivedStateOf</h3><p>在View层，依然是拿到整个大的State，我们通过增加内部属性并使derivedStateOf这个方法将State分割。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">val age by remember {
    derivedStateOf {
        modelState.age
    }
}
val intro by remember {
    derivedStateOf {
        modelState.intro
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>如上，将age和intro分割出来，在点击按钮是，age+1，而intro不变，通过日志，发现只有Age Changed，所以intro的被跳过重组了。</p>
<blockquote>
<p>Note: 在日常开发中，需求feature会越来越多，State也会随之而增加，以上两种方式虽然一定程度上能减少重组，但是也很大程度上增加了维护工作和带来新的问题。</p>
</blockquote>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在Compose上，UI的更新必然脱离不了重组，所以重组本身并不是大问题，我们需要注意的是尽量在可重组项中尽量少操作多余的工作。所以通常我们不必在意这些，因为Compose帮我做了很多优化的工作了。<br>当然如果在某个UI上，有其中一部份UI变化相对比较频繁，而其他则很少，此时我建议对状态进行拆分</p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack Compose</tag>
        <tag>Performance</tag>
      </tags>
  </entry>
  <entry>
    <title>如何避免生成代码/拷贝文件影响GitHub Languages stats</title>
    <url>/posts/2023/09/25/b9b953e9/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在GitHub Repository中，右边基本都会有一个名为<code>Languages</code>的Section，展示了该仓库所用语言的占比，而作为一个开发者，GitHub的信息多少能够体现owner的能力和经验，所以会存在通过api统计GitHub信息的需要。<br>例如：<br><img src="/images/post/top-langs.jpg" alt="BreakZero stats" title="BreakZero stats"><br>参考：<a href="https://github.com/anuraghazra/github-readme-stats">github-readme-stats</a></p>
<p>但是，有时候我们的仓库中会存在一些copy过来的文件(e.g: jQuery.js)或者是工具生成的代码，而这些的代码通常会比较大从而大大影响我们统计的实际结果。<br>同样，笔者在Profile中添加了Top Languages的分析，由于<code>Blog</code>这个仓库中使用了theme/生成的文件中存在大量的js文件，导致Top Languages 中javascript排名第一，而笔者对javascript只是了解和看懂的水平，所以统计出来的结果比较不符合实际。<br><code>Linguist</code>能帮你避免分析这类文件。</p>
<span id="more"></span>

<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown">Linguist is used on GitHub.com to detect blob languages, ignore binary or vendored files, suppress generated files in diffs, and generate language breakdown graphs.<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<p>更多请看 <a href="https://github.com/github-linguist/linguist">linguist</a></p>
<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p><code>Linguist</code> 能做的东西挺多，本文只根据笔者所遇情况介绍<code>Excluded from stats(从统计中排除)</code>的用法。<br>只需两步：</p>
<ol>
<li>在仓库根目录上创建一个名为<code>.gitattributes</code>的文件</li>
<li>按照文档将你需要标记的文件目录写入文件中，并使用对应的<code>attribute</code>标识<br> 例如: <pre class="line-numbers language-text" data-language="text"><code class="language-text">js/** linguist-vendored
css/** linguist-vendored
libs/** linguist-vendored<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
更多信息请看 <a href="https://github.com/github-linguist/linguist/blob/master/docs/overrides.md">Overrides</a></li>
</ol>
<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>如果你是在已存在的仓库中设置，当你提交后，可能发现它并没有效果，因为之前的所有提交并没有得到这些<code>attribute</code>处理文件。<br>解决也很简单</p>
<ol>
<li>首先确保当前没有其他提交，如果有，请先push或隐藏，可以使用命令查看 <pre class="line-numbers language-git" data-language="git"><code class="language-git">git status<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li>
<li>重置 <pre class="line-numbers language-git" data-language="git"><code class="language-git">git rm --cached -r .
git reset --hard<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
重置会让文件使用上定义的<code>attribute</code>s</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p><code>Linguist</code> 不仅在Language统计分析上能有效避免错误结果，同样支持不同语言定义和路径覆盖等功能。在每次创建和保存文件时，git都会根据定义的<code>attribute</code>s自动保存文件。</p>
<h2 id="Ending"><a href="#Ending" class="headerlink" title="Ending"></a>Ending</h2>]]></content>
      <categories>
        <category>Technical</category>
        <category>Tools</category>
      </categories>
      <tags>
        <tag>Github</tag>
      </tags>
  </entry>
  <entry>
    <title>Flutter学习记录 - Widget、State、Context</title>
    <url>/posts/2023/10/04/e5178a3c/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><blockquote>
<p>如何学习使用一门工具进行开发？</p>
</blockquote>
<p>对于这个问题，笔者认为应该从概念开始，了解概念后才更好去理解其原理，而只有在理解原理之后你才敢说会使用了。<br>类似Jetpack Compose，如果不理解State、可重组函数、重组作用域这些概念，又从何挖其原理呢？又如何去考虑避免或减少重组触发写出准确的代码呢？<br>笔者个人认为会使用，并不只是能够运用其出预期结果，而是会根据自己的理解使用正确/准确的方式实现预期结果。</p>
<p>Flutter是一个跨平台UI构架工具，其中的 <em><strong>Widget</strong></em>、<em><strong>State</strong></em>和<em><strong>Context</strong></em> 是每一个Flutter开发者都需要充分理解的重要概念之一，特别是对于初学者。<br>笔者作为一个Flutter初学者，本文将尝试解释这些概念，加深理解。</p>
<span id="more"></span>

<h2 id="Widget"><a href="#Widget" class="headerlink" title="Widget"></a>Widget</h2><p>Widget的中文意思是小部件，用户所看到的界面基本都是Widget，当然也可能是Platform View(平台视图)。<br>有这么一句话：在flutter中，一切都是Widget。其实在flutter中，每个widget专注的功能很小，比如padding/alignment，在其他语言中它是一个修饰的属性，但是在flutter中，它却是一个widget。这样做的好处就是我们开发者可以通过这些基础的widgets任意组合，从而构建出复杂，个性化的大widget。但是这样的缺点也很明显，就是套层。跟套娃一样，组合多了，层级也更多，代码看起来就不那么舒服了。<br>以下是常见Widget <code>Container</code>中的build方法</p>
<pre class="line-numbers language-dart" data-language="dart"><code class="language-dart">@override
Widget build(BuildContext context) {
  Widget? current = child;

  if (child == null &amp;&amp; (constraints == null || !constraints!.isTight)) {
    current = LimitedBox(
      maxWidth: 0.0,
      maxHeight: 0.0,
      child: ConstrainedBox(constraints: const BoxConstraints.expand()),
    );
  } else if (alignment != null) {
    current = Align(alignment: alignment!, child: current);
  }

  final EdgeInsetsGeometry? effectivePadding = _paddingIncludingDecoration;
  if (effectivePadding != null) {
    current = Padding(padding: effectivePadding, child: current);
  }

  if (color != null) {
    current = ColoredBox(color: color!, child: current);
  }

  if (clipBehavior != Clip.none) {
    assert(decoration != null);
    current = ClipPath(
      clipper: _DecorationClipper(
        textDirection: Directionality.maybeOf(context),
        decoration: decoration!,
      ),
      clipBehavior: clipBehavior,
      child: current,
    );
  }

  if (decoration != null) {
    current = DecoratedBox(decoration: decoration!, child: current);
  }

  if (foregroundDecoration != null) {
    current = DecoratedBox(
      decoration: foregroundDecoration!,
      position: DecorationPosition.foreground,
      child: current,
    );
  }

  if (constraints != null) {
    current = ConstrainedBox(constraints: constraints!, child: current);
  }

  if (margin != null) {
    current = Padding(padding: margin!, child: current);
  }

  if (transform != null) {
    current = Transform(transform: transform!, alignment: transformAlignment, child: current);
  }

  return current!;
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>通过代码可以看到，current这个widget通过一步一步根据使用传递的参数进行判断从而嵌入到不同的widget组合而成。</p>
<h3 id="StatelessWidget-StatefulWidget"><a href="#StatelessWidget-StatefulWidget" class="headerlink" title="StatelessWidget &amp; StatefulWidget"></a>StatelessWidget &amp; StatefulWidget</h3><p>根据Widget是否包含状态，Widget又分为<code>StatelessWidget</code>和<code>StatefulWidget</code>两种Widget。<br>StatelessWidget无状态小部件，是不会随着绑定的值的改变而动态改变的。所以StatelessWidget的生命周期很简单: 初始化 –&gt; 通过build方法渲染。<br>StatefulWidget有状态小部件，和StatelessWidget刚好相反，它需要绑定一个状态，且build方法在State中。当这个状态在改变的时候，将强制重建Widget。<br>StatefulWidget的生命周期如下：</p>
<pre class="mermaid">stateDiagram
[*] --&gt; createState
createState --&gt; initState
initState --&gt; didChangeDependencies
didChangeDependencies --&gt; build
build --&gt; addPostFrameCallback
addPostFrameCallback --&gt; didUpdateWidget
didUpdateWidget --&gt; deactivate
deactivate --&gt; dispose
dispose --&gt; [*]
</pre>

<h2 id="State"><a href="#State" class="headerlink" title="State"></a>State</h2><p>前面我们了解了Widget，在flutter中，Widget的构建是基于State的，所以State是数据，是用来渲染widget的数据。<br>在flutter应用中，State又分为<code>Ephemeral State</code>和<code>App State</code>。</p>
<h3 id="Ephemeral-State"><a href="#Ephemeral-State" class="headerlink" title="Ephemeral State"></a>Ephemeral State</h3><p>Ephemeral State是局部状态，一般作用于某个或某些Widget，在开发的过程中，我们可以针对某个页面/页面部分内容进行整理和定义Ephemeral State，而不需要使用状态管理技术来处理这些状态，使用<code>setState()</code>方法实现目的即可。</p>
<h3 id="App-State"><a href="#App-State" class="headerlink" title="App State"></a>App State</h3><p>App State是全局状态，它作用于整个App范围。相对于Ephemeral State，App State处理情况会更复杂点，它可以运用于整个app中任意时间的任意界面，而不是对某个或某些特定的Widget来管理。所以这里我们需要用到状态管理技术。<br>了解更多: <a href="https://docs.flutter.dev/data-and-backend/state-mgmt/options">List of state management approaches</a></p>
<h2 id="Context"><a href="#Context" class="headerlink" title="Context"></a>Context</h2><p>这里所谓的Context实际指的是BuildContext，每个Widget都有一个build方法，这个方法就有一个参数便是BuildContext。上文提到过，flutter的Widget专注于更小的功能，即使一个简单的Container也是由多个Widget组合而成，那么其组合之间的关系是通过什么管理的呢？ 任何的Widget都可重用到其他地方，所以它在Widget组合树中，Widget不会保留任何信息。在Widget抽象类中有这么个方法<code>createElement</code>，其注释<code>Inflates this configuration to a concrete instance.</code> 翻译过来就是将配置扩展为具体实例。<br>另外在flutter源码中，BuildContext上也有这么一句注释</p>
<pre class="line-numbers language-dart" data-language="dart"><code class="language-dart">[BuildContext] objects are actually [Element] objects. The [BuildContext] interface is used to discourage direct manipulation of [Element] objects.<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<p>所以Widget和Context之间的关系是一对一的，而维护Widget树的关联信息在Element中，而Context则是为了避免直接操作Element。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>Widget是flutter UI的基本，分为<code>StatelessWidget</code>和<code>StatefulWidget</code>，如果一个Widget在其生命周期中考虑变更且变更后将强制重建，这时候使用StatefulWidget，否则使用StatelessWidget。</li>
<li>State是Widget的数据信息，分为<code>Ephemeral State</code>和<code>App State</code>，<code>Ephemeral State</code>多数用于StatefulWidget并在其内部管理，用<code>setState</code>方法实现更新。<code>App State</code>一般都通过状态管理技术进行管理。</li>
<li>BuildContext和Widget存在一对一的关系，每个Widget都有一个Context。Context实际上是Element object，在构建一个widget时，会调用widget的createElement方法创建Element，而element对象中又存在parent,children,size等信息，从而构成element tree。</li>
</ol>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Multiplatform</category>
      </categories>
      <tags>
        <tag>Flutter</tag>
      </tags>
  </entry>
  <entry>
    <title>浅谈Jetpack Compose LaunchedEffect</title>
    <url>/posts/2023/07/04/c6b943f6/</url>
    <content><![CDATA[<p>在Jetpack Compose 代码中，我们经常看到<code>LaunchedEffect</code>这个常见的。今天就简单了解下这个方法吧。在说<code>LaunchedEffect</code>前，让我们先说一下<code>Side Effect</code>.</p>
<h2 id="什么是Side-Effect"><a href="#什么是Side-Effect" class="headerlink" title="什么是Side Effect"></a>什么是Side Effect</h2><p>在编程语言中，<code>Side-Effect</code>指的是调用函数时，除了返回可能的函数值外，该函数还对函数范围外的变量，参数等进行了修改。<br>举个🌰，看下面的函数，它有2个参数，返回它们的和，但是没有对任何其他变量有修改，所以它是没有副作用(No Side-Effect)的</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">fun sum(number1: Int, number2: Int) = number1 + number2<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<p>但如果我们将函数修改成下面的实现，每次调用该函数都会对sum重新赋值，而sum又是在函数范围外，所以认为这个函数有一个副作用</p>
<pre class="line-numbers language-Kotlin" data-language="Kotlin"><code class="language-Kotlin">var sum = 0
fun sum(number1: Int, number2: Int) {
    sum = number1 + number2
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>

<h2 id="在Jetpack-Compose-中，什么是LaunchedEffect"><a href="#在Jetpack-Compose-中，什么是LaunchedEffect" class="headerlink" title="在Jetpack Compose 中，什么是LaunchedEffect"></a>在Jetpack Compose 中，什么是LaunchedEffect</h2><p>根据源码我们看到，<code>LaunchedEffect</code>是一个带有<code>@Composable</code>的函数，注释如下</p>
<span id="more"></span>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">When LaunchedEffect enters the composition it will launch block into the composition's CoroutineContext. The coroutine will be cancelled and re-launched when LaunchedEffect is recomposed with a different key1 or key2. The coroutine will be cancelled when the LaunchedEffect leaves the composition.
This function should not be used to (re-)launch ongoing tasks in response to callback events by way of storing callback data in MutableState passed to key. Instead, see rememberCoroutineScope to obtain a CoroutineScope that may be used to launch ongoing jobs scoped to the composition in response to event callbacks.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<p>简单来说，<code>LaunchedEffect</code>是一个可在当前可组合项的作用域内运行挂起函数(block是suspend的)的可组合函数。<br>在<a href="https://developer.android.com/jetpack/compose/mental-model">Thinking in Compose</a>中，我们了解到一个可组合项应该是没有副作用的，如果我们想要在可组合项中修改应用状态，需要通过Effect API启动协程操作，所以可以说<code>LaunchedEffect</code>在Jetpack Compose中提供了在可组合项中调用挂起函数的能力。</p>
<p>下面我们通过一个例子简单了解下， 这个例子是一个TodoTask下的一个小功能，根据选择的类别展示当前类别的所有Task。<br>简单的准备mock tasks，代码如下：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">data class TodoTask(
    val id: Long,
    val name: String,
    val description: String
)

// mock tasks respose
val mockTasks = (1..10).map {
    TodoTask(
        id = it.toLong(),
        name = "task name $it",
        description = "task description $it"
    )
}
// mock get tasks from api, delay 2 seconds
private suspend fun getTasks(): List&lt;TodoTask&gt; {
    delay(2000)
    return mockTasks
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>在TodoListOfCategoryScreen主要是一个Lazy Column用来展示任务名称的，其中参数是Category</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Composable
fun TodoListOfCategoryScreen(
    category: String
) {
    println("TASK TAG: out of column scope")
    var tasks by remember {
        mutableStateOf(emptyList&lt;TodoTask&gt;())
    }
    Column(
        modifier = Modifier.fillMaxSize()
    ) {
        println("TASK TAG: TodoListScreen: ${tasks.size}, category: $category")

        LaunchedEffect(key1 = category) {
            tasks = getTasks()
            println("TASK TAG: get tasks from api, category: $category")
        }
        LazyColumn(
            modifier = Modifier.fillMaxSize()
        ) {
            items(tasks, key = {
                it.id
            }) {
                Text(modifier = Modifier.fillMaxWidth(), text = it.name)
            }
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>首先我们的category是固定的，在getTasks之后，修改了tasks导致Column发生重组，所以scope相关的日志打印了2次。而模拟网络请求的<code>LaunchedEffect</code>内只调用了一次，是因为<code>LaunchedEffect</code>的调用时期只有进入重组项或者Key变化重组，如果是普通的启动协程，在重组时，会导致多次调用api请求。<br><img src="/images/codes/launched_effect_1.jpg" alt="Logcat" title="launched_effect"></p>
<blockquote>
<p>PS: 这里为什么out of column scope也会打印2次呢，其实是因为Column是inline的， 它只能共享调用方的重组范围。</p>
</blockquote>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><code>LaunchedEffect</code>是在Jetpack Compose中提供了在可组合项中调用挂起函数的能力的一种方式。</li>
<li>在进入组合项时，<code>LaunchedEffect</code>会启动一个协程并执行挂起代码块(block)，在退出组合项时，协程将取消</li>
<li>使用不同的key(参数key1, key2)重组时，会取消当前协程并启动新的协程执行挂起函数</li>
<li>写<code>LaunchedEffect</code>函数时，最少要一个key(IDE 会有提示)</li>
<li><code>LaunchedEffect</code>的函数调度器是主线程</li>
</ul>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Jetpack Compose</tag>
      </tags>
  </entry>
  <entry>
    <title>Compose的重组作用域</title>
    <url>/posts/2023/07/06/c60ea2e3/</url>
    <content><![CDATA[<h2 id="什么是重组"><a href="#什么是重组" class="headerlink" title="什么是重组"></a>什么是重组</h2><p>在了解Compose的重组作用域之前，我们先来了解下重组是什么<br>对于Jetpack Compose，即使是初学者也都知道它被称为声明式UI开发，相对于之前通过xml方式的命令式UI。而在命令式UI模式下，如果我们想要更新一个widget，我们需要拿到这个widget通过调用其setter函数来改变其内部状态。但是Jetpack Compose中的widget相对无状态并不提供setter/getter函数，而是重新调用可组合函数来实现更新，这个过程就叫重组。目前Jetpack Compose重组是在UI线程执行的，所以重组过程是有性能消耗的。为此，Compose编译器内部做了大量工作保证重组的范围尽可能小来提升重组效率。</p>
<h2 id="重组范围"><a href="#重组范围" class="headerlink" title="重组范围"></a>重组范围</h2><p>重组范围在Compose中是一个很重要的组成部份，通过尽量缩小重组范围来减少Compose在准备每一帧时的工作量。</p>
<ul>
<li>它们是通过重组更新Compose节点的最小单位</li>
<li>它们通过跟踪观察基于快照的状态对象，并在这些状态变化时会无效<span id="more"></span>
对于每个<code>non-inline</code>并且返回<code>Unit</code>的可组合函数， Compose编译器会生成代码将这个函数包装到一个可重组块中，当这个代码块被标记为Invalidated时，Compose Runtime将确保在渲染下一帧之前该代码块被重新执行（重组）。<br>PS: <p style="color:red">是怎样被标记为Invalidated，这块关系到State/Snapshot暂时不展开，反正先记住，不依赖State的代码是不参与重组的。</p></li>
</ul>
<h2 id="为什么要non-inline且没有返回值的函数才可作为可重组块"><a href="#为什么要non-inline且没有返回值的函数才可作为可重组块" class="headerlink" title="为什么要non-inline且没有返回值的函数才可作为可重组块"></a>为什么要non-inline且没有返回值的函数才可作为可重组块</h2><p>inline函数，在编译过程中会直接将函数体拷贝到调用处，所以它只能共享调用者的重组范围<br>对于有返回值的函数，由于返回值的变化会影响调用者，因此无法单独重组，而必须连同调用方一同参与重组，因此它不能作为入口被标记为invalid。假设如果只是单独发生重组，其返回值改变了，但是其调用者并没有重组，还是使用的旧值，这不是预期的结果。</p>
<h2 id="表达式和函数调用"><a href="#表达式和函数调用" class="headerlink" title="表达式和函数调用"></a>表达式和函数调用</h2><p>在深入了解最小重组范围前，我们先了解一个关于常规旧函数调用如何工作的基本概念。在Java/Kotlin中，当你将表达式传递给函数时，该表达式会在调用函数中计算。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">println("hello" + "world")<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<p>它等价于</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">val arg = "hello" + "world"
println(arg)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>
<p>简而言之，先执行表达式得到结果，再执行函数调用（了解这个，对于确认重组作用域有一定的帮助）</p>
<h2 id="确认重组作用域"><a href="#确认重组作用域" class="headerlink" title="确认重组作用域"></a>确认重组作用域</h2><p>大致了解了表达式和函数调用后，我们通过一个简单的例子过一下如何确认重组作用域。先看下面的代码块</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Composable fun Foo() {
    var text by remember { mutableStateOf("") }
    println("TAG: Foo Scope")
    Button(onClick = { text = "$text\n$text" }) {
        println("TAG: Button lambda Scope")
        Text(text).also {
            println("TAG: Text Scope")
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<h3 id="1-为什么text改变后，不是只有Text发生重组"><a href="#1-为什么text改变后，不是只有Text发生重组" class="headerlink" title="1. 为什么text改变后，不是只有Text发生重组"></a>1. 为什么text改变后，不是只有Text发生重组</h3><p>点击Button时，State <code>text</code> 被重新赋值而改变，而State <code>text</code>只有Text访问到，为什么重组范围不是只有Text？<br>从上面代码块看，text是通过<code>by</code>关键字再根据<code>MutableState</code>的代理调用<code>getValue</code>得到结果的，它是一个表达式，而Text则是一个函数，通过前面我们对函数使用表达式作为参数的调用关系了解，在调用函数之前会先确认表达式的结果，确认表达式的结果在这个例子中是在<code>Button lambda</code>中。所以能确认重组作用域在<code>Button lambda</code>。</p>
<h3 id="2-为什么不是整个Foo发生重组"><a href="#2-为什么不是整个Foo发生重组" class="headerlink" title="2. 为什么不是整个Foo发生重组"></a>2. 为什么不是整个Foo发生重组</h3><p>我们知道当State发生变化时才会发生重组，而Foo中没有访问任何State对象，所以它不会发生重组。让我们修改下代码看看，将<code>by</code>改成<code>=</code>，逻辑就清晰了</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Composable fun Foo() {
    val text: MutableState&lt;String&gt; = remember { mutableStateOf("") }
    println("TAG: Foo Scope")
    Button(onClick = { text.value = "${text.value}\n${text.value}" }) {
        println("TAG: Button lambada Scope")
        Text(text.value).also {
            println("TAG: Text Scope")
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>显而易见，text一直都是同一个MutableState对象，修改的只不过是它内部持有的value，而value只在Text中访问，所以Foo不会发生重组。</p>
<h3 id="3-为什么不是Button发生重组"><a href="#3-为什么不是Button发生重组" class="headerlink" title="3. 为什么不是Button发生重组"></a>3. 为什么不是Button发生重组</h3><p>因为<code>Foo</code>没发生重组，所以重组时并不会调用Button函数</p>
<h3 id="4-onClick-lambada表达式呢"><a href="#4-onClick-lambada表达式呢" class="headerlink" title="4. onClick lambada表达式呢"></a>4. onClick lambada表达式呢</h3><p>重组范围只能是在可重组函数中，即使handlers，<code>Button</code>的<code>onClick</code>都不是可重组函数。所以这种函数不在重组范围内。</p>
<h2 id="重组中-Inline-的坑"><a href="#重组中-Inline-的坑" class="headerlink" title="重组中 Inline 的坑"></a>重组中 Inline 的坑</h2><p>前面也提到了，inline函数是将代码拷贝，所以共享调用者的重组范围。在Compose中很多常见的widget被修饰成inline函数的，比如<code>Column</code>,<code>Row</code>,<code>Box</code>等。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>Jetpack Compose是通过重组来刷新UI的</li>
<li>不依赖State的可重组函数不参与重组（因为没有观察和通知触发重组）</li>
<li>重组范围只能是在可重组函数中</li>
<li>inline可重组函数共享调用者作用域</li>
</ol>
<p>原文地址：<a href="https://dev.to/zachklipp/scoped-recomposition-jetpack-compose-what-happens-when-state-changes-l78">Scoped recomposition in Jetpack Compose — what happens when state changes?</a></p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack Compose</tag>
      </tags>
  </entry>
  <entry>
    <title>MVVM/MVI in Android</title>
    <url>/posts/2024/03/06/8c99ce9c/</url>
    <content><![CDATA[<p>还记得刚开始接触Android开发工作，哪有什么架构模式选择，几乎所有逻辑都放到Activity中。随着时间推移，后来听过MVC，用过MVP，MVVM和对MVI模糊的理解（为何是模糊的理解，因为我在使用的时候，并没有觉得和MVVM有差异，也不知道是否正确地使用&gt;_&lt;）</p>
<p>而这两年通过在项目实践过程中的经验积累，多少对近年比较流行的MVVM/MVI有了一定的理解。想尝试写篇博文，如有错误请指出。</p>
<h2 id="MVVM"><a href="#MVVM" class="headerlink" title="MVVM"></a>MVVM</h2><p>MVVM是Model-View-ViewModel的缩写，是前些年在Android中比较火的一个词。是Android开发中备受欢迎的架构模式，其架构图如下：</p>
<img src="/posts/2024/03/06/8c99ce9c/mvvm_architecture.jpg" class="" title="mvvm_architecture">
<p>图片来自<a href="https://www.geeksforgeeks.org/mvvm-model-view-viewmodel-architecture-pattern-in-android/">MVVM (Model View ViewModel) Architecture Pattern in Android</a></p>
<p><strong>Model</strong>： 模型，业务领域模型<br><strong>View</strong>： 用户界面，展示给用户能看到的。可以是Activit/Fragment(xml)或者Compose中的可组合项。一般通过观察的方式绑定业务模型数据实现界面同步更新<br><strong>ViewModel</strong>： 前两者数据连接的桥梁。其职责有</p>
<ul>
<li>根据View操作事件，实现业务逻辑处理</li>
<li>实现View和Model数据绑定，同步更新</li>
</ul>
<p>基本工作流程: </p>
<ol>
<li>界面在初始化完成后，ViewModel会将View和Model进行双向绑定(如：xxxBinding.setViewModel(**))</li>
<li>用户操作界面，触发View操作事件</li>
<li>ViewModel收到View的操作事件，实现业务逻辑处理</li>
<li>业务逻辑处理结果导致Model内容改变，用户界面观察到内容改变从而更新界面</li>
</ol>
<p>优点：</p>
<ul>
<li>解偶度高。View和Model之间完全解偶</li>
<li>提高开发效率。基于双向绑定，在开发过程中无需手动更新UI，关注点侧重放在业务逻辑和Model内容关系上，降低开发复杂性和工作量</li>
</ul>
<p>缺点：</p>
<ul>
<li>双向绑定导致Model变化难以跟踪，增加错误调试难度</li>
</ul>
<h2 id="MVI"><a href="#MVI" class="headerlink" title="MVI"></a>MVI</h2><p>MVI，Model-View-Intent的缩写，网上说法各种各样，唯独对于Intent层基本都是理解为用户意图。来自官方的架构图:</p>
<img src="/posts/2024/03/06/8c99ce9c/UDF_works_in_app_architecture.jpg" class="" title="how_UDF_works_in_app_architecture">

<p>MVI有两大特点：</p>
<ul>
<li>状态统一管理<br>MVI中还有一个概念，UiState。它一般是不可变的(data class)，每一个界面对应一个UiState</li>
<li>数据单向流动<br>根据上图，了解UDF状态是自上而下，而事件则自下而上形成一个环<img src="/posts/2024/03/06/8c99ce9c/cycle_of_events_and_data_in_UDF.jpg" class="" title="cycle_of_events_and_data_in_UDF"></li>
</ul>
<p>以下则是笔者参考并结合自己开发过程的理解来说明的</p>
<p><strong>Model</strong>: 如MVVM一样，领域模型。UiState根据Model变化而改变<br><strong>View</strong>: 如MVVM一样，展示给用户的界面<br><strong>Intent</strong>: 用户意图，用户对界面功能的操作意图。比如想要返回而点击按钮</p>
<p>基本工作流程: （参考自<a href="https://developer.android.com/topic/architecture/ui-layer#state-holders">官方文档</a>）</p>
<ol>
<li>ViewModel持有并展露UiState，View会根据UiState渲染界面</li>
<li>用户通过意图操作通知ViewModel</li>
<li>ViewModel根据用户意图处理逻辑并更新状态(UiState)</li>
<li>对于任何状态都是重复以上步骤</li>
</ol>
<p>优点:</p>
<ul>
<li>数据单向流动保证了数据的一致性，提高了可测试性和调试便利</li>
<li>统一管理状态，一个界面对应一个状态，加强了代码可读性</li>
</ul>
<p>缺点:</p>
<ul>
<li>由于Intent的存在，需要定义Event并通过UI传递给ViewModel，会导致ViewModel逻辑代码增加。</li>
</ul>
<h2 id="说一说"><a href="#说一说" class="headerlink" title="说一说"></a>说一说</h2><p>对于MVVM的实现，相信大多开发者都跟笔者差不多，使用DataBinding/LiveData吧，不过后来Kotlin出现了StateFlow/SharedFlow，LiveData就也被替换了。</p>
<ol>
<li><p>MVVM with DataBinding<br>  对于DataBinding，对于目前声明式UI的实现无需考虑。在命令式UI时候，通过外层加<code>&lt;layout&gt;&lt;/layout&gt;</code>使得代码生成Binding类，然后通过set方法持有Model对象实现View &lt;-&gt; Model之间的绑定。<br>  不过随着界面的增加，编译速度会越来越慢，而且出问题的时候相对难定位。在Model中，更是几乎每个属性都需要用ObservableField，如遇到不支持的Binding还需要手动添加BindingAdapter支持等。</p>
</li>
<li><p>MVVM with LiveData/StateFlow<br>  在这套MVVM实现方式中，你需要更多了解LiveData和StateFlow，因为出问题的时候很多是因为对LiveData/StateFlow原理实现不理解，如为什么突然界面就刷新了？为什么数据被覆盖而显示错误的界面等。</p>
</li>
<li><p>MVI<br>  在学习Jetpack Compose过程中，笔者使用最多的就是MVI架构模式了。因为MVI中设计的UiState和Jetpack Compose重组刷新界面正好可以一一对应。在开发过程中会根据需求和界面定义好各种UiState，然后只需要考虑好这些UiState在ViewModel经过逻辑之后的输出正确即可(对于单元测试挺友好的)。<br>  在使用MVI开发过程中，也曾有些时候矛盾过，创建这么多类真的有必要吗？但是好像为了按照架构来实现又不能删掉。</p>
</li>
</ol>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>其实在实际开发过程中，大多时候MVVM也渐渐从双向绑定走向了数据单向流实现。从Google官方对架构的介绍中可以看出，类似的MVI很大程度上和MVVM相同的。</p>
<p>在Google介绍的MVI中，每个UiState对应一个UI界面，这对于声明式UI特别友好。比如Jetpack Compose中，收到UiState触发重组即可。但对于传统的xml开发界面，你会发现判断条件并没有少多少。</p>
<p>由于这种架构模式的设计，为了解耦，层级分明导致需要更多类来实现，对于一些简单的app或者feature来说，笔者有时候看着空荡荡的类会觉得其实没必要。所以在接受范围内做一些妥协并没有什么。</p>
<p>其实不管哪种实现，在开发过程中，都会因为需求不同而遇到各种各样问题的，没有任何一种解决方案是完美的。 开发的过程要有自己的思想和理解，才能更有效提升。</p>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
  </entry>
  <entry>
    <title>Jetpack Compose实现放大镜效果竟如此简单</title>
    <url>/posts/2024/01/06/a0983be/</url>
    <content><![CDATA[<h3 id="例子说明"><a href="#例子说明" class="headerlink" title="例子说明"></a>例子说明</h3><p>在一个Box中，显示图片并支持放大镜功能。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><p>直接上代码</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">var offset by remember {
    mutableStateOf(Offset.Zero)
}
Box(
    modifier = Modifier
        .fillMaxWidth()
        .wrapContentHeight()
        .pointerInput(true) {
            detectDragGestures { change, _ -&gt;
                // 跟踪手指在屏幕的位置
                offset = change.position
            }
        }
        .magnifier(
            // 源图片中心位置
            sourceCenter = { offset },
            // 放大镜中心位置，为了错开手指，加上y轴偏移量
            magnifierCenter = { offset - Offset(0f, 200f) },
            // 样式，这个样式展示了圆形的放大镜，可按设计要求调整
            style = MagnifierStyle(
                size = DpSize(100.dp, 100.dp),
                cornerRadius = 100.dp
            )
        )
) {
    Image(
        modifier = Modifier.fillMaxWidth(),
        contentScale = ContentScale.FillWidth,
        painter = painterResource(id = R.drawable.source_code),
        contentDescription = null
    )
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>以上代码很简单，没有什么好解释的。</p>
<hr>
<p>效果如下：</p>
<img src="/posts/2024/01/06/a0983be/magnifier.gif" class="" title="magnifier">]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Jetpack Compose</tag>
      </tags>
  </entry>
  <entry>
    <title>RPC with Kotlinx Serialization</title>
    <url>/posts/2022/06/08/ed292b98/</url>
    <content><![CDATA[<blockquote>
<p>因为将项目从Retrofit切换到Ktor和gson切换到kotlinx serialize。而kotlinx serialize和gson的实现差异导致在rpc接口中对于rpc 请求参数类型不一致的情况下，序列化出现了异常报错，比如类似一下的请求Body</p>
</blockquote>
<pre class="line-numbers language-json" data-language="json"><code class="language-json">{
    "id": 0,
    "jsonrpc": "2.0",
    "method": "eth_call",
    "params": [
        {
            "data": "0x5ec88c7900000000000000000000000081080a7e991bcdddba8c2302a70f45d6bd369ab5",
            "from": "0x81080a7e991bcDdDBA8C2302A70f45d6Bd369Ab5",
            "to": "0xb3831584acb95ED9cCb0C11f677B5AD01DeaeEc0"
        },
        "latest"
    ]
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<h2 id="List-Contextual"><a href="#List-Contextual" class="headerlink" title="List<Any> + @Contextual"></a><code>List&lt;Any&gt;</code> + @Contextual</h2><p>本来以为在Kotlinx Serialize上会像Gson一样轻松解决，一开始便对于原来的数据结构定义成</p>
<span id="more"></span>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Serializable
data class TestBaseRpc(
    val jsonrpc: String,
    val method: String,
    val params: List&lt;@Contextual Any&gt;,
    val id: Int
)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>并在Json 配置的时候将 Contextual配置上</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">Json {
    useArrayPolymorphism = true
    prettyPrint = true
    allowStructuredMapKeys = true
    serializersModule = SerializersModule {
        contextual(Any::class) {
            TestCaller.serializer()
            String.serializer()
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>将单元测试代码跑起来后发现，contextual 只使用了最后一个serializer来解析Any类型，所以这个方向无法实现想要的结果<br>报错如下：</p><p style="color:red">***.TestCaller cannot be cast to class java.lang.String</p><p></p>
<blockquote>
<p>Note: 如果将两个serializer调换顺序就会发现报错也会两个类型互换</p>
</blockquote>
<h2 id="通过-Sealed-Interface-Polymorphic"><a href="#通过-Sealed-Interface-Polymorphic" class="headerlink" title="通过 Sealed Interface + Polymorphic"></a>通过 Sealed Interface + Polymorphic</h2><p>polymorphic多态的意思，顾名思义，可以根据实际情况进行选择对应的Serializer，所以需要有一个基类(可以interface，sealed…)</p>
<ol>
<li>首先定义一个interface<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">internal interface Parameter<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li>
<li>定义subclass<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">internal sealed interface Parameter {
    @Serializable
    data class CallParameter(
        val data: String,
        val from: String,
        val to: String
    ) : Parameter

    @Serializable(with = IntListParameterSerializer::class)
    data class IntListParameter(
        val items: List&lt;Int&gt;
    ) : Parameter

    @Serializable(with = StringParameterSerializer::class)
    data class StringParameter(
        val content: String
    ) : Parameter
    ...
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
<li>配置Json模块，绑定subclass的Serializer<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">Json {
    prettyPrint = true
    isLenient = true
    ignoreUnknownKeys = true
    allowStructuredMapKeys = true
    serializersModule = SerializersModule {
        polymorphic(List::class) {
            ListSerializer(ParameterSerialize)
        }
        polymorphic(Parameter::class) {
            subclass(Parameter.CallParameter::class, Parameter.CallParameter.serializer())
            subclass(Parameter.StringParameter::class, Parameter.StringParameter.serializer())
            subclass(Parameter.IntListParameter::class, Parameter.IntListParameter.serializer())
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
<li>针对不同结构定义不同的Serializer<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">internal object IntListParameterSerializer : KSerializer&lt;Parameter.IntListParameter&gt; {
    override fun deserialize(decoder: Decoder): Parameter.IntListParameter {
        val items = decoder.decodeSerializableValue(ListSerializer(Int.serializer()))
        return Parameter.IntListParameter(
            items = items
        )
    }

    override val descriptor: SerialDescriptor
        get() = buildClassSerialDescriptor("IntListParameter") {
            this.element&lt;List&lt;Int&gt;&gt;("items")
        }

    override fun serialize(encoder: Encoder, value: Parameter.IntListParameter) {
        encoder.encodeSerializableValue(ListSerializer(Int.serializer()), value.items)
    }
}

internal object RpcRequestBodySerializer : KSerializer&lt;RpcRequestBody&gt; {
    override fun deserialize(decoder: Decoder): RpcRequestBody {
        return decoder.decodeStructure(descriptor) {
            var rpc: String? = null
            var method: String? = null
            var params: List&lt;Parameter&gt; = emptyList()
            var id: Int? = null
            loop@ while (true) {
                when (decodeElementIndex(descriptor)) {
                    CompositeDecoder.DECODE_DONE -&gt; break@loop

                    0 -&gt; rpc = decodeStringElement(descriptor, 0)
                    1 -&gt; method = decodeStringElement(descriptor, 1)
                    2 -&gt; params = decodeSerializableElement(descriptor, 2, ListSerializer(ParameterSerialize))
                    3 -&gt; id = decodeIntElement(descriptor, 3)
                }
            }
            RpcRequestBody(
                jsonrpc = rpc ?: "",
                method = method ?: "",
                params = params,
                id = id ?: 1
            )
        }
    }

    override val descriptor: SerialDescriptor
        get() = buildClassSerialDescriptor("RpcRequestBody") {
            this.element&lt;String&gt;("jsonrpc", isOptional = false)
            this.element&lt;String&gt;("method", isOptional = false)
            this.element&lt;List&lt;Parameter&gt;&gt;("params", isOptional = false)
            this.element&lt;Int&gt;("id", isOptional = false)
        }

    override fun serialize(encoder: Encoder, value: RpcRequestBody) {
        encoder.encodeStructure(descriptor) {
            encodeStringElement(descriptor, 0, value.jsonrpc)
            encodeStringElement(descriptor, 1, value.method)
            encodeSerializableElement(descriptor, 2, ListSerializer(ParameterSerialize), value.params)
            encodeIntElement(descriptor, 3, value.id)
        }
    }
}
...<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
<li>如果需要deserialize，便需要加上如下选择器<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">internal object ParameterSerialize : JsonContentPolymorphicSerializer&lt;Parameter&gt;(Parameter::class) {
    override fun selectDeserializer(element: JsonElement): DeserializationStrategy&lt;out Parameter&gt; {
        // override the condition for your point
        return when(element) {
            is JsonPrimitive -&gt; Parameter.StringParameter.serializer()
            is JsonArray -&gt; Parameter.IntListParameter.serializer()
            is JsonObject -&gt; Parameter.CallParameter.serializer()
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
运行单元测试，验证通过：<br><img src="/images/pic1.png" alt="Unit Test Result!" title="Unit Test Result"></li>
</ol>
<p>最后附上仓库链接:<br><a href="https://github.com/BreakZero/Kotlinx-Serialization-Example">Kotlinx-Serialization-Example</a></p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>只是为了在项目中减少gson的使用，所以花了点时间验证下具体情况，但是：<br>虽然目前看起来是解决了问题，不过相对来说比较麻烦，并且如果类型越来越多也会增加不必要的工作量，所以笔者觉得如果你的项目接口也有类似的情况，gson或许是个不错的选择</p>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Kotlin</tag>
        <tag>Kotlin Serialization</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP协议的3次握手</title>
    <url>/posts/2023/08/28/e4ef3026/</url>
    <content><![CDATA[<h2 id="TCP"><a href="#TCP" class="headerlink" title="TCP"></a>TCP</h2><p>TCP（全称：Transmission Control Protocol）是计算机网络中用于设备间通信的传输层协议的一种，它是面向连接的传输协议，<br>还提供了各种机制保证传输过程的可靠性，包括错误检查，流量控制等机制。</p>
<h2 id="3次握手"><a href="#3次握手" class="headerlink" title="3次握手"></a>3次握手</h2><p>要清晰理解3次握手的过程，先来想2个问题：</p>
<ol>
<li>3次握手的目的是什么？</li>
<li>通过什么保证握手过程客户端和服务端相互理解？</li>
</ol>
<h3 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h3><p>目的？TCP是面向连接的，所以很明显，3次握手的目的是建立连接。</p>
<h3 id="通过什么数据-或信息-使得客户端和服务端相互理解并成功建立连接？"><a href="#通过什么数据-或信息-使得客户端和服务端相互理解并成功建立连接？" class="headerlink" title="通过什么数据(或信息)使得客户端和服务端相互理解并成功建立连接？"></a>通过什么数据(或信息)使得客户端和服务端相互理解并成功建立连接？</h3><p>既然是建立连接，那自然需要沟通，3次握手就是3次沟通的过程，在这个沟通的过程中建立起连接。既然有沟通就需要消息承载沟通信息。</p>
<span id="more"></span>
<p>首先了解下TCP报文首部字段 (<a href="%22https://developer.aliyun.com/article/975295%22">拷贝来源</a>)</p>
<blockquote>
<ul>
<li>16位端口号：源端口号，主机该报文段是来自哪里；目标端口号，要传给哪个上层协议或应用程序</li>
<li>32位序号：一次TCP通信（从TCP连接建立到断开）过程中某一个传输方向上的字节流的每个字节的编号。</li>
<li>32位确认号：用作对另一方发送的tcp报文段的响应。其值是收到的TCP报文段的序号值加1。</li>
<li>4位头部长度：表示tcp头部有多少个32bit字（4字节）。因为4位最大能标识15，所以TCP头部最长是60字节。</li>
<li>6位标志位：URG(紧急指针是否有效)，ACK（表示确认号是否有效），PSH（缓冲区尚未填满），RST（表示要求对方重新建立连接），SYN（建立连接消息标志接），FIN（表示告知对方本端要关闭连接了）</li>
<li>16位窗口大小：是TCP流量控制的一个手段。这里说的窗口，指的是接收通告窗口。它告诉对方本端的TCP接收缓冲区还能容纳多少字节的数据，这样对方就可以控制发送数据的速度。</li>
<li>16位校验和：由发送端填充，接收端对TCP报文段执行CRC算法以检验TCP报文段在传输过程中是否损坏。注意，这个校验不仅包括TCP头部，也包括数据部分。这也是TCP可靠传输的一个重要保障。</li>
<li>16位紧急指针：一个正的偏移量。它和序号字段的值相加表示最后一个紧急数据的下一字节的序号。因此，确切地说，这个字段是紧急指针相对当前序号的偏移，不妨称之为紧急偏移。TCP的紧急指针是发送端向接收端发送紧急数据的方法。</li>
</ul>
</blockquote>
<p>注意6位标志位的描述。对于3次握手和4次挥手，我们只关注以下3个：</p>
<ul>
<li>SYN -&gt; Synchronize的缩写，表示请求初始化或建立连接</li>
<li>ACK -&gt; Acknowledgement的缩写，表示确认收到请求消息数据</li>
<li>FIN -&gt; Final的缩写，请求终止一个连接</li>
</ul>
<p>数据包头部字段：</p>
<ul>
<li>seq -&gt; Sequence number, 序列号，用于沟通并确保消息顺序的</li>
<li>ack -&gt; Acknowledge number，是对上一个包的序号进行确认的号，ack = seq + 1</li>
</ul>
<p>过程示意图：<br><img src="/images/tcp_3ways_handshake.png" alt="3Ways Handshake" title="3Ways Handshake"><br>经过3次握手建立连接：</p>
<ul>
<li><h4 id="第一次握手"><a href="#第一次握手" class="headerlink" title="第一次握手"></a>第一次握手</h4></li>
</ul>
<p>Client将SYN设置为1，并随意生成一个序列号值为x(seq = x)，包装好数据包发送至Server，进入等待Server确认状态。</p>
<ul>
<li><h4 id="第二次握手"><a href="#第二次握手" class="headerlink" title="第二次握手"></a>第二次握手</h4></li>
</ul>
<p>Server收到SYN消息，知道Client想建立连接，并作出回应。SYN和ACK设置为1，ack number = x + 1，随机产生一个序列号y给seq，包装数据包发送给Client，Server进入已接受并回应状态。</p>
<ul>
<li><h4 id="第三次握手"><a href="#第三次握手" class="headerlink" title="第三次握手"></a>第三次握手</h4></li>
</ul>
<p>Client收到Server的确认回应后，拿到确认回应的数据，检查对应的数据是否正确：</p>
<blockquote>
<ol>
<li>ack 是否为 x+1</li>
<li>ACK 是否和Client请求时一致</li>
</ol>
</blockquote>
<p>经过上面数据检查通过后，同样需要给Server发送确认回应消息，Server再收到回应消息同样做以上2步检查。<br>如果检查通过，则连接建立成功。Client和Server进入<code>ESTABLISHED</code>状态。完成3次握手。接下来可以相互开始传输数据了。</p>
<h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://www.geeksforgeeks.org/tcp-3-way-handshake-process/">TCP 3-Way Handshake Process</a><br><a href="https://packetlife.net/blog/2010/jun/7/understanding-tcp-sequence-acknowledgment-numbers/">Understanding TCP Sequence and Acknowledgment Numbers</a></p>
<!-- ## TCP和UDP两协议的区别？ -->
<!-- 作为一个Android开发，在面试的时候也经常会被问到这个问题。笔者一直好奇问这个的意义的点在哪？ -->
<!-- 毕竟貌似在移动端开发的时候几乎说很少甚至没有需要考虑这方面的情况（也许经验和项目上的原因吧）。哈哈哈，这些都是题外话啦！ -->
<!-- 回归正题。 -->

<!-- ## 区别 -->
<!-- TCP（Transmission Control Protocol）和UDP（User Datagram Protocol）都是网络中用于设备间通信的传输层协议。 -->
<!-- 它们最主要的区别是TCP是面向连接的协议，而UDP是无连接的协议。 -->

<!-- ### 1. 可靠性 -->
<!-- - TCP 是一种面向连接的协议，这意味着它在发送者和接收者之间建立了可靠有序的连接。它保证数据包会以正确的顺序、没有错误地传递。 -->
<!-- - UDP 是一种无连接协议，不像 TCP 那样提供同样级别的可靠性。它不能保证所有数据包都会被传递，数据包可能无序地到达或丢失，而且没有通知。 -->

<!-- ### 2. 传递顺序 -->
<!-- - 通过 TCP 发送的数据按照发送顺序进行传递。这对于需要顺序传递数据的应用程序（如网页或文件传输）非常重要。 -->
<!-- - UDP 不保证传递顺序。它适用于实时通信比严格顺序更重要的情况，比如流媒体或在线游戏等。 -->

<!-- ### 3. 错误检查 -->
<!-- - TCP 提供错误检查机制以确保数据完整性。如果数据包丢失或损坏，TCP 将请求重新传输缺失或损坏的数据。 -->
<!-- - UDP 不执行错误检查或纠正。如果需要，应用程序需要处理任何错误。 -->

<!-- ### 4. 流量控制 -->
<!-- - TCP 使用流量控制机制来管理发送者和接收者之间的数据传输速率。这有助于防止拥塞，并确保接收者能够处理传入的数据。 -->
<!-- - UDP 不包括内置的流量控制机制。如果数据发送过快，它可能会超负荷地影响接收者。 -->

<!-- ### 5. 资源开销 -->
<!-- - TCP 的开销较高，因为需要额外的连接建立、错误检查和流量控制机制。 -->
<!-- - UDP 的开销较低。 -->]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Interview</tag>
        <tag>HTTP</tag>
        <tag>TCP/IP</tag>
      </tags>
  </entry>
  <entry>
    <title>TODO List KMM 实践遇到的问题记录</title>
    <url>/posts/2023/08/21/92856721/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p> TODO List KMM是一个基于Kotlin Multiplatform Mobile实现的一个小项目，主要提供查看Todo task列表，增加/删除/编辑task功能，目前有加上sign in/sign up页面，但是没有整合后端，有时间可能会加上。当然，这并不重要，本篇文章主要为了介绍KMM项目结构和公共模块抽取的个人想法和学习实践过程的一个回顾。<br>项目地址在文末。</p>
<blockquote>
<p>至今，虽然Kotlin Multiplatform可以直接使用Compose实现两端UI了，但是它还处于非稳定版本，<br>而且实际上在iOS上存在卡顿现象，所以TODO List还是使用SwiftUI/Jetpack Compose分别实现Android/iOS 界面</p>
</blockquote>
<h2 id="基本结构"><a href="#基本结构" class="headerlink" title="基本结构"></a>基本结构</h2><p> 作为一个KMM项目，Android/iOS 这两个模块是必不可少的，它们分别是用来实现Android/iOS页面和使用或构建数据逻辑相关的代码，而shared当然也很重要，shared是两平台的共用代码。在大多数项目中，我们的数据来源双端都是一致的，所以shared模块完全可以作为一个数据<br>提供者，不管是来源是后端api还是本地，都是没有任何异议的。当然，除此之外，还可以提供一些工具扩展类。</p>
<h2 id="项目创建和配置"><a href="#项目创建和配置" class="headerlink" title="项目创建和配置"></a>项目创建和配置</h2><ol>
<li>首先我们创建一个KMM项目，catalogs管理依赖库的方式很早就被推荐使用了，所以我们将准备好的<code>libs.versions.toml</code>文件copy到<code>gradle</code>目录下<br><code>composite build</code>同样也是比较推荐的一种做法，从nowinandroid项目中copy过来，并加以修改放到我们项目中，命名为<code>build-logic</code>，里面有插件，<br>这些插件是为了减少的module下的gradle配置模板代码从而更易于管理。</li>
<li>配置好项目之后，确定shared模块主要需要实现哪些数据相关的功能，并将其分模块来实现，在该项目中有4个模块分别是core, data, database, model.</li>
</ol>
<ul>
<li>2.1 core 提供核心工具类或扩展方法</li>
<li>2.2 model 封装上层需要的数据结构，用于UI或者逻辑需要的过度Model类</li>
<li>2.3 database 提供本地存储和读取功能作用</li>
<li>2.4 data 数据源层，主要是combine本地或者远程api数据来源的组装和mapping等逻辑<span id="more"></span>
除了以上，同样可以添加一个domain层用来处理复杂的用例（domain层是可有可无的）</li>
</ul>
<ol start="3">
<li>根据配置精简各个模块的gradle 配置之后便可以专注于代码的编写了。在编写代码的时候，要考虑好哪些模块实现哪些功能，<br>解耦之类的问题。<blockquote>
<p>Noted: 很多时候，我们在UI层需要实现一些数据的转换（KMM Compiler生成的类和Swift的类型之间），在Swift上实现起来比较麻烦。<br>但是我们知道Kotlin中有Native和Object-C已经帮我们实现了很多数据的对应和方法，所以可以将这些转换放到iOSMain中实现。比如ByteArray和UIImage</p>
</blockquote>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">val data = bytes.usePinned {
    NSData.create(
    bytes = it.addressOf(0),
    length = bytes.size.toULong()
    )
}
return UIImage(data)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
</ol>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="sqldelight-上的坑"><a href="#sqldelight-上的坑" class="headerlink" title="sqldelight 上的坑"></a>sqldelight 上的坑</h3><p> 虽然依赖库中的gradle 已经在lineropts中加上了<code>-lsqlite3</code>，但是有时候在运行iOS平台项目还是会出现找不到的问题。<br>使用XCode打开iOS项目，在Build Settings中的Other linker flags上加上就好了</p>
<h3 id="Koin"><a href="#Koin" class="headerlink" title="Koin"></a>Koin</h3><p> 对于KMM项目，我个人觉得Koin是目前最舒服的一个依赖注入方式，其也只是作用于shared中的Kotlin代码，Swift还是需要通过调用init实例化操作。所以为了防止依赖注入的helper类随着项目扩大而mapping方法也增加到可怕的数量，建议一开始就管理分类。</p>
<h3 id="Flow和协程"><a href="#Flow和协程" class="headerlink" title="Flow和协程"></a>Flow和协程</h3><p> Swift的闭包很好用，对于suspend方法都有一个<code>completionHandler</code>的闭包回调，对于Flow当然也可以使用闭包在相关的时候进行回调。<br>比如:</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">// onEach(it) onThrow(it) onComplete()是闭包参数
flow.onEach { onEach(it) }
    .catch { onThrow(it) }
    .onCompletion { onComplete() }<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>
<blockquote>
<p>Note：需要注意的是，要清楚这个回调在哪个线程中，如果你需要在回调方法后实现dismiss当前界面或者其他UI操作，而协程又是在IO线程中执行，并没有切换会主线程再调用回调，这时候回调同样是在IO线程，而IO线程中的UI操作是无效果的（dismiss不会有效果）需要使用DispatchQueue.main切到主线程中<br><span style="color:red">那么我们是应该注意在kotlin代码上主动切回主线程再进行回调还是让其在Swift上来考虑呢？</span></p>
</blockquote>
<p><a href="https://github.com/BreakZero/TODO-LIST-KMM">TODO-LIST-KMM</a></p>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Kotlin</tag>
        <tag>SwiftUI</tag>
        <tag>Multiplatform</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Git Submodule减少Gradle构建配置模板代码</title>
    <url>/posts/2023/09/27/ddd9439e/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>最近为了学习和了解KMP，先后创建了<a href="https://github.com/BreakZero/TODO-LIST-KMM">TODO-LIST</a>和<a href="https://github.com/BreakZero/E-Wallet-KMP">E-Wallet</a>两个项目。在笔者每次创建新的项目之后，都要copy一个名为build-logic的文件夹到新的项目上去，虽然偶尔会根据情况进行一些修改，其实基本上是没有多大改动的，于是在想，如果通过submodule进行管理是不是会更方便些？</p>
<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p><a href="https://github.com/android/nowinandroid">nowinandroid</a>这个项目是一个非常值得去看去学习的，项目不大，阅读代码也不需要花太多时间。<br>在<code>nowinandroid</code>项目中，对<a href="https://github.com/android/nowinandroid/blob/main/docs/ModularizationLearningJourney.md">modularization</a>和<a href="https://github.com/android/nowinandroid/blob/main/docs/ArchitectureLearningJourney.md">architecture</a>有详细的介绍文档。而<code>build-logic</code>目的就在于实现modularization时，提供使用和配置的方便性。我们知道，多模块项目中，每个模块都有一个gradle配置文件，而构建配置的复杂性将影响着模块的同步时间和维护成本。另外，如果我们不整合管理，模块的增加将带来持续的维护成本。而约定插件可以将这些可重用和可组合的构建配置提取成kotlin代码中管理。在对应模块配置的时候apply对应的plugin id即可。</p>
<h2 id="修改"><a href="#修改" class="headerlink" title="修改"></a>修改</h2><h3 id="1-删除不确定依赖关系"><a href="#1-删除不确定依赖关系" class="headerlink" title="1. 删除不确定依赖关系"></a>1. 删除不确定依赖关系</h3><p>在<code>nowinandroid</code>中，<code>build-logic</code>作为一个单个项目的约定插件管理模块，其内部存在对外部其他模块的依赖。但如果想将其作为一个submodule，对外部这些不确定的依赖自然需要删除，所以在<a href="https://github.com/android/nowinandroid/blob/main/docs/ArchitectureLearningJourney.md">architecture</a>提到的各层相关模块依赖关系需要从中删除，保留基本的构建配置项即可。</p>
<h3 id="2-增加构建配置项支持"><a href="#2-增加构建配置项支持" class="headerlink" title="2. 增加构建配置项支持"></a>2. 增加构建配置项支持</h3><p>在<code>nowinandroid</code>中，<code>build-logic</code>中已有<code>jacoco</code>,<code>app</code>,<code>feature</code>,<code>library</code>,<code>hilt</code>等常用可组合构建配置插件，但是项目不同，构建配置需求也会不一样。就比如，在KMP(Android/iOS)项目中，在<code>app</code>,<code>feature</code>,<code>library</code>上的构建配置是不一样的，并且di上<code>koin</code>比<code>hilt</code>有更好的可用性。所以增加构建配置项的支持，能大大增加submodule的适配可行性。</p>
<h3 id="3-加上catalog的管理并保留可覆盖和可扩展性"><a href="#3-加上catalog的管理并保留可覆盖和可扩展性" class="headerlink" title="3. 加上catalog的管理并保留可覆盖和可扩展性"></a>3. 加上catalog的管理并保留可覆盖和可扩展性</h3><p>在可配置项中，比如<code>hilt</code>，<code>koin</code>，<code>jacoco</code>等，需要使用到依赖库的版本等信息，所以catalog的管理是需要的。笔者在仓库中增加了默认的版本库，并提供一些常用的依赖库参考使用。而且当使用者使用自己catalog并将名字设置为<code>libs</code>即可覆盖使用。</p>
<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><h3 id="Step1-使用submodule"><a href="#Step1-使用submodule" class="headerlink" title="Step1 使用submodule"></a>Step1 使用submodule</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">git submodule add https://github.com/BreakZero/build-logic<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
<h3 id="Step2-配置必须catalog"><a href="#Step2-配置必须catalog" class="headerlink" title="Step2 配置必须catalog"></a>Step2 配置必须catalog</h3><ul>
<li>Option 1 直接使用<code>build-logic</code>目录下的<code>default.versions.toml</code><br>在project目录下的<code>settings.gradle.kts</code>中，将下面的代码copy到<code>dependencyResolutionManagement</code>块中<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">versionCatalogs {
    create("libs") {
        from(files("./build-logic/default.versions.toml"))
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre></li>
<li>Option 2 使用自己的catalog文件</li>
</ul>
<ol>
<li>在项目<code>gradle</code>目录下，创建一个名为<code>libs.versions.toml</code>的文件</li>
<li>把<code>build-logic</code>目录下的<code>default.versions.toml</code>内容全部copy到你的catalog下，再自行添加其他的。（<code>catalogs</code>目录下有参考）</li>
</ol>
<h3 id="Step3-进行gradle同步操作并使用"><a href="#Step3-进行gradle同步操作并使用" class="headerlink" title="Step3 进行gradle同步操作并使用"></a>Step3 进行gradle同步操作并使用</h3><p>通过ide操作同步gradle进行初始构建，在同步成功后，便可以直接使用plugin替换相关模块的gradle配置项<br>如：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin-android) apply false

    id("easy.android.application")
    id("easy.android.application.compose")
} <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">dependencies {
    implementation(libs.core.ktx)
    implementation(libs.androidx.compose.activity)
} <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>

<p>经过使用plugin之后，gradle配置文件内容将大大减少，基本如下内容所示，只需对<code>dependencies</code>做好管理即可。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">@Suppress("DSL_SCOPE_VIOLATION")
plugins {
    alias(easy.plugins.android.feature.koin)
    alias(easy.plugins.android.library.jacoco)
}

android {
    namespace = "com.easy.wallet.home"
}

dependencies {
    implementation(project(":Wallet-Android:design-system"))
    implementation(project(":Wallet-Android:core"))
    implementation(project(":platform:datastore"))
    implementation(project(":platform:shared"))
    implementation(project(":platform:model"))

    implementation(libs.androidx.paging)
    implementation(libs.androidx.paging.compose)
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>更多可以查看<a href="https://github.com/BreakZero/E-Wallet-KMP">E-Wallet</a>下的配置文件比较。</p>
<h2 id="项目地址"><a href="#项目地址" class="headerlink" title="项目地址"></a>项目地址</h2><p><a href="https://github.com/BreakZero/build-logic">build-logic</a></p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>目前这个submodule也是通过在两个项目使用的过程中一步一步慢慢整理出来的，对于一些相对特殊的情况并不会被考虑到</li>
<li>虽然submodule确实减少了一部分工作，但是同时也丢失了一部分方便性，例如前文提到的<code>architecture</code>中各层相关模块的依赖性，因为层与层之间的依赖确定性基本不会改变，所以这块如果在kotlin代码中也会减少很多维护成本的。</li>
<li>虽如#2所说，但是在<code>multi-repo</code>项目中，这种使用方式笔者还是比较推荐的。</li>
</ol>
<p>不过在这方面仁者见仁，智者见智吧。 Have a good day!!!</p>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Gradle</tag>
        <tag>Gradle Plugin</tag>
      </tags>
  </entry>
  <entry>
    <title>2023年总结</title>
    <url>/posts/2023/12/31/c262e439/</url>
    <content><![CDATA[<p>2023年即将过去，回顾这一年，安慰自己一下，其实也没那么糟。一句话的总结为：</p>
<blockquote>
<p>“悠闲的一年，迷茫的一年，但也正因此，有机会不断反思自己和思考，在思想上成长最可观的一年”</p>
</blockquote>
<h2 id="工作-学习"><a href="#工作-学习" class="headerlink" title="工作&amp;学习"></a>工作&amp;学习</h2><p>自Crypto.com离职之后，前后又错过几次机会，后来经过一番尝试和结合行情的情况，决定今年在工作上随缘吧。<br>学习上吧，偶尔看看书，也通过写几个项目了解新的技术和保持编码的习惯，当然没有在公司项目中通过遇到问题解决问题学习效率那么高。所以平台很重要。</p>
<h2 id="生活"><a href="#生活" class="headerlink" title="生活"></a>生活</h2><p>没有工作的生活，确实少了些规律，但很多时候依然过着单一的生活，这也是我认为迷茫的方面之一。<br>刚离职不久，前几个月去了几个地方游玩，倒也过得开心<br>在深圳那段时间，经常去咖啡厅学习，顺便看看那里的人和事，晚上也常夜跑，有那么些日子也算充实<br>但也有偶尔颓废颓废、躺平，这种生活是最煎熬和压抑的<br>值得欣慰的是能在家陪父母，帮他们干些活<br>买了一辆车，并已开熟练</p>
<h2 id="三两事"><a href="#三两事" class="headerlink" title="三两事"></a>三两事</h2><ul>
<li>当过电工，帮家里安装了车库的电路和电器。排线，各开关接线方式都有讲究，电器上各种标识都是最好的说明书。</li>
<li>当过水泥工，搅拌泥土浆，刷墙。墙体太干，泥浆不好上墙，墙体太湿，难刷平。</li>
<li>记得有一天，广东早上也就几度，寒风凛冽，开着摩托车去砍甘蔗搬甘蔗，整整连续工作了将近10个小时，腰腿酸了几天才好转。为了生活，有些人真的不容易</li>
</ul>
<h2 id="感想和心得"><a href="#感想和心得" class="headerlink" title="感想和心得"></a>感想和心得</h2><ul>
<li>人脉很重要，人脉很重要，人脉很重要。得广交朋友</li>
<li>工作中，不公的事情常有，最需要注意的是控制好情绪和端正态度</li>
<li>做什么事，如果坚持不下去了，想想自己最初的初衷</li>
</ul>
<p>今天2023年最后一天，虽然今年没有赚到什么钱，但也没有工作上很多烦心事，让我得以有时间了解自己，也算是人生的一个转折</p>
<hr>
<p>2024将是更好的开始…</p>
]]></content>
      <categories>
        <category>随记</category>
      </categories>
  </entry>
  <entry>
    <title>Android面试之Handler简述</title>
    <url>/posts/2021/10/12/84a43c7b/</url>
    <content><![CDATA[<p>作为Android开发者，在面试的过程中，handler这个知识点很大概率会被问到，今天通过源码来了解下这个handler。</p>
<h2 id="Looper和MessageQueue"><a href="#Looper和MessageQueue" class="headerlink" title="Looper和MessageQueue"></a>Looper和MessageQueue</h2><h3 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h3><p>要了解Handler的原理，我们先来了解两个类，分别是Looper，MessageQueue。因为只有这两个类都准备好的前提下，handler才有作用。<br>在Android应用中，我们都知道，其入口是ActivityThread的main函数</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">public static void main(String[] args) {
    ...
    Looper.prepareMainLooper();

    ...

    if (false) {
        Looper.myLooper().setMessageLogging(new
                LogPrinter(Log.DEBUG, "ActivityThread"));
    }

    // End of event ActivityThreadMain.
    Trace.traceEnd(Trace.TRACE_TAG_ACTIVITY_MANAGER);
    Looper.loop();

    throw new RuntimeException("Main thread loop unexpectedly exited");
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>首先我们跟进<code>prepareMainLooper</code>函数内部，代码并不多，主要是创建Looper。<br>相关代码如下：</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">private Looper(boolean quitAllowed) {
    mQueue = new MessageQueue(quitAllowed);
    mThread = Thread.currentThread();
}

private static void prepare(boolean quitAllowed) {
    if (sThreadLocal.get() != null) {
        throw new RuntimeException("Only one Looper may be created per thread");
    }
    sThreadLocal.set(new Looper(quitAllowed));
}

public static void prepareMainLooper() {
    prepare(false);
    synchronized (Looper.class) {
        if (sMainLooper != null) {
            throw new IllegalStateException("The main Looper has already been prepared.");
        }
        sMainLooper = myLooper();
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>在私有构造函数中看到，Looper其内部有个mQueue的变量是MessageQueue类型的，此时Looper，MessageQueue都已经准备好了。</p>
<h3 id="Looper和MessageQueue如何运行的"><a href="#Looper和MessageQueue如何运行的" class="headerlink" title="Looper和MessageQueue如何运行的"></a>Looper和MessageQueue如何运行的</h3><p>回到main函数的<code>Looper.loop();</code>这个方法</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">public static void loop() {
    final Looper me = myLooper();
    if (me == null) {
        throw new RuntimeException("No Looper; Looper.prepare() wasn't called on this thread.");
    }   
    if (me.mInLoop) {
        Slog.w(TAG, "Loop again would have the queued messages be executed"
                + " before this one completed.");
    }
    ...
    for (;;) {
        if (!loopOnce(me, ident, thresholdOverride)) {
            return;
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>首先第2～4行，异常message说的很明确，同时前面笔者所说的只有在Looper和MessageQueue都准备好，handler才有效，也是这个原因。而且MessageQueue是Looper中的一个成员变量，所以Looper.prepare()内部都已准备好。<br>最后是一个for循环，再跟进去看，我们暂时只关注handler相关的部份，主要代码如下：</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">private static boolean loopOnce(final Looper me,
        final long ident, final int thresholdOverride) {
    Message msg = me.mQueue.next(); // might block
    if (msg == null) {
        // No message indicates that the message queue is quitting.
        return false;
    }
    ...
    
    try {
        msg.target.dispatchMessage(msg);
        if (observer != null) {
            observer.messageDispatched(token, msg);
        }
        dispatchEnd = needEndTime ? SystemClock.uptimeMillis() : 0;
    } catch (Exception exception) {
        if (observer != null) {
            observer.dispatchingThrewException(token, msg, exception);
        }
        throw exception;
    } finally {
        ThreadLocalWorkSource.restore(origWorkSource);
        if (traceTag != 0) {
            Trace.traceEnd(traceTag);
        }
    }
    ...

    msg.recycleUnchecked();

    return true;
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>通过MessageQueue.next方法取出消息，对该消息进行处理，并在最后释放消息内容。<br>到这里，我们了解到了，在Android应用程序启动是，首先调用了Looper.prepareMainLooper进行主线程Looper的准备工作，然后在调用Looper的<code>loop</code>方法进入死循环，在循环内部通过MessageQueue的<code>next</code>方法获取消息并进行处理。在Looper中值得注意的是两个变量，分别是<code>sThreadLocal</code>和<code>mQueue</code>，<code>sThreadLocal</code>是保证了当前looper只属于当前线程，而<code>mQueue</code>是looper的消息队列。</p>
<blockquote>
<p>注： 留意这里的<code>msg.target.dispatchMessage(msg);</code>，后面会提到</p>
</blockquote>
<h2 id="Handler的原理"><a href="#Handler的原理" class="headerlink" title="Handler的原理"></a>Handler的原理</h2><h3 id="Handler的使用"><a href="#Handler的使用" class="headerlink" title="Handler的使用"></a>Handler的使用</h3><p>首先我们需要初始化一个Handler</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">private val handler = Handler(Looper.getMainLooper()) {
    println("===== callback thread: ${Thread.currentThread().name}")
    println("===== ${it.what}")
    println("===== ${it.data.getString("Content")}")
    true
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>在子线程send messge</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">GlobalScope.launch(Dispatchers.IO) {
    println("===== send thread: ${Thread.currentThread().name}")
    handler.sendMessage(Message.obtain().apply {
        this.data = bundleOf("Content" to "Hello world!!!")
        this.what = 1
    });
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<blockquote>
<p>注：这里的代码在Activity/Fragment中的，handler也是通过主线程创建的</p>
</blockquote>
<p>现在我们知道handler如何使用了，接下来跟踪代码来理解，首先来看看<code>sendMessage</code>方法，相关代码全部贴出来</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">public final boolean sendMessage(@NonNull Message msg) {
    return sendMessageDelayed(msg, 0);
}

public final boolean sendMessageDelayed(@NonNull Message msg, long delayMillis) {
    if (delayMillis &lt; 0) {
        delayMillis = 0;
    }
    return sendMessageAtTime(msg, SystemClock.uptimeMillis() + delayMillis);
}

public boolean sendMessageAtTime(@NonNull Message msg, long uptimeMillis) {
    MessageQueue queue = mQueue;
    if (queue == null) {
        RuntimeException e = new RuntimeException(
                this + " sendMessageAtTime() called with no mQueue");
        Log.w("Looper", e.getMessage(), e);
        return false;
    }
    return enqueueMessage(queue, msg, uptimeMillis);
}

private boolean enqueueMessage(@NonNull MessageQueue queue, @NonNull Message msg,
        long uptimeMillis) {
    msg.target = this;
    msg.workSourceUid = ThreadLocalWorkSource.getUid();

    if (mAsynchronous) {
        msg.setAsynchronous(true);
    }
    return queue.enqueueMessage(msg, uptimeMillis);
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>看到最终调用的是<code>enqueueMessage</code>方法，注意<code>msg.target = this;</code>和<code>queue.enqueueMessage(msg, uptimeMillis);</code><br>先来看<code>queue.enqueueMessage(msg, uptimeMillis);</code>，这个queue就是mQueue，而mQueue在初始化Handler的时候赋值的<code>mQueue = mLooper.mQueue;</code>，也就是说调用sendMessage方法最终会通过looper的messageQueue将这个消息入队列之中。再根据前面我们说的，loop函数会一直查看消息队列的消息，出队列时做处理。<br>现在看<code>msg.target = this;</code>，为什么要关注这个呢？ 首先这个方法是在Handler类之中的，这个this指的就是当前的handler。<br>前文提到looper会冲消息队列通过next出队并处理，其中就有一行代码<code>msg.target.dispatchMessage(msg);</code><br>现在来看Handler的<code>dispatchMessage(msg)</code>方法</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">public void dispatchMessage(@NonNull Message msg) {
    if (msg.callback != null) {
        handleCallback(msg);
    } else {
        if (mCallback != null) {
            if (mCallback.handleMessage(msg)) {
                return;
            }
        }
        handleMessage(msg);
    }
}

private static void handleCallback(Message message) {
    message.callback.run();
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>使用例子中的case，msg是没有传callback的，所以其实这里调用了mCallback.handleMessage(msg)。也就是我们创建Handler实例的回调。<br>到这里，Handler与Looper、MessageQueue、Message之间的关系讲完了。</p>
<h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><ol>
<li><p>子线程能否使用handler？如果可以需要注意什么？如果没有消息后应该如何处理？<br>Handler其实就是一个类，当然可以在子线程中使用。子线程handler创建的时候一定需要设定一个Looper给这个handler(Looper.prepare())，否则会在调用loop() 的时候抛出Runtime异常。主要是如下代码</p>
<pre class="line-numbers language-java" data-language="java"><code class="language-java">if (me == null) {
    throw new RuntimeException("No Looper; Looper.prepare() wasn't called on this thread.");
}   <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>
<p>当没有消息的时候，我们应该调用looper.quit() 方法释放资源，否则会因为死循环导致线程内存泄漏。</p>
</li>
<li><p>Handler在等待新消息的时候会阻塞吗？ 如果会，那会不会导致ANR？<br>Handler在等待新消息的时候其实就是在死循环的过程，也称为looper阻塞。所以是会阻塞的。但不会导致ANR，因为Android系统本来就是事件驱动的，looper.loop不断接受事件和处理事件，如果它停止了，App也就停止了，所以这个问题本身就是假命题。<br>另一种说法就是ANR本来就是类似定时炸弹一样的设计，在事件发送时埋下定时炸弹，如果在规定的时间内没有收到处理完的通知，则判定为超时，引爆炸弹（ANR），收到则拆除炸弹。而Looper在loop的过程并没埋下这个定时炸弹</p>
</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>通过跟踪源码，我们清晰了解到了Handler处理消息的过程。</p>
<ol>
<li>首先在程序入口调用Looper.prepareMainLooper()方法，提供主线程Looper和MessageQueue</li>
<li>然后继续调用Looper的loop方法进入死循环，通过MessageQueue的next方法取得消息并处理</li>
<li>取得的消息怎么来的？ Handler send/post(post方式会传入Runable给message)来的</li>
<li>在send过程中，handler会被赋给message.target</li>
<li>取得的消息中，Message拿到target，即为handler，处理消息就是通过handler.dispatchMessage方法调用回调方法返回主线程处理(因为这里的handler是在主线程创建的)</li>
</ol>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Handler</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS 读取存储在Container的文件问题</title>
    <url>/posts/2023/08/13/bb96b1c7/</url>
    <content><![CDATA[<h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>在使用KMM实现iOS平台文件存储时，通过导出container文件可以查看到文件确实存在目录下，但是在调用<code>NSData.dataWithContentsOfFile</code>时总是返回空，根据错误信息得知找不到该文件或目录<code>No such File or Directory</code>。其中代码如下：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">val errorPtr: ObjCObjectVar&lt;NSError?&gt; = alloc()
NSData.dataWithContentsOfFile(fullPath, options = 0, error = errorPtr.ptr)?.let { bytes -&gt;
    val array = ByteArray(bytes.length.toInt())
    bytes.getBytes(array.refTo(0).getPointer(this), bytes.length)
    return@withContext array
}
println(errorPtr.value?.description.orEmpty())
return@withContext null<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>errorPtr是为了查看具体错误，所以加上这个为了更好定位问题</p>
<span id="more"></span>

<h2 id="问题定位"><a href="#问题定位" class="headerlink" title="问题定位"></a>问题定位</h2><ol>
<li>通过上面的error日志中得知，大概率是因为fullPath这个目录是一个错误的目录了，而我又从device中导出了<code>xxx.xcappdata</code>，<br>在这个目录下确实能找到对应名字的文件，而且文件是没有问题的，这个可以确定问题是文件是存在的，但是目录又不一致</li>
<li>既然文件存在，但是目录上却找不到该文件，问题出在哪呢？于是想通过代码打印出fullPath和目录下所有文件的路径对比是否一致，上代码：<pre class="line-numbers language-swift" data-language="swift"><code class="language-swift">let fileManager = FileManager.default
let documentsURL = fileManager.urls(for: .documentDirectory, in: .userDomainMask)[0]
do {
    let fileURLs = try fileManager.contentsOfDirectory(at: documentsURL, includingPropertiesForKeys: nil)
    print(fileURLs)
    // process files
} catch {
    print("Error while enumerating files \(documentsURL.path): \(error.localizedDescription)")
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
这一块是Swift的代码，然后获取document Directory的url（因为我存储文件的目录就是这个），然后通过遍历将所有文件url打印并对比</li>
</ol>
<ul>
<li>fullPath路径是这样的<blockquote>
<p>/var/mobile/Containers/Data/Application/3FC1CCE9-A788-47AB-902A-FA133FAA3D30/Documents/60F570BE-2A16-4EFB-96B3-9203C0A0ABCE.jpg</p>
</blockquote>
</li>
<li>而文件url则是<blockquote>
<p>file:///private/var/mobile/Containers/Data/Application/C5AC5178-6887-40F4-9EE5-8D56CB830CB3/Documents/60F570BE-2A16-4EFB-96B3-9203C0A0ABCE.jpg</p>
</blockquote>
</li>
</ul>
<p>果然，Application后面的UUID并不一致，那全路径肯定也是不对的咯<br>通过查阅资料，得知在<code>iOS8.0</code>之后每次debug运行或者更新，这个UUID都会改变，而我又为了方便，一开始就存储了全路径，所以每次重新运行代码就会出现找不到这个文件，因为UUID的改变导致了整个目录都变了，自然找不到。</p>
<h2 id="解决问题"><a href="#解决问题" class="headerlink" title="解决问题"></a>解决问题</h2><p>既然知道了问题所在，解决问题就简单了</p>
<ol>
<li>遍历目录，通过fileName过滤拿到对应的文件<br>刚开始，由于对iOS这边的api之类的还不是很了解，解决思路也很直接，既然Swift上能拿到目录下所有文件的url了，在kotlin native层肯定有对应的API，于是开搞，上代码：<pre class="line-numbers language-Kotlin" data-language="Kotlin"><code class="language-Kotlin">// 拿到目录的url
val url = fileManager.URLForDirectory(
    directory = NSDocumentDirectory,
    inDomain = NSUserDomainMask,
    appropriateForURL = null,
    create = false,
    error = null
)!!
// 拿到目录下所有文件的url
val fileUrls = fileManager.contentsOfDirectoryAtURL(url = url, includingPropertiesForKeys = null, options = 0, error = errorPtr.ptr)
// 通过全路径拿到文件名，然后通过文件名过滤得到唯一的文件url
val currentFileUrl = fileUrls?.first { it.toString().contains(fileName) } as? NSURL
// 读取到NSData中
currentFileUrl?.let {
    NSData.dataWithContentsOfURL(currentFileUrl)
    ?.let { bytes -&gt;
        val array = ByteArray(bytes.length.toInt())
        bytes.getBytes(array.refTo(0).getPointer(this), bytes.length)
        return@withContext array
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
再次运行，成功拿到bytes（✌️）<br>但是这样存在一个问题，随着时间，这个目录的文件肯定会越来越多，到时候遍历肯定是一个耗时任务。那有没有可以直接通过filename拿到对应文件的url呢？<br>看了下代码，既然能拿到document目录的url，那是不是也有API可以直接拿到文件的url？一开始想通过url + filename组装一下<br>他们类型不一致，相对麻烦，还好找到了，代码：<pre class="line-numbers language-Kotlin" data-language="Kotlin"><code class="language-Kotlin">val documentDirectory = NSSearchPathForDirectoriesInDomains(
    directory = NSDocumentDirectory,
    domainMask = NSUserDomainMask,
    expandTilde = true
).first() as NSString
val fullPath = documentDirectory.stringByAppendingPathComponent(fileName)
println(fullPath)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
路径正确，直接调用<code>NSData.dataWithContentsOfFile</code>传入文件路径即可</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在 iOS8.0 之后，由于Application（Bundle identifier）的UUID会随机改变，所以不能直接存全路径，可以直接存名字<br>而可以通过<code>NSSearchPathForDirectoriesInDomains</code>拿到对应的路径，再加上文件名得到当前文件准确的全路径，这时候读取即可。</p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Multiplatform</category>
        <category>iOS</category>
      </categories>
      <tags>
        <tag>SwiftUI</tag>
        <tag>Multiplatform</tag>
        <tag>Kotlin Native</tag>
      </tags>
  </entry>
  <entry>
    <title>remember VS rememberSaveable</title>
    <url>/posts/2023/12/04/f68195/</url>
    <content><![CDATA[<p>在Jetpack Compose中，存在两种方式记住某个计算的值，从而避免在重组的时候重新计算实现性能上的提升，这两种方式分别为<code>remember {}</code>和<code>rememberSaveable {}</code>。</p>
<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>笔者在前段时间遇到这么个情况：<br>在一个页面存在两部分组件，分别是Dashboard和Asset List，其中List是可以滚动，而Dashboard会根据用户滑动behavior来调整Scale动画（当然，这并不是最终效果，只是一个简单的use case）<br>当使用<code>remember {}</code>来记录Scale，如果在tab之间切换比较快的话，偶尔能正常的，但是如果在其他tab停留了一段时间再切回这个tab，scale将被重置从而放大了Dashboard UI，情况如下显示：<br><img src="/images/post/remember.gif" alt="Recording" title="remember"><br>如果使用<code>rememberSaveable {}</code>则符合预期，不管如何，回到页面都能展示之前的scale。<br>伪代码：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">// remember/rememberSaveable
var scale by rememberSaveable {
    mutableFloatStateOf(1.0f)
}
val nestedScrollConnection = remember {...}
Box(
    modifier = modifier.nestedScroll(nestedScrollConnection),
) {
    DashboardView(
        modifier = Modifier
            .fillMaxWidth()
            .scale(scale)
            .graphicsLayer { translationY = toolbarHeightRange.last * (scale - 1.0f) },
        onEvent = onEvent
    )
    LazyColumn(
        modifier = Modifier
            .fillMaxSize()
            .graphicsLayer { translationY = toolbarState.height + toolbarState.offset }
            .background(MaterialTheme.colorScheme.background)
            .shadow(
                elevation = 3.dp,
                shape = RoundedCornerShape(topEnd = 20.dp, topStart = 20.dp)
            )
            .pointerInput(Unit) {
                detectTapGestures(
                    onPress = { scope.coroutineContext.cancelChildren() }
                )
            },
        verticalArrangement = Arrangement.spacedBy(12.dp),
        state = listState
    ) {
        items(uiState.tokens, key = { it.token.id }) {
            TokenItemView(extraToken = it, onEvent = onEvent)
        }
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>所以在Compose中如何正确使用这两个方法呢？</p>
<h3 id="remember-和-rememberSaveable-的区别"><a href="#remember-和-rememberSaveable-的区别" class="headerlink" title="remember {} 和 rememberSaveable {}的区别"></a>remember {} 和 rememberSaveable {}的区别</h3><h4 id="remember"><a href="#remember" class="headerlink" title="remember {}"></a>remember {}</h4><p>在官方文档对可重组项的生命周期描述中<a href="https://developer.android.com/jetpack/compose/lifecycle">Lifecycle of composables</a><br>我们知道对于每一个可重组项，其生命周期分为3个阶段， 进入组合 -&gt; 重组 -&gt; 退出组合<br>其中对于<code>remember {}</code>方法的注释如下</p>
<blockquote>
<p>Remember the value produced by calculation. calculation will only be evaluated during the composition. Recomposition will always return the value produced by composition.</p>
</blockquote>
<p>参考对应的生命周期，可以理解为在仅仅是在进入组合时<code>remember {}</code>中的block会被执行计算，而重组则直接返回无需再计算（当然也可以添加key参数增加重新计算条件）</p>
<h4 id="rememberSaveable"><a href="#rememberSaveable" class="headerlink" title="rememberSaveable {}"></a>rememberSaveable {}</h4><p>在官方文档对<code>rememberSaveable {}</code>的说明是这样的，<a href="https://developer.android.com/reference/kotlin/androidx/compose/runtime/saveable/package-summary#rememberSaveable(kotlin.Array,androidx.compose.runtime.saveable.Saver,kotlin.String,kotlin.Function0)">参考文档</a></p>
<blockquote>
<p>Remember the value produced by init.<br>It behaves similarly to remember, but the stored value will survive the activity or process recreation using the saved instance state mechanism (for example it happens when the screen is rotated in the Android application).</p>
</blockquote>
<p>翻译过来大概意思就是：它和<code>remember</code>一样，都是用来保留某个结果的值的，但是其保留的值在Activity重建之后仍然存在</p>
<blockquote>
<p>注：If you use it with types which can be stored inside the Bundle then it will be saved and restored automatically using autoSaver, otherwise you will need to provide a custom Saver implementation via the saver param.</p>
</blockquote>
<p>在默认的情况下，会使用<code>autoSaver()</code>保存在Bundle中，当然可以自己实现Saver。</p>
<p>因为Bundle不支持没有被系列化的对象，所以普通的data class是无法正常使用的，需要自己实现Saver。<br>笔者已经验证过，存储普通的data class会报如下错误信息：<br><span style="color:red"><br>java.lang.IllegalArgumentException: MutableState containing ScaleStore(scale=0.0) cannot be saved using the current SaveableStateRegistry. The default implementation only supports types which can be stored inside the Bundle.<br></span></p>
<p>想要使用<code>rememberSaveable</code>保留特殊的类型，需要自己实现Saver</p>
<p>通过上面的描述，大致了解了<code>remember {}</code> 和 <code>rememberSaveable {}</code>的过程</p>
<ul>
<li>remember {} 会在进入组合时开始计算，重组则直接返回记录下来的值；当可重组项退出组合后再进入组合，这属于开始新的生命周期，自然会重新计算。</li>
<li>rememberSaveable {} 默认是将数据缓存到Bundle中，在屏幕旋转或者其他Configuration变化时，触发重建，直接从Bundle中取得保留过的值，无需重新计算。</li>
</ul>
<h3 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h3><p>首先验证使用<code>remember {}</code>的用例，使用最简单的日志法。加上如下代码</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">// 验证是否进行重新计算
var scale by remember {
    println("=== re-calculate")
    mutableFloatStateOf(1.0f)
}
// 是否退出组合
DisposableEffect(key1 = null) {
    onDispose {
        println("=== onDispose")
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<blockquote>
<p>DisposableEffect 和 key1 = null，是为了保证可重组项是因为退出组合时才执行<code>onDispose</code>。<br>打印日志如下：<br><img src="/images/post/remember_log.jpg" alt="Logger" title="remember_log"><br>除了第一次进入组合打印的<code>re-calculate</code>，之后的都是要经过dispose之后再打印，在dispose之后，重新进入重组而进行再次计算，会将scale重置为1.0，也就是放大了Dashboard组件的大小。符合笔者遇到情况的解释。<br>（注：如果在tab之间切换较快，两条日志都不会出现。这也对应前面提到过当在tab之间切换较快，结果正常，scale没有变化，而是保留了原来的大小。笔者对这种情况的猜测解释是可组合项还没来得及回收保留的数据，又被重新唤醒了）<br>同样将remember 换成 rememberSaveable，日志如下：<br><img src="/images/post/remember_log1.jpg" alt="Logger1" title="remember_log1"><br>除了第一次进入组合打印的<code>re-calculate</code>，之后再也没有出现<code>re-calculate</code>，证明了一直使用的都是记录下来的数据。</p>
</blockquote>
<h3 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h3><p>在搜索资料时，笔者找到这么一篇记录 <a href="https://jetc.dev/slack/2021-04-13-when-do-we-use-remembersaveable.html">One Off the Slack: When Do We Use rememberSaveable()?</a><br>在整个过程中，笔者对这句话<code>composable leaving the screen is not recreation</code>一直有些疑惑。<br>笔者个人认为，这句话在文中的理解应该是，Configuration变化导致重建，但是可组合项目离开屏幕不是Configuration变化，所以不会导致重建，可以理解成字面上的不是重建的意思。<br>但是对于Compose中的可重组项来说，当离开了屏幕，会调用onDispose，对于生命周期的上，这个可重组项重新进入屏幕，是重新走了一遍生命周期的，应该属于重建。<br>日常开发过程中，除去特殊情况，可组合项离开屏幕包含2种：</p>
<ol>
<li>Navigation跳转</li>
<li>滑动导致某些可组合项滑出屏幕外</li>
</ol>
<p>对于Navigation跳转，上文已经解释了，此处就不赘述。接下来通过简单的例子看看滑动的情况：<br>一个简单的LazyColumn + Item DisposableEffect</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">LazyColumn(
    modifier = Modifier
        .fillMaxSize()
        .padding(it)
) {
    items(uiState.instruments) {
        SpotItemView(
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 16.dp), instrument = it
        )
    }
}

@Composable
private fun SpotItemView(
    modifier: Modifier = Modifier,
    instrument: Instrument
) {
    DisposableEffect(key1 = null, effect = {
        onDispose {
            println("=== ${instrument.instId}")
        }
    })
    Row(
        modifier = modifier,
        horizontalArrangement = Arrangement.SpaceBetween
    ) {
        ...
    }
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
<p>跑起来通过日志可以了解到，当某个item滑出屏幕外时，<code>onDispose</code>会触发，意味退出组合。<br>在可组合项目的生命周期来看，退出组合则表明这个可重组项生命周期结束，当需要重新回到屏幕，需要重新走一遍，所以remember中会重新进行计算。<br>但是对于LazyColumn，当前position的记录需要考虑下个页面返回到当前页面而需要记录当前位置的情况，所以<code>rememberLazyListState</code>使用的就是rememberSaveable记录状态的。</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>如果某个状态只考虑在重组的时候记录的，remember是最好的选择。</li>
<li>对于其他情况，rememberSaveable或许会更符合。</li>
<li>当然ViewModel中存储也是一种方式，不过有些时候组件的封装，状态提升是更好的设计，ViewModel就不符合这个理念了</li>
</ul>
<p>不管如何，弄明白其原理和实现之后，再使用的过程中自然会考虑到这方面的差异而选择更好的。</p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Multiplatform</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Jetpack Compose</tag>
      </tags>
  </entry>
  <entry>
    <title>The Android Clean Architecture</title>
    <url>/posts/2024/02/12/53af0ebf/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>对于Android开发者，对MVP，MVVM，MVI架构都有所了解吧，也曾听过The Clean Architecture吧？<br>之前笔者都认为这几个架构是同一个目的不同实现，但后来发现其实不然。</p>
<ul>
<li>Clean是一种架构风格，其核心思想就是关注点分离，强调应用程序不同部分应有明确的责任且相互独立，通过分层来提升代码可读性、可测试性和可维护性，针对于整个应用设计的。</li>
<li>MVP，MVVM，MVI这些则是架构模式，是应用程序对数据（或状态）和用户界面之间关系管理的一种解决方案（注：在app开发过程中，笔者个人是这么理解的）。</li>
</ul>
<p><strong>为什么笔者认为这些架构模式是应用程序对数据（或状态）和用户界面之间关系管理的一种解决方案？</strong></p>
<p>相信大家都有过MVVM开发经验，在开发一个feature的时候，应该都有如下步骤：</p>
<ol>
<li>通过Rest Api获取请求体并转换成Model(一般data class)</li>
<li>ViewModel通过Repo获取得到Model，并根据情况转换或组合成UiState</li>
<li>在View上观察UiState并刷新界面<br>以上步骤不管MVVM还是MVI都有类似。</li>
</ol>
<p>在获得Model之前，不管经过Network还是LocalDB亦或组合成的数据的方式拿到Model，都不属于MVVM/MVI架构模式下的考虑范围。所以这些架构模式就是解决了传统Android开发过程中几乎所有业务逻辑代码都放在Activity中让人难以忍受的问题。</p>
<h3 id="Clean架构在Android应用上的变体"><a href="#Clean架构在Android应用上的变体" class="headerlink" title="Clean架构在Android应用上的变体"></a>Clean架构在Android应用上的变体</h3><p>Clean架构是<em>Uncle Bob</em>提出的一种软件设计架构，其架构图如下：</p>
<img src="/posts/2024/02/12/53af0ebf/clean-architecture.webp" class="" title="clean-architecture">

<p>这是整个软件系统的架构图，对于Android应用，我们应该有更贴切的Clean架构图，参考<a href="https://github.com/android/nowinandroid/tree/main">nowinandroid</a>项目和<a href="https://mp.weixin.qq.com/s/wDGUPkHQKrkKO3ZCX8aaMg">博客</a>以及经验总结，Android应用程序的架构图大体如下：</p>
<img src="/posts/2024/02/12/53af0ebf/android-clean-architecture.png" class="" title="android-clean-architecture">


<p>通过了解Google对<a href="https://developer.android.com/topic/architecture/intro">App architecture介绍</a>，整理得如下数据流向图：</p>
<img src="/posts/2024/02/12/53af0ebf/data-flow.png" class="" title="data-flow">

<p>整体对应Android Clean架构分层，而在UI Layer中则使用所谓MVVM，MVI架构模式解决UI界面和业务逻辑数据的分离，并通过ViewModel连接两者。使用这种架构模式可以帮助开发人员更好管理和维护代码，更易于测试和扩展。</p>
<h3 id="依赖关系"><a href="#依赖关系" class="headerlink" title="依赖关系"></a>依赖关系</h3><p><strong>Clean架构的基本准则是层级之间，内层不依赖外层，依赖关系永远是从外向内的。</strong></p>
<p>如上图，Model层不应该有任何依赖，Domain层可以依赖Repo或Model层，但不能依赖ViewModel层， UI层依赖ViewModel，但ViewModel不能依赖UI。</p>
<p>其中，Data层最终返回数据可以通过Mapper将Local DB的Entities或Remote Server返回的json转换成Model层的数据并提供外层使用。Domain层是可选择层，是否应该有取决于对业务场景的设计用例（UseCase）。ViewModel则是管理UiState。</p>
<h3 id="Model层"><a href="#Model层" class="headerlink" title="Model层"></a>Model层</h3><p>业务模型或者领域模型，是根据业务设计出来的具体模型，一般来说是一个data class，其中不包含任何业务逻辑，仅仅是个单纯的模型对象。<br>由于是在整个架构的最内层，所以不依赖任何其他模块，应该稳定。所以设计时应该考虑，如果模型发生变化，则意味着整个上层依赖方都可能发生变化，需要重新设计。<br>也正因如此，之前笔者对于这层的把控总有些不自信，因为有些模型并不需要对所有层公开，但如果将其放到Model层则对有所其他层级都有影响（包括Gradle执行速度）。<br>所以根据Clean架构的基本准则，在设计Model的时候多考虑考虑，而Google也有所建议</p>
<img src="/posts/2024/02/12/53af0ebf/model-recommended.jpg" class="" title="model-recommended">

<h3 id="Mapper层"><a href="#Mapper层" class="headerlink" title="Mapper层"></a>Mapper层</h3><p>也有很多人称之为Adapter（数据适配器）层，它介于Domain和Model层，包含于Repo层（也可以单独提取一层，但笔者认为没必要），主要有两个职责：</p>
<ul>
<li>网络返回的json数据/本地数据库Entities和领域模型之间的转换</li>
<li>领域模型之间的相互转换</li>
</ul>
<p>所以它相对纯粹，只负责数据转换，通常使用xxxMapper命名类 + 扩展函数，如：<br>UserEntityMapper.kt 文件中</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">fun User.toUserEntity(): UserEntity {
  return UserEntity(...)
}

fun UserEntity.toUser(): User {
  return User(...)
}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<h3 id="Repo层"><a href="#Repo层" class="headerlink" title="Repo层"></a>Repo层</h3><p>如上数据流图所示，Repo是Data Layer中的一部分，是对网络接口数据或本地数据库的数据读写的封装，对于上层使用者Domain或ViewModel来说，不必关注其具体实现，使用即可。因此，Repo应当隐藏具体实现细节，这意味着Repo层对外暴露的函数的入参和出参不能包含接口返回的实体类，也不应该包含数据库表的Entity类，只能包含领域模型或基本类型，这里也体现了Mapper层的作用。</p>
<hr>
<blockquote>
<p>更多请看<a href="https://mp.weixin.qq.com/s/wDGUPkHQKrkKO3ZCX8aaMg">Clean 架构下的现代 Android 架构指南 | 开发者说·DTalk</a>详细，里面对每层的描述讲解的都非常到位和详细。</p>
</blockquote>
<p>但也要注意这些都不是一成不变的，开发过程中我们应该视业务需求而定，要有自己的思考。<br>比如我在开发一个多链支持的钱包应用时，就发送Token这个需求而言，大体流程都是如下：</p>
<pre class="mermaid">graph TD
A(开始) --&gt;|选择Token| B[输入Amount，收款地址，Memo等信息]
B --&gt; C[Fee选择]
C --&gt; D[签名交易]
D --&gt; E{确认并广播交易}
E --&gt;|成功| F[成功界面]
E --&gt;|失败| G[失败界面]
</pre>
<p>但是在开发过程中，你会发现由于不同链需要的数据不一致所致，随着支持的链越来越多，在使用同一个导航界面实现的情况下，UiState和Model也会随之增大，代码中也许会出现大量的<code>if else</code>或<code>when</code>判断，对于维护和扩展是个很大的问题。<br>在重构的过程中，对Token统计并执行业务分类，将发送流程分为EVM、Bitcoin、Cosmos等，在选择Token的时候便走向不同的导航页面。而对于那些共同属性(如hash，amount，recipient，sender等)用接口包装分别实现，保留了部分共性避免没必要的重复代码。</p>
<p>流程还是如上图，但是我们在输入之前通过判断导航到不同UI界面，从而选择了不同的领域模型和业务处理能力的Repo。而没有在同一个ViewModel中持有所有链相关的Model和业务处理能力的Repo。</p>
<h3 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h3><p><a href="https://semihtekin.medium.com/what-is-the-clean-architecture-c80c2a2ff69a">What is the Clean Architecture?</a><br><a href="https://developer.android.com/topic/architecture/intro">App architecture</a><br><a href="https://mp.weixin.qq.com/s/wDGUPkHQKrkKO3ZCX8aaMg">Clean 架构下的现代 Android 架构指南 | 开发者说·DTalk</a></p>
]]></content>
      <categories>
        <category>Technical</category>
        <category>Android</category>
      </categories>
  </entry>
</search>
