<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>AI Agent 和 Skill 有什么区别？从执行任务到沉淀能力</title>
    <url>/posts/2026/05/17/a1b2c3d4/</url>
    <content><![CDATA[<p>在使用 Codex 这类开发型 AI 工具时，一个很常见的困惑是：</p>
<p><strong>Agent、Skill、提示词和工具调用到底是什么关系？</strong></p>
<p>这些概念都和 AI 能力增强有关，但解决的问题并不一样。如果不区分清楚，很容易把所有要求都塞进一个超长提示词里，或者误以为只要有 Agent，就不需要沉淀可复用的工作方式。</p>
<p>更清晰的理解是：</p>
<p><strong>Agent 负责执行任务，Skill 负责沉淀能力。</strong></p>
<p>Agent 更像一个能围绕目标持续推进的执行者；Skill 更像一套可复用的专业流程、经验规则和上下文说明。两者结合起来，才更接近一个稳定的 AI 工作流。</p>
<hr>
<h2 id="一、什么是-Agent"><a href="#一、什么是-Agent" class="headerlink" title="一、什么是 Agent"></a>一、什么是 Agent</h2><p>Agent 可以理解为：</p>
<p><strong>一个带有明确角色、目标、上下文、工具权限和执行流程的 AI 助手。</strong></p>
<p>它和普通聊天助手最大的区别，不是回答更长，而是可以围绕一个目标持续行动。</p>
<p>当用户给出一个任务后，Agent 通常会完成一组动作：</p>
<ol>
<li>读取上下文</li>
<li>判断任务要解决的问题</li>
<li>制定执行步骤</li>
<li>调用可用工具</li>
<li>修改文件或生成内容</li>
<li>运行命令或做验证</li>
<li>交付结果并说明改动</li>
</ol>
<p>比如这个任务：</p>
<blockquote>
<p>帮我优化这篇博客文章，让它适合发布到个人博客和公众号。</p>
</blockquote>
<p>普通聊天助手可能会直接返回一版改写文本。</p>
<p>开发型 Agent 则会先读取文章文件，理解仓库结构和写作约定，再修改 Markdown、补齐 front matter、检查构建结果，最后说明改动内容和验证结果。</p>
<p>所以 Agent 的重点不是“会聊天”，而是：</p>
<p><strong>它能围绕目标推进任务，并对最终交付负责。</strong></p>
<hr>
<h2 id="二、Agent-的核心能力"><a href="#二、Agent-的核心能力" class="headerlink" title="二、Agent 的核心能力"></a>二、Agent 的核心能力</h2><p>一个有用的 Agent，通常不只是“大模型 + 一段提示词”。</p>
<p>它至少需要四类能力。</p>
<h3 id="1-明确角色和目标"><a href="#1-明确角色和目标" class="headerlink" title="1. 明确角色和目标"></a>1. 明确角色和目标</h3><p>Agent 需要知道自己是谁，以及这次任务要解决什么问题。</p>
<p>不同角色会带来不同的优先级：</p>
<ul>
<li>代码审查 Agent：优先发现 bug、风险和缺失测试</li>
<li>写作优化 Agent：优先优化结构、表达、标题和 SEO</li>
<li>数据分析 Agent：优先整理数据、发现趋势、输出结论</li>
</ul>
<p>角色越清楚，输出越稳定。</p>
<h3 id="2-理解上下文"><a href="#2-理解上下文" class="headerlink" title="2. 理解上下文"></a>2. 理解上下文</h3><p>Agent 需要读取和任务相关的信息。</p>
<p>对代码任务来说，上下文可能是项目结构、已有实现、测试结果和依赖版本。对写作任务来说，上下文可能是文章草稿、历史文章风格、发布平台要求和 SEO 字段。</p>
<p>没有上下文时，Agent 很容易给出看似合理但不贴合实际的结果。</p>
<h3 id="3-调用工具"><a href="#3-调用工具" class="headerlink" title="3. 调用工具"></a>3. 调用工具</h3><p>工具调用是 Agent 区别于普通问答的重要能力。</p>
<p>Agent 可以读文件、搜索代码、运行测试、调用 API、查询数据，甚至直接修改项目文件。</p>
<p>工具让 Agent 从“只能给建议”变成“可以完成任务”。</p>
<h3 id="4-执行和验证"><a href="#4-执行和验证" class="headerlink" title="4. 执行和验证"></a>4. 执行和验证</h3><p>Agent 不应该只停留在方案层面。</p>
<p>如果任务是修改代码，应该尽量运行测试；如果任务是优化文章，应该检查 Markdown、front matter 和构建结果；如果任务是接入 API，应该验证调用是否真的成功。</p>
<p>这也是 Agent 的核心价值：</p>
<p><strong>不只生成答案，而是推进任务并验证结果。</strong></p>
<hr>
<h2 id="三、什么是-Skill"><a href="#三、什么是-Skill" class="headerlink" title="三、什么是 Skill"></a>三、什么是 Skill</h2><p>如果说 Agent 是执行者，那么 Skill 可以理解为：</p>
<p><strong>一组可复用的专业能力、操作流程和上下文说明。</strong></p>
<p>Skill 通常不会自己主动执行任务。它更像一份能力说明，告诉 Agent 遇到某类任务时应该怎么做。</p>
<p>它可以包含：</p>
<ul>
<li>任务判断规则</li>
<li>推荐读取的文件或资料</li>
<li>需要遵守的风格和边界</li>
<li>可复用脚本、模板或参考资料</li>
<li>结果交付格式</li>
</ul>
<p>比如“博客内容优化 Skill”可以规定：</p>
<ul>
<li>先阅读草稿和历史文章风格</li>
<li>保留作者核心观点，不编造事实</li>
<li>优化标题、开头、逻辑主线和结尾</li>
<li>补齐 <code>description</code>、<code>keywords</code>、<code>tags</code></li>
<li>输出适合个人博客和公众号的版本</li>
</ul>
<p>这样下次再优化文章时，就不需要重新写一大段提示词。Agent 加载这个 Skill 后，就能继承这套相对稳定的工作方式。</p>
<p>所以 Skill 的重点不是独立完成任务，而是：</p>
<p><strong>把某类任务的经验沉淀下来，让 Agent 更稳定地执行。</strong></p>
<hr>
<h2 id="四、Agent-和-Skill-的核心区别"><a href="#四、Agent-和-Skill-的核心区别" class="headerlink" title="四、Agent 和 Skill 的核心区别"></a>四、Agent 和 Skill 的核心区别</h2><p>可以用一张表快速区分：</p>
<table>
<thead>
<tr>
<th>维度</th>
<th>Agent</th>
<th>Skill</th>
</tr>
</thead>
<tbody><tr>
<td>核心定位</td>
<td>执行任务的主体</td>
<td>可复用的能力包</td>
</tr>
<tr>
<td>解决问题</td>
<td>这次任务怎么完成</td>
<td>这类任务应该怎么做</td>
</tr>
<tr>
<td>关注点</td>
<td>目标、上下文、工具、执行、验证</td>
<td>流程、规范、模板、经验、约束</td>
</tr>
<tr>
<td>是否主动执行</td>
<td>会主动推进任务</td>
<td>通常不主动执行</td>
</tr>
<tr>
<td>典型形态</td>
<td>具备工具权限的 AI 助手</td>
<td>说明文档、脚本、模板或工作流</td>
</tr>
</tbody></table>
<p>一句话概括：</p>
<p><strong>Agent 是“谁来做”，Skill 是“怎么做得更专业”。</strong></p>
<p>Agent 没有 Skill，也可以完成任务，但结果可能不够稳定，每次风格和判断标准都不一样。</p>
<p>Skill 没有 Agent，也只是静态经验，需要被某个执行者调用后才会产生价值。</p>
<p>两者结合起来，才能形成更稳定、可复用的 AI 工作流。</p>
<hr>
<h2 id="五、什么时候需要-Agent，什么时候需要-Skill"><a href="#五、什么时候需要-Agent，什么时候需要-Skill" class="headerlink" title="五、什么时候需要 Agent，什么时候需要 Skill"></a>五、什么时候需要 Agent，什么时候需要 Skill</h2><h3 id="适合使用-Agent-的场景"><a href="#适合使用-Agent-的场景" class="headerlink" title="适合使用 Agent 的场景"></a>适合使用 Agent 的场景</h3><p>如果任务需要连续判断、工具调用和结果验证，就适合交给 Agent。</p>
<p>典型场景包括：</p>
<ul>
<li>修复一个代码 bug</li>
<li>阅读项目并生成技术文档</li>
<li>优化一篇博客文章并更新文件</li>
<li>调用外部 API 获取数据后生成报告</li>
<li>根据测试结果继续调整实现</li>
</ul>
<p>这些任务都有一个共同点：不是回答一句话就结束，而是需要持续推进。</p>
<h3 id="适合沉淀成-Skill-的场景"><a href="#适合沉淀成-Skill-的场景" class="headerlink" title="适合沉淀成 Skill 的场景"></a>适合沉淀成 Skill 的场景</h3><p>如果某类任务会反复出现，而且希望结果保持稳定，就适合沉淀成 Skill。</p>
<p>典型场景包括：</p>
<ul>
<li>博客文章优化规范</li>
<li>代码审查流程</li>
<li>前端设计规范</li>
<li>API 文档生成流程</li>
<li>项目初始化模板</li>
<li>发布前检查清单</li>
</ul>
<p>这些任务的重点不是“今天完成一次”，而是“以后每次都按同样的标准完成”。</p>
<hr>
<h2 id="六、为什么不要把所有东西都写进提示词"><a href="#六、为什么不要把所有东西都写进提示词" class="headerlink" title="六、为什么不要把所有东西都写进提示词"></a>六、为什么不要把所有东西都写进提示词</h2><p>很多人在刚开始使用 AI 工具时，会不断加长提示词：</p>
<blockquote>
<p>你要这样做，不要那样做，遇到这种情况先检查这个文件，输出时按这个格式，风格要像之前那篇文章……</p>
</blockquote>
<p>短期看，这种方式有效。</p>
<p>但提示词越长，维护成本越高，也越难复用。每次换任务、换项目、换上下文，都要重新组织一遍。</p>
<p>更好的方式是分层：</p>
<ul>
<li>通用执行能力交给 Agent</li>
<li>稳定经验沉淀成 Skill</li>
<li>当前提示词只描述这一次的目标和材料</li>
</ul>
<p>这样提示词会变短，任务边界也更清楚。</p>
<p>AI 的使用方式也会从“一次性问答”变成“可加载工作流的执行系统”。</p>
<hr>
<h2 id="七、用写作任务理解-Agent-和-Skill"><a href="#七、用写作任务理解-Agent-和-Skill" class="headerlink" title="七、用写作任务理解 Agent 和 Skill"></a>七、用写作任务理解 Agent 和 Skill</h2><p>以博客文章优化为例。</p>
<p>Writer Agent 负责执行这次任务：</p>
<ul>
<li>读取文章草稿</li>
<li>理解发布目标</li>
<li>调整文章结构</li>
<li>修改 Markdown 文件</li>
<li>补齐发布字段</li>
<li>检查构建结果</li>
</ul>
<p>博客内容优化 Skill 则负责提供稳定方法：</p>
<ul>
<li>不编造事实</li>
<li>保留作者核心观点</li>
<li>中文表达自然</li>
<li>技术内容谨慎准确</li>
<li>默认输出优化方向、完整文章、修改说明、标题候选和平台标签</li>
</ul>
<p>当用户提出：</p>
<blockquote>
<p>帮我把这篇草稿优化成适合个人博客和公众号发布的文章。</p>
</blockquote>
<p>Agent 负责完成这次交付，Skill 负责保证这类任务的执行方式稳定。</p>
<p>这时得到的结果就不只是一次“文本改写”，而是更接近真实工作流的一次任务完成。</p>
<hr>
<h2 id="八、总结"><a href="#八、总结" class="headerlink" title="八、总结"></a>八、总结</h2><p>Agent 和 Skill 不是互相替代的关系。</p>
<p>更准确地说：</p>
<ul>
<li><strong>Agent 是执行者</strong>：负责理解目标、读取上下文、调用工具、推进任务、验证结果。</li>
<li><strong>Skill 是能力沉淀</strong>：负责把某类任务的经验、流程、规范和模板复用起来。</li>
</ul>
<p>如果只有 Agent，没有 Skill，任务也能做，但结果可能不够稳定。</p>
<p>如果只有 Skill，没有 Agent，它只是静态说明，不能主动完成任务。</p>
<p>更有价值的方式是：</p>
<p><strong>用 Agent 执行任务，用 Skill 沉淀方法。</strong></p>
<p>理解这层关系后，使用 AI 工具时就不只是思考“提示词怎么写”，而是会进一步思考：</p>
<ul>
<li>哪些任务应该交给 Agent？</li>
<li>哪些经验应该沉淀成 Skill？</li>
<li>哪些流程可以长期复用？</li>
</ul>
<p>这才是 AI 工作流真正变得稳定、可复制的开始。</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>AI Agent</tag>
        <tag>Skill</tag>
      </tags>
  </entry>
  <entry>
    <title>从 0 跑通 MCP：用 TypeScript 写一个 CoinGecko 工具，并接入 Inspector / Codex</title>
    <url>/posts/2026/04/09/4dc832d4/</url>
    <content><![CDATA[<p>如果你已经看过不少 MCP 介绍，但还没有亲手跑通过一个完整例子，这篇文章就是为你准备的。</p>
<p>本文不先展开复杂概念，也不一开始讨论远程部署，而是只做一件事：</p>
<p><strong>从零写一个最小的 TypeScript MCP Server，接入 CoinGecko 的真实市场数据接口，并在 MCP Inspector 和 Codex 中完成验证。</strong></p>
<p>这个示例最终只暴露一个工具：<code>fetch_market_data</code>。</p>
<p>它的功能很简单：输入 <code>BTC</code>、<code>ETH</code> 或 <code>SOL</code>，返回来自 CoinGecko 的实时市场价格、24 小时涨跌幅和更新时间。</p>
<p>但这个小工具会把 MCP 入门最关键的链路完整跑通：</p>
<ul>
<li>写一个本地 MCP Server</li>
<li>注册一个可被宿主发现的工具</li>
<li>接入真实第三方 API</li>
<li>用 Inspector 做工具级调试</li>
<li>用 Codex 验证宿主是否真的调用了工具</li>
</ul>
<p>配套示例代码已放在 GitHub，建议边看边对照实践：</p>
<ul>
<li>GitHub 仓库：<a href="https://github.com/BreakZero/mcp-tooling-example">mcp-tooling-example</a></li>
</ul>
<hr>
<h2 id="一、这篇文章解决什么问题"><a href="#一、这篇文章解决什么问题" class="headerlink" title="一、这篇文章解决什么问题"></a>一、这篇文章解决什么问题</h2><p>学 MCP 时，新手最容易卡住的通常不是“概念完全看不懂”，而是：</p>
<p><strong>不知道一个最小可运行项目到底应该长什么样。</strong></p>
<p>所以本文的目标很明确。读完并跑通后，你应该能完成下面几件事：</p>
<ol>
<li>理解 MCP 在这个例子里的定位</li>
<li>用 TypeScript 写一个最小 MCP Server</li>
<li>注册一个可调用工具：<code>fetch_market_data</code></li>
<li>让工具真实调用 CoinGecko API</li>
<li>用 MCP Inspector 验证工具是否可用</li>
<li>把它接入 Codex CLI，验证宿主是否能调用它</li>
</ol>
<p>这不是一个只返回 <code>hello world</code> 的演示，而是一个有输入、有外部数据、有工具注册、有调试验证的完整入门项目。</p>
<hr>
<h2 id="二、先建立一个最小认知：MCP-在这里是什么"><a href="#二、先建立一个最小认知：MCP-在这里是什么" class="headerlink" title="二、先建立一个最小认知：MCP 在这里是什么"></a>二、先建立一个最小认知：MCP 在这里是什么</h2><p>在开始写代码前，先把最关键的一点说清楚：</p>
<p><strong>MCP 不是业务逻辑本身，而是一层标准化的工具协议。</strong></p>
<p>在本文这个例子里，调用链路大概是这样：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">Codex / ChatGPT / Inspector
    -&gt; MCP Client
    -&gt; 你的 MCP Server
    -&gt; 你的 TypeScript 逻辑
    -&gt; CoinGecko API<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>每一层职责不同：</p>
<ul>
<li><strong>宿主（Host）</strong>：例如 Codex、Inspector、ChatGPT，负责发现工具、调用工具、消费结果。</li>
<li><strong>MCP Client</strong>：按 MCP 协议与 Server 通信。</li>
<li><strong>MCP Server</strong>：把你的能力暴露成标准工具。</li>
<li><strong>业务逻辑</strong>：真正做事的代码，例如根据 symbol 拉取市场价格。</li>
<li><strong>第三方 API</strong>：例如 CoinGecko，提供真实数据来源。</li>
</ul>
<p>所以，本文真正要实现的能力是：</p>
<p><strong>输入一个币种 symbol，比如 BTC，返回对应的实时市场数据。</strong></p>
<p>MCP 负责的是让这个能力能被宿主以标准方式发现和调用。</p>
<p>这点很重要。很多人刚接触 MCP 时，会把 MCP、Agent、业务逻辑、应用框架混在一起。更准确的理解是：</p>
<p><strong>MCP 解决的是“能力如何暴露和被调用”，不是“业务本身如何实现”。</strong></p>
<h3 id="为什么先用-STDIO，而不是-HTTP"><a href="#为什么先用-STDIO，而不是-HTTP" class="headerlink" title="为什么先用 STDIO，而不是 HTTP"></a>为什么先用 STDIO，而不是 HTTP</h3><p>MCP 支持不同传输方式，但入门阶段我建议先用本地 <strong>STDIO</strong>。</p>
<p>原因很直接：</p>
<ul>
<li>不需要先搭 Web 服务</li>
<li>不需要处理端口和路由</li>
<li>不需要 HTTPS 或公网暴露</li>
<li>本地调试链路更短，问题更容易定位</li>
</ul>
<p>入门阶段最重要的是先完成一个闭环：<strong>工具写出来，并且真的能被宿主调用。</strong></p>
<p>所以本文的策略是：</p>
<p><strong>先用 STDIO 跑通最小闭环，再考虑远程服务化。</strong></p>
<hr>
<h2 id="三、从-0-搭一个最小-MCP-Server"><a href="#三、从-0-搭一个最小-MCP-Server" class="headerlink" title="三、从 0 搭一个最小 MCP Server"></a>三、从 0 搭一个最小 MCP Server</h2><h3 id="1-准备工作"><a href="#1-准备工作" class="headerlink" title="1. 准备工作"></a>1. 准备工作</h3><p>你需要准备：</p>
<ul>
<li>Node.js 18 或更高版本</li>
<li>npm</li>
<li>一个 CoinGecko API Key</li>
<li>Codex CLI（后文用于扩展验证，可选）</li>
</ul>
<h4 id="获取-CoinGecko-API-Key"><a href="#获取-CoinGecko-API-Key" class="headerlink" title="获取 CoinGecko API Key"></a>获取 CoinGecko API Key</h4><p>CoinGecko 支持通过请求头传递 API Key：</p>
<ul>
<li>Demo Key：<code>x-cg-demo-api-key</code></li>
<li>Pro Key：<code>x-cg-pro-api-key</code></li>
</ul>
<p>本文用 Demo Key 即可。我们的目标是跑通 MCP 链路，不是做生产级行情服务。</p>
<h3 id="2-创建项目"><a href="#2-创建项目" class="headerlink" title="2. 创建项目"></a>2. 创建项目</h3><p>先初始化一个最小项目：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">mkdir</span> mcp-coingecko-demo
<span class="token builtin class-name">cd</span> mcp-coingecko-demo
<span class="token function">npm</span> init <span class="token parameter variable">-y</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>安装依赖：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">npm</span> <span class="token function">install</span> @modelcontextprotocol/sdk zod dotenv
<span class="token function">npm</span> <span class="token function">install</span> <span class="token parameter variable">-D</span> typescript tsx @types/node<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>初始化 TypeScript 配置：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">npx tsc <span class="token parameter variable">--init</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>到这里，一个可以编写 MCP Server 的基础项目就准备好了。</p>
<h3 id="3-设计项目结构"><a href="#3-设计项目结构" class="headerlink" title="3. 设计项目结构"></a>3. 设计项目结构</h3><p>为了让示例足够简单，同时避免把所有逻辑塞进一个文件，建议使用下面的结构：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">mcp-coingecko-demo/
  package.json
  .env
  src/
    server.ts
    domain/
      market.ts<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>两个核心文件的职责：</p>
<ul>
<li><code>src/server.ts</code>：MCP Server 入口，负责注册工具、定义输入、连接传输层。</li>
<li><code>src/domain/market.ts</code>：业务逻辑层，负责调用 CoinGecko API 并整理市场数据。</li>
</ul>
<p>这样拆分有一个好处：你可以清楚地区分 <strong>MCP 协议层</strong> 和 <strong>业务逻辑层</strong>。</p>
<p>对入门项目来说，这不是为了“架构感”，而是为了减少理解成本。后面要增加第二个工具、替换数据源，或者写测试，都会更容易。</p>
<h3 id="4-配置环境变量"><a href="#4-配置环境变量" class="headerlink" title="4. 配置环境变量"></a>4. 配置环境变量</h3><p>在项目根目录创建 <code>.env</code>：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token assign-left variable">COINGECKO_API_KEY</span><span class="token operator">=</span>your_demo_key_here
<span class="token assign-left variable">COINGECKO_API_PLAN</span><span class="token operator">=</span>demo<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>两个变量分别表示：</p>
<ul>
<li><code>COINGECKO_API_KEY</code>：你的 CoinGecko API Key</li>
<li><code>COINGECKO_API_PLAN</code>：当前使用的套餐类型，本文使用 <code>demo</code></li>
</ul>
<p>配置放进环境变量后，后续切换 Pro Key 或不同环境时，就不需要改业务代码。</p>
<h3 id="5-先写业务逻辑：获取市场数据"><a href="#5-先写业务逻辑：获取市场数据" class="headerlink" title="5. 先写业务逻辑：获取市场数据"></a>5. 先写业务逻辑：获取市场数据</h3><p>新建 <code>src/domain/market.ts</code>：</p>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">import</span> <span class="token string">"dotenv/config"</span><span class="token punctuation">;</span>

<span class="token keyword">export</span> <span class="token keyword">type</span> <span class="token class-name">MarketData</span> <span class="token operator">=</span> <span class="token punctuation">{</span>
  id<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  <span class="token builtin">symbol</span><span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  price<span class="token operator">:</span> <span class="token builtin">number</span><span class="token punctuation">;</span>
  currency<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  source<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  change24h<span class="token operator">?</span><span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">|</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
  lastUpdatedAt<span class="token operator">?</span><span class="token operator">:</span> <span class="token builtin">number</span> <span class="token operator">|</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> <span class="token constant">SYMBOL_TO_COINGECKO_ID</span><span class="token operator">:</span> Record<span class="token operator">&lt;</span><span class="token builtin">string</span><span class="token punctuation">,</span> <span class="token builtin">string</span><span class="token operator">&gt;</span> <span class="token operator">=</span> <span class="token punctuation">{</span>
  <span class="token constant">BTC</span><span class="token operator">:</span> <span class="token string">"bitcoin"</span><span class="token punctuation">,</span>
  <span class="token constant">ETH</span><span class="token operator">:</span> <span class="token string">"ethereum"</span><span class="token punctuation">,</span>
  <span class="token constant">SOL</span><span class="token operator">:</span> <span class="token string">"solana"</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">function</span> <span class="token function">getCoinGeckoConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> plan <span class="token operator">=</span> <span class="token punctuation">(</span>process<span class="token punctuation">.</span>env<span class="token punctuation">.</span><span class="token constant">COINGECKO_API_PLAN</span> <span class="token operator">||</span> <span class="token string">"demo"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toLowerCase</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">const</span> apiKey <span class="token operator">=</span> process<span class="token punctuation">.</span>env<span class="token punctuation">.</span><span class="token constant">COINGECKO_API_KEY</span><span class="token punctuation">;</span>

  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>apiKey<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span><span class="token string">"Missing COINGECKO_API_KEY"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">if</span> <span class="token punctuation">(</span>plan <span class="token operator">===</span> <span class="token string">"pro"</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token punctuation">{</span>
      baseUrl<span class="token operator">:</span> <span class="token string">"https://pro-api.coingecko.com/api/v3"</span><span class="token punctuation">,</span>
      headers<span class="token operator">:</span> <span class="token punctuation">{</span>
        <span class="token string-property property">"x-cg-pro-api-key"</span><span class="token operator">:</span> apiKey<span class="token punctuation">,</span>
      <span class="token punctuation">}</span><span class="token punctuation">,</span>
      source<span class="token operator">:</span> <span class="token string">"coingecko-pro"</span><span class="token punctuation">,</span>
    <span class="token punctuation">}</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">return</span> <span class="token punctuation">{</span>
    baseUrl<span class="token operator">:</span> <span class="token string">"https://api.coingecko.com/api/v3"</span><span class="token punctuation">,</span>
    headers<span class="token operator">:</span> <span class="token punctuation">{</span>
      <span class="token string-property property">"x-cg-demo-api-key"</span><span class="token operator">:</span> apiKey<span class="token punctuation">,</span>
    <span class="token punctuation">}</span><span class="token punctuation">,</span>
    source<span class="token operator">:</span> <span class="token string">"coingecko-demo"</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">function</span> <span class="token function">resolveCoinId</span><span class="token punctuation">(</span><span class="token builtin">symbol</span><span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">)</span><span class="token operator">:</span> <span class="token builtin">string</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> normalizedSymbol <span class="token operator">=</span> <span class="token builtin">symbol</span><span class="token punctuation">.</span><span class="token function">trim</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toUpperCase</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>normalizedSymbol<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span><span class="token string">"symbol is required"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">const</span> coinId <span class="token operator">=</span> <span class="token constant">SYMBOL_TO_COINGECKO_ID</span><span class="token punctuation">[</span>normalizedSymbol<span class="token punctuation">]</span><span class="token punctuation">;</span>

  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>coinId<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">Unsupported symbol: </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>normalizedSymbol<span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">return</span> coinId<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchMarketData</span><span class="token punctuation">(</span><span class="token builtin">symbol</span><span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">)</span><span class="token operator">:</span> <span class="token builtin">Promise</span><span class="token operator">&lt;</span>MarketData<span class="token operator">&gt;</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> normalizedSymbol <span class="token operator">=</span> <span class="token builtin">symbol</span><span class="token punctuation">.</span><span class="token function">trim</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toUpperCase</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">const</span> coinId <span class="token operator">=</span> <span class="token function">resolveCoinId</span><span class="token punctuation">(</span>normalizedSymbol<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">const</span> <span class="token punctuation">{</span> baseUrl<span class="token punctuation">,</span> headers<span class="token punctuation">,</span> source <span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">getCoinGeckoConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">const</span> url <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name"><span class="token constant">URL</span></span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>baseUrl<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">/simple/price</span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  url<span class="token punctuation">.</span>searchParams<span class="token punctuation">.</span><span class="token function">set</span><span class="token punctuation">(</span><span class="token string">"ids"</span><span class="token punctuation">,</span> coinId<span class="token punctuation">)</span><span class="token punctuation">;</span>
  url<span class="token punctuation">.</span>searchParams<span class="token punctuation">.</span><span class="token function">set</span><span class="token punctuation">(</span><span class="token string">"vs_currencies"</span><span class="token punctuation">,</span> <span class="token string">"usd"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  url<span class="token punctuation">.</span>searchParams<span class="token punctuation">.</span><span class="token function">set</span><span class="token punctuation">(</span><span class="token string">"include_24hr_change"</span><span class="token punctuation">,</span> <span class="token string">"true"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  url<span class="token punctuation">.</span>searchParams<span class="token punctuation">.</span><span class="token function">set</span><span class="token punctuation">(</span><span class="token string">"include_last_updated_at"</span><span class="token punctuation">,</span> <span class="token string">"true"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">const</span> response <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">fetch</span><span class="token punctuation">(</span>url<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
    method<span class="token operator">:</span> <span class="token string">"GET"</span><span class="token punctuation">,</span>
    headers<span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>response<span class="token punctuation">.</span>ok<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">const</span> text <span class="token operator">=</span> <span class="token keyword">await</span> response<span class="token punctuation">.</span><span class="token function">text</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">CoinGecko API error: </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>response<span class="token punctuation">.</span>status<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string"> </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>text<span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">const</span> json <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token keyword">await</span> response<span class="token punctuation">.</span><span class="token function">json</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">as</span> Record<span class="token operator">&lt;</span>
    <span class="token builtin">string</span><span class="token punctuation">,</span>
    <span class="token punctuation">{</span>
      usd<span class="token operator">?</span><span class="token operator">:</span> <span class="token builtin">number</span><span class="token punctuation">;</span>
      usd_24h_change<span class="token operator">?</span><span class="token operator">:</span> <span class="token builtin">number</span><span class="token punctuation">;</span>
      last_updated_at<span class="token operator">?</span><span class="token operator">:</span> <span class="token builtin">number</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
  <span class="token operator">&gt;</span><span class="token punctuation">;</span>

  <span class="token keyword">const</span> data <span class="token operator">=</span> json<span class="token punctuation">[</span>coinId<span class="token punctuation">]</span><span class="token punctuation">;</span>

  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>data <span class="token operator">||</span> <span class="token keyword">typeof</span> data<span class="token punctuation">.</span>usd <span class="token operator">!==</span> <span class="token string">"number"</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">No price data returned for </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>coinId<span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">return</span> <span class="token punctuation">{</span>
    id<span class="token operator">:</span> coinId<span class="token punctuation">,</span>
    <span class="token builtin">symbol</span><span class="token operator">:</span> normalizedSymbol<span class="token punctuation">,</span>
    price<span class="token operator">:</span> data<span class="token punctuation">.</span>usd<span class="token punctuation">,</span>
    currency<span class="token operator">:</span> <span class="token string">"USD"</span><span class="token punctuation">,</span>
    source<span class="token punctuation">,</span>
    change24h<span class="token operator">:</span> <span class="token keyword">typeof</span> data<span class="token punctuation">.</span>usd_24h_change <span class="token operator">===</span> <span class="token string">"number"</span> <span class="token operator">?</span> data<span class="token punctuation">.</span>usd_24h_change <span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span>
    lastUpdatedAt<span class="token operator">:</span>
      <span class="token keyword">typeof</span> data<span class="token punctuation">.</span>last_updated_at <span class="token operator">===</span> <span class="token string">"number"</span> <span class="token operator">?</span> data<span class="token punctuation">.</span>last_updated_at <span class="token operator">:</span> <span class="token keyword">null</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这段代码主要做了 5 件事：</p>
<ol>
<li>读取环境变量，判断使用 Demo 还是 Pro 配置</li>
<li>把用户输入的 symbol 映射成 CoinGecko 识别的 coin id</li>
<li>调用 CoinGecko 的 <code>/simple/price</code> 接口</li>
<li>提取价格、24 小时涨跌幅和更新时间</li>
<li>整理成统一的 <code>MarketData</code> 结构返回</li>
</ol>
<p>也就是说，这一层解决的是：</p>
<p><strong>如何把用户能理解的输入，转换成第三方 API 能理解的请求，再把返回值整理成自己可控的输出。</strong></p>
<h4 id="为什么先手写-symbol-映射"><a href="#为什么先手写-symbol-映射" class="headerlink" title="为什么先手写 symbol 映射"></a>为什么先手写 symbol 映射</h4><p>这里没有一开始就支持所有币种，而是先手写了几个映射：</p>
<ul>
<li><code>BTC -&gt; bitcoin</code></li>
<li><code>ETH -&gt; ethereum</code></li>
<li><code>SOL -&gt; solana</code></li>
</ul>
<p>这不是最完整的方案，但很适合入门。</p>
<p>原因很简单：先降低复杂度，把工具注册、API 调用、数据返回、宿主调用这条主链路跑通。至于自动支持所有 symbol、缓存 <code>/coins/list</code>、模糊搜索、错误纠正，都可以放到下一阶段。</p>
<p>技术学习里，一个很实用的原则是：</p>
<p><strong>先完成最小可运行版本，再逐步扩展能力。</strong></p>
<h3 id="6-再写-MCP-Server：把能力暴露成工具"><a href="#6-再写-MCP-Server：把能力暴露成工具" class="headerlink" title="6. 再写 MCP Server：把能力暴露成工具"></a>6. 再写 MCP Server：把能力暴露成工具</h3><p>业务逻辑已经独立可用后，再写 <code>src/server.ts</code>：</p>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">import</span> <span class="token punctuation">{</span> McpServer <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"@modelcontextprotocol/sdk/server/mcp.js"</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> StdioServerTransport <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"@modelcontextprotocol/sdk/server/stdio.js"</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> z <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"zod"</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> fetchMarketData <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">"./domain/market.js"</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> server <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">McpServer</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  name<span class="token operator">:</span> <span class="token string">"market-data-server"</span><span class="token punctuation">,</span>
  version<span class="token operator">:</span> <span class="token string">"1.1.0"</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

server<span class="token punctuation">.</span><span class="token function">registerTool</span><span class="token punctuation">(</span>
  <span class="token string">"fetch_market_data"</span><span class="token punctuation">,</span>
  <span class="token punctuation">{</span>
    description<span class="token operator">:</span>
      <span class="token string">"Fetch crypto market data from CoinGecko for symbols like BTC, ETH, or SOL"</span><span class="token punctuation">,</span>
    inputSchema<span class="token operator">:</span> <span class="token punctuation">{</span>
      <span class="token builtin">symbol</span><span class="token operator">:</span> z
        <span class="token punctuation">.</span><span class="token function">string</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">min</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token string">"symbol cannot be empty"</span><span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">describe</span><span class="token punctuation">(</span><span class="token string">"Crypto asset symbol, for example BTC, ETH, or SOL"</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token keyword">async</span> <span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token builtin">symbol</span> <span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
    <span class="token keyword">try</span> <span class="token punctuation">{</span>
      <span class="token keyword">const</span> result <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">fetchMarketData</span><span class="token punctuation">(</span><span class="token builtin">symbol</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token keyword">return</span> <span class="token punctuation">{</span>
        content<span class="token operator">:</span> <span class="token punctuation">[</span>
          <span class="token punctuation">{</span>
            type<span class="token operator">:</span> <span class="token string">"text"</span><span class="token punctuation">,</span>
            text<span class="token operator">:</span> <span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span>result<span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
          <span class="token punctuation">}</span><span class="token punctuation">,</span>
        <span class="token punctuation">]</span><span class="token punctuation">,</span>
      <span class="token punctuation">}</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span>error<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">const</span> message <span class="token operator">=</span>
        error <span class="token keyword">instanceof</span> <span class="token class-name">Error</span> <span class="token operator">?</span> error<span class="token punctuation">.</span>message <span class="token operator">:</span> <span class="token string">"Unknown error occurred"</span><span class="token punctuation">;</span>

      <span class="token keyword">return</span> <span class="token punctuation">{</span>
        content<span class="token operator">:</span> <span class="token punctuation">[</span>
          <span class="token punctuation">{</span>
            type<span class="token operator">:</span> <span class="token string">"text"</span><span class="token punctuation">,</span>
            text<span class="token operator">:</span> <span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">Failed to fetch market data: </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>message<span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">`</span></span><span class="token punctuation">,</span>
          <span class="token punctuation">}</span><span class="token punctuation">,</span>
        <span class="token punctuation">]</span><span class="token punctuation">,</span>
        isError<span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
      <span class="token punctuation">}</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> transport <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">StdioServerTransport</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">await</span> server<span class="token punctuation">.</span><span class="token function">connect</span><span class="token punctuation">(</span>transport<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"MCP server is running..."</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">catch</span><span class="token punctuation">(</span><span class="token punctuation">(</span>error<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
  <span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"Server error:"</span><span class="token punctuation">,</span> error<span class="token punctuation">)</span><span class="token punctuation">;</span>
  process<span class="token punctuation">.</span><span class="token function">exit</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这段 MCP Server 代码可以拆成 4 步理解：</p>
<ol>
<li>创建一个 MCP Server，声明服务名称和版本。</li>
<li>注册 <code>fetch_market_data</code> 工具。</li>
<li>用 Zod 描述输入参数，让宿主知道工具需要什么。</li>
<li>在工具处理函数中调用 <code>fetchMarketData</code>，并把结果按 MCP 工具返回格式输出。</li>
</ol>
<p>这里最重要的不是 API 细节，而是分层关系：</p>
<p><strong>MCP 层只负责“暴露工具”，真正的业务仍然在 <code>domain/market.ts</code> 里。</strong></p>
<p>如果一开始把协议代码和业务代码都写在一个文件里，新手很容易分不清“工具注册”和“行情获取”分别在解决什么问题。</p>
<h3 id="7-启动服务，并注意-STDIO-日志"><a href="#7-启动服务，并注意-STDIO-日志" class="headerlink" title="7. 启动服务，并注意 STDIO 日志"></a>7. 启动服务，并注意 STDIO 日志</h3><p>给 <code>package.json</code> 增加脚本：</p>
<pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"commonjs"</span><span class="token punctuation">,</span>
  <span class="token property">"scripts"</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token property">"dev"</span><span class="token operator">:</span> <span class="token string">"tsx watch src/server.ts"</span><span class="token punctuation">,</span>
    <span class="token property">"build"</span><span class="token operator">:</span> <span class="token string">"tsc src/server.ts --outDir dist --module commonjs --target es2020 --esModuleInterop"</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>然后启动：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">npm</span> run dev<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>如果进程没有退出，而是保持运行、等待输入，通常说明 MCP Server 已经启动成功。</p>
<p>这里有一个入门时很容易忽略的细节：</p>
<p><strong>对 STDIO Server 来说，“启动后挂起等待输入”是正常现象，不是程序卡死。</strong></p>
<p>因为它本来就在等待宿主通过标准输入和它通信。</p>
<h4 id="日志为什么要写到-stderr"><a href="#日志为什么要写到-stderr" class="headerlink" title="日志为什么要写到 stderr"></a>日志为什么要写到 stderr</h4><p>如果使用 STDIO 传输，<code>stdout</code> 是 MCP 协议消息通道，不能随便输出普通日志。</p>
<p>下面这种写法有风险：</p>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"server started"</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>普通日志一旦混进协议输出，可能会破坏 MCP 通信。</p>
<p>所以在 STDIO 模式下，日志应该写到 <code>stderr</code>：</p>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"MCP server is running..."</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>很多人第一次跑不通，不是 MCP 概念理解错了，而是被这类运行细节卡住了。</p>
<hr>
<h2 id="四、验证工具是否真的可用"><a href="#四、验证工具是否真的可用" class="headerlink" title="四、验证工具是否真的可用"></a>四、验证工具是否真的可用</h2><h3 id="1-用-MCP-Inspector-验证"><a href="#1-用-MCP-Inspector-验证" class="headerlink" title="1. 用 MCP Inspector 验证"></a>1. 用 MCP Inspector 验证</h3><p>在接入 Codex 前，建议先用 MCP Inspector 验证。Inspector 更适合做工具级调试，可以先排除 Server 本身的问题。</p>
<p>如果已经编译成 JS，可以这样启动：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">npm</span> run build
npx @modelcontextprotocol/inspector <span class="token function">node</span> dist/server.js<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>如果你习惯直接用 <code>tsx</code> 跑，也可以按自己的本地方式配置。</p>
<p>在 Inspector 阶段，重点看 3 件事：</p>
<ul>
<li>Server 是否连接成功</li>
<li>是否能看到 <code>fetch_market_data</code></li>
<li>工具是否真的可以被调用</li>
</ul>
<p>先测试正常输入：</p>
<pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"symbol"</span><span class="token operator">:</span> <span class="token string">"BTC"</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>预期返回类似：</p>
<pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"id"</span><span class="token operator">:</span> <span class="token string">"bitcoin"</span><span class="token punctuation">,</span>
  <span class="token property">"symbol"</span><span class="token operator">:</span> <span class="token string">"BTC"</span><span class="token punctuation">,</span>
  <span class="token property">"price"</span><span class="token operator">:</span> <span class="token number">123456</span><span class="token punctuation">,</span>
  <span class="token property">"currency"</span><span class="token operator">:</span> <span class="token string">"USD"</span><span class="token punctuation">,</span>
  <span class="token property">"source"</span><span class="token operator">:</span> <span class="token string">"coingecko-demo"</span><span class="token punctuation">,</span>
  <span class="token property">"change24h"</span><span class="token operator">:</span> <span class="token number">1.23</span><span class="token punctuation">,</span>
  <span class="token property">"lastUpdatedAt"</span><span class="token operator">:</span> <span class="token number">1712345678</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>价格会变化，这也说明你拿到的是实时数据，而不是写死的 mock。</p>
<p>再测试几个异常输入：</p>
<pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"symbol"</span><span class="token operator">:</span> <span class="token string">""</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>
  <span class="token property">"symbol"</span><span class="token operator">:</span> <span class="token string">"DOGE"</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>这一步不是为了让所有输入都成功，而是确认：</p>
<ul>
<li>合法输入能正常返回</li>
<li>非法输入不会把 Server 打崩</li>
<li>错误信息对人来说是可理解的</li>
</ul>
<p>一个入门项目能做到这里，已经比纯演示性质的示例更接近真实开发。</p>
<h4 id="Inspector-验证截图"><a href="#Inspector-验证截图" class="headerlink" title="Inspector 验证截图"></a>Inspector 验证截图</h4><p>下面这张图是 <code>fetch_market_data</code> 在 MCP Inspector 中连接成功并调用成功的结果。可以看到：</p>
<ul>
<li>左侧已成功连接本地 <code>market-data-server</code></li>
<li>工具列表中出现了 <code>fetch_market_data</code></li>
<li>右侧输入 <code>ETH</code> 后，工具返回了来自 CoinGecko 的真实市场数据</li>
<li><code>Tool Result: Success</code> 说明这条本地 MCP 调用链路已经跑通</li>
</ul>
<p><img src="/images/20260409/inspector-validation-success.png" alt="Inspector 连接验证成功结果"></p>
<h3 id="2-接入-Codex-CLI-验证"><a href="#2-接入-Codex-CLI-验证" class="headerlink" title="2. 接入 Codex CLI 验证"></a>2. 接入 Codex CLI 验证</h3><p>Inspector 验证通过后，再接入 Codex 会更稳。因为这时你已经知道工具本身可用，剩下的问题只可能出在宿主配置或调用方式上。</p>
<p>创建或编辑 Codex 配置文件：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">~/.codex/config.toml<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>加入 MCP Server 配置：</p>
<pre class="line-numbers language-toml" data-language="toml"><code class="language-toml"><span class="token key property">model</span> <span class="token punctuation">=</span> <span class="token string">"gpt-5.4"</span>

<span class="token punctuation">[</span><span class="token table class-name">mcp_servers.market_data</span><span class="token punctuation">]</span>
<span class="token key property">command</span> <span class="token punctuation">=</span> <span class="token string">"npx"</span>
<span class="token key property">args</span> <span class="token punctuation">=</span> <span class="token punctuation">[</span><span class="token string">"tsx"</span><span class="token punctuation">,</span> <span class="token string">"/绝对路径/mcp-coingecko-demo/src/server.ts"</span><span class="token punctuation">]</span>

<span class="token punctuation">[</span><span class="token table class-name">mcp_servers.market_data.env</span><span class="token punctuation">]</span>
<span class="token key property">COINGECKO_API_KEY</span> <span class="token punctuation">=</span> <span class="token string">"your_demo_key_here"</span>
<span class="token key property">COINGECKO_API_PLAN</span> <span class="token punctuation">=</span> <span class="token string">"demo"</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>注意把路径替换成你自己的绝对路径。</p>
<p>启动 Codex：</p>
<pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">codex<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>第一次测试时，建议明确要求它调用工具：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">Use the MCP tool fetch_market_data with symbol BTC. Return the raw result first, then explain it.<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>验证阶段不需要追求提示词自然，目标是确认宿主是否真的触发了工具调用。</p>
<h3 id="如何判断-Codex-不是在“猜答案”"><a href="#如何判断-Codex-不是在“猜答案”" class="headerlink" title="如何判断 Codex 不是在“猜答案”"></a>如何判断 Codex 不是在“猜答案”</h3><p>重点看返回结果里是否出现你在工具中定义过的字段，例如：</p>
<ul>
<li><code>source: coingecko-demo</code></li>
<li><code>id: bitcoin</code></li>
<li><code>change24h</code></li>
<li><code>lastUpdatedAt</code></li>
</ul>
<p>如果这些字段都出现了，通常就可以判断：</p>
<p><strong>Codex 不是在凭语言模型常识回答，而是真的通过 MCP 调用了你的工具。</strong></p>
<h4 id="Codex-调用截图"><a href="#Codex-调用截图" class="headerlink" title="Codex 调用截图"></a>Codex 调用截图</h4><p>下面这张图展示的是把 MCP Server 接入 Codex 后的调用结果。可以看到 Codex 明确调用了：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">market_data.fetch_market_data({"symbol":"BTC"})<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>并返回了你在工具中定义的字段：</p>
<ul>
<li><code>id: bitcoin</code></li>
<li><code>symbol: BTC</code></li>
<li><code>price</code></li>
<li><code>source: coingecko-demo</code></li>
<li><code>change24h</code></li>
<li><code>lastUpdatedAt</code></li>
</ul>
<p>这说明 Codex 已经通过 MCP 工具拿到了真实返回结果。</p>
<p><img src="/images/20260409/codex-execution-success.png" alt="Codex 配置执行结果"></p>
<hr>
<h2 id="五、跑通之后，你真正掌握了什么"><a href="#五、跑通之后，你真正掌握了什么" class="headerlink" title="五、跑通之后，你真正掌握了什么"></a>五、跑通之后，你真正掌握了什么</h2><p>如果前面的步骤都已经跑通，那么你完成的不是一个简单 demo，而是一个完整的 MCP 入门闭环：</p>
<ol>
<li>写了一个 TypeScript MCP Server</li>
<li>注册了一个真实可用的工具 <code>fetch_market_data</code></li>
<li>接入了第三方 API，而不是只返回静态内容</li>
<li>用 Inspector 验证了工具层是否正常</li>
<li>用 Codex 验证了宿主能否调用工具</li>
</ol>
<p>这一步很关键，因为它意味着你已经从“知道 MCP 概念”进入了“能做出一个可验证工具”的阶段。</p>
<p>本文本质上只做了一件事：</p>
<p><strong>用一个真实的 CoinGecko 市场数据工具，把 MCP 的完整入门流程跑通。</strong></p>
<p>刚接触 MCP 时，不需要一开始就把所有协议细节都啃透。更重要的是先建立几个直觉：</p>
<ul>
<li>MCP 是一层工具协议</li>
<li>业务逻辑仍然是你自己写</li>
<li>宿主通过 MCP 发现并调用工具</li>
<li>Inspector 适合做调试验证</li>
<li>Codex 可以作为真实宿主消费你的工具能力</li>
</ul>
<p>只要这条链路亲手跑通过一次，后面再扩展第二个工具、替换数据源、接入更多宿主，理解都会快很多。</p>
<p>下一步可以继续做三件事：</p>
<ol>
<li>增加更多 symbol，并把映射改成可配置</li>
<li>给 CoinGecko 请求增加缓存和限流处理</li>
<li>再注册一个工具，例如 <code>compare_market_data</code>，比较多个币种的价格和涨跌幅</li>
</ol>
<p>这时你就不只是“会跑一个 MCP 示例”，而是开始真正把 MCP 当成可扩展的工具协议来使用。</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>MCP</tag>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS 沙盒路径踩坑：KMP 读取 Documents 文件为什么会报 No such file</title>
    <url>/posts/2023/08/13/bb96b1c7/</url>
    <content><![CDATA[<p>在 Kotlin Multiplatform 项目里做 iOS 文件存储时，遇到过一个很典型的问题：</p>
<p><strong>文件明明存在于 Documents 目录，但用 <code>NSData.dataWithContentsOfFile</code> 读取时始终返回空，并报 <code>No such file or directory</code>。</strong></p>
<p>这个问题看起来像是文件没写成功，最后排查下来，真正原因不是文件不存在，而是读取时使用了已经失效的 iOS 沙盒完整路径。</p>
<hr>
<h2 id="一、问题现象"><a href="#一、问题现象" class="headerlink" title="一、问题现象"></a>一、问题现象</h2><p>当时的现象比较明确：</p>
<ul>
<li>导出 <code>.xcappdata</code> 后，可以确认图片确实在 App 的 <code>Documents</code> 目录下</li>
<li>Kotlin Native 侧用 <code>NSData.dataWithContentsOfFile(fullPath, ...)</code> 读取时返回 <code>null</code></li>
<li>错误信息是 <code>No such File or Directory</code></li>
</ul>
<p>读取代码大概如下，为了排查问题，增加了 <code>errorPtr</code> 输出：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">val</span> errorPtr<span class="token operator">:</span> ObjCObjectVar<span class="token operator">&lt;</span>NSError<span class="token operator">?</span><span class="token operator">&gt;</span> <span class="token operator">=</span> <span class="token function">alloc</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
NSData<span class="token punctuation">.</span><span class="token function">dataWithContentsOfFile</span><span class="token punctuation">(</span>fullPath<span class="token punctuation">,</span> options <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> error <span class="token operator">=</span> errorPtr<span class="token punctuation">.</span>ptr<span class="token punctuation">)</span><span class="token operator">?</span><span class="token punctuation">.</span><span class="token function">let</span> <span class="token punctuation">{</span> bytes <span class="token operator">-&gt;</span>
    <span class="token keyword">val</span> array <span class="token operator">=</span> <span class="token function">ByteArray</span><span class="token punctuation">(</span>bytes<span class="token punctuation">.</span>length<span class="token punctuation">.</span><span class="token function">toInt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
    bytes<span class="token punctuation">.</span><span class="token function">getBytes</span><span class="token punctuation">(</span>array<span class="token punctuation">.</span><span class="token function">refTo</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getPointer</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">)</span><span class="token punctuation">,</span> bytes<span class="token punctuation">.</span>length<span class="token punctuation">)</span>
    <span class="token keyword">return</span><span class="token label symbol">@withContext</span> array
<span class="token punctuation">}</span>
<span class="token function">println</span><span class="token punctuation">(</span>errorPtr<span class="token punctuation">.</span>value<span class="token operator">?</span><span class="token punctuation">.</span>description<span class="token punctuation">.</span><span class="token function">orEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token keyword">return</span><span class="token label symbol">@withContext</span> <span class="token keyword">null</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p><code>fullPath</code> 看起来没问题，文件也确实存在，但运行时就是读不到。</p>
<span id="more"></span>

<hr>
<h2 id="二、定位过程：路径里的-UUID-不一致"><a href="#二、定位过程：路径里的-UUID-不一致" class="headerlink" title="二、定位过程：路径里的 UUID 不一致"></a>二、定位过程：路径里的 UUID 不一致</h2><p>第一反应是路径可能不对。</p>
<p>于是用 Swift 在运行时重新获取当前 App 的 Documents 目录，并列出目录下文件，再和 Kotlin 侧保存的 <code>fullPath</code> 对比。</p>
<p>Kotlin 中使用的完整路径类似：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">/var/mobile/Containers/Data/Application/3FC1CCE9-A788-47AB-902A-FA133FAA3D30/Documents/60F570BE-2A16-4EFB-96B3-9203C0A0ABCE.jpg<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>Swift 运行时拿到的文件 URL 是：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">file:///private/var/mobile/Containers/Data/Application/C5AC5178-6887-40F4-9EE5-8D56CB830CB3/Documents/60F570BE-2A16-4EFB-96B3-9203C0A0ABCE.jpg<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>关键差异在 <code>Application</code> 后面的 UUID：</p>
<ul>
<li>旧路径：<code>3FC1CCE9-...</code></li>
<li>当前路径：<code>C5AC5178-...</code></li>
</ul>
<p>Swift 侧验证代码如下：</p>
<pre class="line-numbers language-swift" data-language="swift"><code class="language-swift"><span class="token keyword">let</span> fileManager <span class="token operator">=</span> <span class="token class-name">FileManager</span><span class="token punctuation">.</span><span class="token keyword">default</span>
<span class="token keyword">let</span> documentsURL <span class="token operator">=</span> fileManager<span class="token punctuation">.</span><span class="token function">urls</span><span class="token punctuation">(</span><span class="token keyword">for</span><span class="token punctuation">:</span> <span class="token punctuation">.</span>documentDirectory<span class="token punctuation">,</span> <span class="token keyword">in</span><span class="token punctuation">:</span> <span class="token punctuation">.</span>userDomainMask<span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>

<span class="token keyword">do</span> <span class="token punctuation">{</span>
    <span class="token keyword">let</span> fileURLs <span class="token operator">=</span> <span class="token keyword">try</span> fileManager<span class="token punctuation">.</span><span class="token function">contentsOfDirectory</span><span class="token punctuation">(</span>
        at<span class="token punctuation">:</span> documentsURL<span class="token punctuation">,</span>
        includingPropertiesForKeys<span class="token punctuation">:</span> <span class="token nil constant">nil</span>
    <span class="token punctuation">)</span>
    <span class="token function">print</span><span class="token punctuation">(</span>fileURLs<span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">{</span>
    <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">"Error while enumerating files </span><span class="token interpolation-punctuation punctuation">\(</span><span class="token interpolation">documentsURL<span class="token punctuation">.</span>path</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">: </span><span class="token interpolation-punctuation punctuation">\(</span><span class="token interpolation">error<span class="token punctuation">.</span>localizedDescription</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">"</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>到这里，问题基本明确了：</p>
<p><strong>文件存在，但之前保存的完整路径已经不是当前运行时的真实路径。</strong></p>
<hr>
<h2 id="三、根因：不要持久化-iOS-沙盒完整路径"><a href="#三、根因：不要持久化-iOS-沙盒完整路径" class="headerlink" title="三、根因：不要持久化 iOS 沙盒完整路径"></a>三、根因：不要持久化 iOS 沙盒完整路径</h2><p>从 iOS 8 开始，App 沙盒中 <code>Application</code> 这一层目录的 UUID 可能会随着安装和部署方式变化。</p>
<p>例如：</p>
<ul>
<li>卸载后重新安装</li>
<li>通过 Xcode 重新安装</li>
<li>TestFlight / App Store 重新安装</li>
</ul>
<p>如果把完整路径持久化下来，下次启动或重新安装后，路径里的 UUID 可能已经变了。文件名还在，但旧路径不再有效。</p>
<p>所以问题不在 <code>NSData</code> 本身，而在路径策略：</p>
<p><strong>不能依赖持久化的完整路径读取文件，应该只保存文件名或相对路径，并在运行时重新解析当前 Documents 目录。</strong></p>
<hr>
<h2 id="四、解决方案"><a href="#四、解决方案" class="headerlink" title="四、解决方案"></a>四、解决方案</h2><p>核心思路只有一个：</p>
<p><strong>保存文件名，读取时重新获取当前 Documents 目录，再拼出真实路径。</strong></p>
<h3 id="方法一：遍历-Documents-后按文件名匹配"><a href="#方法一：遍历-Documents-后按文件名匹配" class="headerlink" title="方法一：遍历 Documents 后按文件名匹配"></a>方法一：遍历 Documents 后按文件名匹配</h3><p>这种方式不依赖旧完整路径，而是先拿到当前 Documents URL，再在目录中查找目标文件：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">val</span> url <span class="token operator">=</span> fileManager<span class="token punctuation">.</span><span class="token function">URLForDirectory</span><span class="token punctuation">(</span>
    directory <span class="token operator">=</span> NSDocumentDirectory<span class="token punctuation">,</span>
    inDomain <span class="token operator">=</span> NSUserDomainMask<span class="token punctuation">,</span>
    appropriateForURL <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">,</span>
    create <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">,</span>
    error <span class="token operator">=</span> <span class="token keyword">null</span>
<span class="token punctuation">)</span><span class="token operator">!!</span>

<span class="token keyword">val</span> fileUrls <span class="token operator">=</span> fileManager<span class="token punctuation">.</span><span class="token function">contentsOfDirectoryAtURL</span><span class="token punctuation">(</span>
    url <span class="token operator">=</span> url<span class="token punctuation">,</span>
    includingPropertiesForKeys <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">,</span>
    options <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span>
    error <span class="token operator">=</span> errorPtr<span class="token punctuation">.</span>ptr
<span class="token punctuation">)</span>

<span class="token keyword">val</span> currentFileUrl <span class="token operator">=</span> fileUrls<span class="token operator">?</span><span class="token punctuation">.</span><span class="token function">first</span> <span class="token punctuation">{</span> it<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">contains</span><span class="token punctuation">(</span>fileName<span class="token punctuation">)</span> <span class="token punctuation">}</span> <span class="token keyword">as</span><span class="token operator">?</span> NSURL

currentFileUrl<span class="token operator">?</span><span class="token punctuation">.</span><span class="token function">let</span> <span class="token punctuation">{</span>
    NSData<span class="token punctuation">.</span><span class="token function">dataWithContentsOfURL</span><span class="token punctuation">(</span>currentFileUrl<span class="token punctuation">)</span><span class="token operator">?</span><span class="token punctuation">.</span><span class="token function">let</span> <span class="token punctuation">{</span> bytes <span class="token operator">-&gt;</span>
        <span class="token keyword">val</span> array <span class="token operator">=</span> <span class="token function">ByteArray</span><span class="token punctuation">(</span>bytes<span class="token punctuation">.</span>length<span class="token punctuation">.</span><span class="token function">toInt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
        bytes<span class="token punctuation">.</span><span class="token function">getBytes</span><span class="token punctuation">(</span>array<span class="token punctuation">.</span><span class="token function">refTo</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getPointer</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">)</span><span class="token punctuation">,</span> bytes<span class="token punctuation">.</span>length<span class="token punctuation">)</span>
        <span class="token keyword">return</span><span class="token label symbol">@withContext</span> array
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这种方式适合调试和验证，但遍历目录会有额外开销。</p>
<h3 id="方法二：运行时获取-Documents，再拼接文件名"><a href="#方法二：运行时获取-Documents，再拼接文件名" class="headerlink" title="方法二：运行时获取 Documents，再拼接文件名"></a>方法二：运行时获取 Documents，再拼接文件名</h3><p>更推荐的方式是直接获取当前 Documents 路径，然后拼接文件名：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">val</span> documentDirectory <span class="token operator">=</span> <span class="token function">NSSearchPathForDirectoriesInDomains</span><span class="token punctuation">(</span>
    directory <span class="token operator">=</span> NSDocumentDirectory<span class="token punctuation">,</span>
    domainMask <span class="token operator">=</span> NSUserDomainMask<span class="token punctuation">,</span>
    expandTilde <span class="token operator">=</span> <span class="token boolean">true</span>
<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">first</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">as</span> NSString

<span class="token keyword">val</span> fullPath <span class="token operator">=</span> documentDirectory<span class="token punctuation">.</span><span class="token function">stringByAppendingPathComponent</span><span class="token punctuation">(</span>fileName<span class="token punctuation">)</span>

NSData<span class="token punctuation">.</span><span class="token function">dataWithContentsOfFile</span><span class="token punctuation">(</span>fullPath<span class="token punctuation">,</span> <span class="token operator">..</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这样得到的 <code>fullPath</code> 始终对应当前 App 实例的真实 Documents 路径，不受旧 UUID 影响。</p>
<hr>
<h2 id="五、排查结论"><a href="#五、排查结论" class="headerlink" title="五、排查结论"></a>五、排查结论</h2><p>这次问题可以总结成下面几点：</p>
<table>
<thead>
<tr>
<th>要点</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>不要保存完整路径</td>
<td>iOS 沙盒 <code>Application</code> 目录 UUID 可能变化，旧路径会失效</td>
</tr>
<tr>
<td>推荐保存文件名</td>
<td>持久化文件名或相对路径，读取时重新解析目录</td>
</tr>
<tr>
<td>优先使用系统 API</td>
<td>通过 <code>NSSearchPathForDirectoriesInDomains</code> 或 FileManager 获取当前目录</td>
</tr>
<tr>
<td>错误表象容易误导</td>
<td><code>No such file</code> 不一定是文件没写入，也可能是路径已经过期</td>
</tr>
</tbody></table>
<p>改造后，读取逻辑不再依赖旧路径，<code>NSData.dataWithContentsOfFile</code> 无法读取文件的问题也就解决了。</p>
<hr>
<h2 id="附录：哪些情况下-UUID-可能变化"><a href="#附录：哪些情况下-UUID-可能变化" class="headerlink" title="附录：哪些情况下 UUID 可能变化"></a>附录：哪些情况下 UUID 可能变化</h2><table>
<thead>
<tr>
<th>安装 / 运行方式</th>
<th>UUID 是否变化</th>
</tr>
</thead>
<tbody><tr>
<td>App 卸载后重新安装</td>
<td>会变化</td>
</tr>
<tr>
<td>通过 Xcode 重新安装</td>
<td>会变化</td>
</tr>
<tr>
<td>Xcode 直接运行且未卸载 App</td>
<td>通常不变</td>
</tr>
<tr>
<td>模拟器运行</td>
<td>可能变化</td>
</tr>
<tr>
<td>TestFlight / App Store 安装</td>
<td>会变化</td>
</tr>
<tr>
<td>iOS OTA 更新 App</td>
<td>通常不变</td>
</tr>
</tbody></table>
<p>所以在 iOS 文件存储里，比较稳妥的原则是：</p>
<p><strong>业务层只保存文件名或相对路径，运行时再解析真实沙盒目录。</strong></p>
]]></content>
      <categories>
        <category>Multiplatform</category>
      </categories>
      <tags>
        <tag>SwiftUI</tag>
        <tag>Kotlin Native</tag>
        <tag>iOS</tag>
      </tags>
  </entry>
  <entry>
    <title>乐歌升降桌 E07 / E08 故障排查：一边桌脚不动时，我是怎么修好的</title>
    <url>/posts/2026/03/02/fbb9923f/</url>
    <content><![CDATA[<p>家里的小故障有时候最磨人。</p>
<p>它不至于让人立刻崩溃，但足够影响日常使用。尤其是当升降桌突然一边能动、一边完全没反应，桌面变得一高一低时，工作状态也会被一起打断。</p>
<p>这次出问题的是家里的乐歌升降桌。</p>
<p>原本只是一次普通的升降操作，按下上升键后，桌子没有正常升起来，反而进入了异常状态：</p>
<ul>
<li>一边桌脚还在动</li>
<li>另一边桌脚完全没反应</li>
<li>控制面板反复显示 <strong>RST、E07、E08</strong></li>
</ul>
<p>售后当时无法马上处理，于是只能先自己排查。最后发现，问题并不是电机彻底损坏，而更像是左右桌脚高度不同步后触发了保护机制。</p>
<p>这篇文章记录完整过程，也给遇到类似情况的人一个参考。</p>
<blockquote>
<p>说明：本文只是一次个人排查记录，不是官方维修教程。涉及拆卸、插线或通电测试时，务必先断电，确认安全后再操作；如果不确定，优先联系售后。</p>
</blockquote>
<hr>
<h2 id="一、故障现象：一边能动，一边不动"><a href="#一、故障现象：一边能动，一边不动" class="headerlink" title="一、故障现象：一边能动，一边不动"></a>一、故障现象：一边能动，一边不动</h2><p>那天晚上，按下升降桌的上升按钮后，很快发现状态不对。</p>
<p>桌子的表现大概是：</p>
<ul>
<li>左边桌脚还有反应，但<strong>只能往下，不能上升</strong></li>
<li>右边桌脚无论上升还是下降都<strong>完全没反应</strong></li>
<li>控制面板反复显示 <strong>RST、E07、E08</strong></li>
</ul>
<p><img src="/images/20260302/8F340FEF-8A30-40F0-A4AC-6D303CF863A6.jpg" alt="控制面板显示 RST / E07 / E08 等报码"></p>
<p><em>图 1：控制面板显示 RST / E07 / E08 等报码</em></p>
<p>连续试了几次后，结论基本一致：</p>
<p><strong>这不是简单卡顿，而是桌子已经进入了异常保护状态。</strong></p>
<p>因为每次操作时，始终只有一边桌脚在动，另一边完全静止。桌面已经明显不平，继续强行升降反而可能让结构受力更糟。</p>
<hr>
<h2 id="二、先联系售后，按标准流程排查"><a href="#二、先联系售后，按标准流程排查" class="headerlink" title="二、先联系售后，按标准流程排查"></a>二、先联系售后，按标准流程排查</h2><p>遇到这类问题，第一步还是联系售后。</p>
<p>乐歌京东自营店客服给出的排查流程比较标准，也符合电动升降桌常见处理方式。</p>
<h3 id="1-先做系统重置"><a href="#1-先做系统重置" class="headerlink" title="1. 先做系统重置"></a>1. 先做系统重置</h3><p>客服先建议把桌子尽量降到最低，然后：</p>
<blockquote>
<p><strong>长按下降键约 7 秒，执行系统重置。</strong></p>
</blockquote>
<p>这是升降桌常见的初始化方式。很多报码不一定代表零件损坏，也可能只是系统需要重新校准。</p>
<p>不过这一步对这台桌子没有立即起作用。</p>
<h3 id="2-再交叉插线"><a href="#2-再交叉插线" class="headerlink" title="2. 再交叉插线"></a>2. 再交叉插线</h3><p>接下来，客服建议拆开桌腿挡板，把控制盒上的 <strong>M1 和 M2 电机线互换</strong>，再观察报码变化。</p>
<p><img src="/images/20260302/8BA33403-D04C-4DA5-98AC-E9D9CFDFBB9B.jpg" alt="客服建议交叉插线排查问题位置"></p>
<p><em>图 2：客服建议交叉插线排查问题位置</em></p>
<p>照着操作后，控制面板上的报码确实发生了变化：从 <strong>E07</strong> 变成了 <strong>E08</strong>。</p>
<p><img src="/images/20260302/C8D25386-26A8-422C-A7B9-B575CBFF5852.jpg" alt="交叉插线后报码从 E07 变成 E08"></p>
<p><em>图 3：交叉插线后，报码从 E07 变成 E08</em></p>
<p>客服根据这个变化，初步判断可能和控制盒有关，需要继续反馈技术部门。由于当时临近假期，后续处理需要等到年后。</p>
<p>但桌子已经拆到这一步，与其直接等待，不如继续确认几个更基础的问题。</p>
<hr>
<h2 id="三、不要只盯报码，更要看现象"><a href="#三、不要只盯报码，更要看现象" class="headerlink" title="三、不要只盯报码，更要看现象"></a>三、不要只盯报码，更要看现象</h2><p>E07 / E08 这样的错误码有参考价值，但不能完全依赖报码判断。</p>
<p>这次真正有用的线索，其实是设备表现出来的现象：</p>
<p><strong>始终只有一边桌脚在动，另一边没有正常响应。</strong></p>
<p>结合网上能查到的说明，以及同类升降桌的常见故障逻辑，E07 / E08 大致可能和两类问题有关：</p>
<ul>
<li>左右桌脚高度不同步</li>
<li>电机连接、通信或控制异常</li>
</ul>
<p>这说明问题未必是电机彻底损坏，也可能是左右不同步后触发了系统保护。</p>
<p>所以排查重点从“报码是什么意思”转成了两个更具体的问题：</p>
<ol>
<li>电机本身还能不能正常工作？</li>
<li>左右桌脚是不是已经明显不平？</li>
</ol>
<hr>
<h2 id="四、拆开检查：先确认电机有没有坏"><a href="#四、拆开检查：先确认电机有没有坏" class="headerlink" title="四、拆开检查：先确认电机有没有坏"></a>四、拆开检查：先确认电机有没有坏</h2><p>既然已经拆开挡板，下一步就是确认电机本体是否正常。</p>
<p>操作前先断电，确认连接位置，再分别测试两侧电机。整个过程大致是：</p>
<ol>
<li>拧开桌腿挡板螺丝</li>
<li>断开两侧电机连接线</li>
<li>分别测试每个电机是否能正常上下动作</li>
</ol>
<p>测试结果比较关键：</p>
<p><strong>两个电机都能正常上下运动。</strong></p>
<p>这意味着问题大概率不在“电机彻底坏了”这一层。至少最麻烦的一种情况可以先排除。</p>
<p>回头再看整个过程，问题开始变得清楚：</p>
<ul>
<li>出问题前，已经有一段时间只有一边桌脚在动</li>
<li>另一边没有正常响应</li>
<li>两边高度差被逐渐拉大</li>
<li>高度差过大后，系统可能触发保护，阻止继续运行</li>
</ul>
<p>也就是说，桌子不是完全坏了，而是可能已经处在一个“不适合继续升降”的状态。</p>
<p>下一步就不再是继续反复按按钮，而是先把左右桌脚重新找平。</p>
<hr>
<h2 id="五、手动找平后，再重新初始化"><a href="#五、手动找平后，再重新初始化" class="headerlink" title="五、手动找平后，再重新初始化"></a>五、手动找平后，再重新初始化</h2><p>处理思路变成了：</p>
<p><strong>先让左右桌脚回到同一个物理基准，再让系统重新校准。</strong></p>
<p>实际操作如下：</p>
<ol>
<li>利用还能动作的那一边，把两边桌脚尽量调整到最低位置</li>
<li>确保左右桌脚在肉眼可见范围内回到同一高度</li>
<li>把两个电机重新接回控制盒</li>
<li>再次长按下降键约 7 秒，执行系统重置</li>
</ol>
<p>这一次，结果明显不同：</p>
<ul>
<li>面板上的错误代码消失</li>
<li>显示重新变回正常高度数字</li>
<li>再测试升降时，左右两边恢复同步运动</li>
</ul>
<p><img src="/images/20260302/379220D3-EF81-44D3-AFD3-1A9DA10B1F23.jpg" alt="问题解决后反馈客服"></p>
<p><em>图 4：问题解决后，回头告知客服</em></p>
<p>最终，这次故障没有走到更换控制盒或更换电机的步骤，而是通过“找平 + 重置”恢复了。</p>
<hr>
<h2 id="六、这次排查的关键步骤"><a href="#六、这次排查的关键步骤" class="headerlink" title="六、这次排查的关键步骤"></a>六、这次排查的关键步骤</h2><p>整个过程可以整理成下面这张表：</p>
<table>
<thead>
<tr>
<th>排查步骤</th>
<th>关键操作</th>
<th>目的</th>
</tr>
</thead>
<tbody><tr>
<td>1. 记录报码</td>
<td>观察 RST / E07 / E08 的变化</td>
<td>缩小问题范围</td>
</tr>
<tr>
<td>2. 联系客服</td>
<td>按官方建议先重置，再交叉插线</td>
<td>建立基础排查路径</td>
</tr>
<tr>
<td>3. 测试电机</td>
<td>分别确认两侧电机能否动作</td>
<td>排除电机本体故障</td>
</tr>
<tr>
<td>4. 手动找平</td>
<td>让左右桌脚回到同一高度</td>
<td>恢复物理基准</td>
</tr>
<tr>
<td>5. 系统重置</td>
<td>长按下降键重新初始化</td>
<td>让控制系统重新校准</td>
</tr>
</tbody></table>
<p>真正起作用的不是某个“神奇步骤”，而是排查顺序：</p>
<ul>
<li>先确认系统是否能重置</li>
<li>再确认连接和报码变化</li>
<li>再排除电机本体故障</li>
<li>最后处理左右桌脚不同步</li>
</ul>
<p>当问题被拆成几个小问题后，排查难度就降低了很多。</p>
<hr>
<h2 id="七、遇到类似情况，可以先检查什么"><a href="#七、遇到类似情况，可以先检查什么" class="headerlink" title="七、遇到类似情况，可以先检查什么"></a>七、遇到类似情况，可以先检查什么</h2><p>如果你的升降桌也出现 <strong>报码 + 一边桌脚不动 + 重置无效</strong>，可以先按低风险顺序检查：</p>
<ol>
<li><p><strong>先停止强行升降</strong><br>如果桌面已经明显不平，继续操作可能让受力更糟。</p>
</li>
<li><p><strong>记录报码和现象</strong><br>记录是 E07、E08、RST，还是其他错误，同时观察哪一边能动、哪一边不动。</p>
</li>
<li><p><strong>先按说明书或客服建议重置</strong><br>很多升降桌都有长按下降键进行初始化的流程。</p>
</li>
<li><p><strong>检查电机线是否松动</strong><br>操作前务必断电，不要在不确定的情况下带电插拔。</p>
</li>
<li><p><strong>观察两边桌脚是否明显不平</strong><br>如果高度差已经很明显，单纯重置可能不会立即恢复，需要先让两边回到接近一致的高度。</p>
</li>
<li><p><strong>不确定时停止操作，联系售后</strong><br>如果出现异响、卡死、线材破损、控制盒异常发热等情况，不建议继续自行排查。</p>
</li>
</ol>
<p>这类排查的重点不是“自己替代维修”，而是在安全前提下，把简单问题先确认清楚。</p>
<hr>
<h2 id="八、这次故障带来的几个提醒"><a href="#八、这次故障带来的几个提醒" class="headerlink" title="八、这次故障带来的几个提醒"></a>八、这次故障带来的几个提醒</h2><h3 id="1-设备停止运行，不一定代表彻底损坏"><a href="#1-设备停止运行，不一定代表彻底损坏" class="headerlink" title="1. 设备停止运行，不一定代表彻底损坏"></a>1. 设备停止运行，不一定代表彻底损坏</h3><p>报码看起来很吓人，但它很多时候只是系统在提示：</p>
<p><strong>当前状态不正常，需要先停止。</strong></p>
<p>停止运行可能是保护机制，而不是硬件已经报废。</p>
<h3 id="2-现象往往比报码更重要"><a href="#2-现象往往比报码更重要" class="headerlink" title="2. 现象往往比报码更重要"></a>2. 现象往往比报码更重要</h3><p>如果只盯着 E07 / E08，排查方向可能会被错误码带着走。</p>
<p>但“只有一边桌脚在动”这个现象，直接指向了左右不同步的可能性。</p>
<h3 id="3-客服流程仍然有价值"><a href="#3-客服流程仍然有价值" class="headerlink" title="3. 客服流程仍然有价值"></a>3. 客服流程仍然有价值</h3><p>这次虽然最后是自己处理好的，但客服给出的重置和交叉插线步骤提供了排查起点。</p>
<p>标准流程的价值在于：先把常见问题按顺序排掉。</p>
<h3 id="4-低风险问题可以先自己排查，但要有边界"><a href="#4-低风险问题可以先自己排查，但要有边界" class="headerlink" title="4. 低风险问题可以先自己排查，但要有边界"></a>4. 低风险问题可以先自己排查，但要有边界</h3><p>重置、观察报码、检查插线、确认是否明显不平，这些属于相对低风险的检查。</p>
<p>但涉及电路、异常发热、结构卡死、零件更换时，还是应该交给售后或专业维修。</p>
<hr>
<h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>这次升降桌故障，最后没有变成一次复杂维修。</p>
<p>它更像是一个生活里的小插曲：一开始看起来很麻烦，但只要愿意停下来观察现象、拆开问题、按顺序排查，就会发现它并没有想象中那么不可处理。</p>
<p>对这次情况来说，最关键的判断是：</p>
<p><strong>问题不一定出在电机损坏，而可能是左右桌脚不同步后触发了保护。</strong></p>
<p>这篇记录不一定适用于所有型号，也不能替代官方维修建议。但如果你遇到的是类似场景：</p>
<ul>
<li>控制面板显示 RST / E07 / E08</li>
<li>一边桌脚不动</li>
<li>重置暂时无效</li>
<li>桌面已经明显不平</li>
</ul>
<p>那么可以先从“记录报码、确认现象、检查连接、手动找平、重新初始化”这条路径排查，或许能少一点慌张，也少走一点弯路。</p>
<hr>
<p><em>本文记录于 2026 年 3 月</em></p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔</tag>
        <tag>升降桌</tag>
      </tags>
  </entry>
  <entry>
    <title>Jetpack Compose 性能优化：为什么 graphicsLayer 值得重点掌握</title>
    <url>/posts/2026/02/27/4ae1fc92/</url>
    <content><![CDATA[<p>在 Jetpack Compose 里，同一个视觉效果往往有不止一种写法。</p>
<p>比如，想让一个组件半透明，你可以这样写：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">Modifier<span class="token punctuation">.</span><span class="token function">alpha</span><span class="token punctuation">(</span><span class="token number">0.5f</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>也可以这样写：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">Modifier<span class="token punctuation">.</span><span class="token function">graphicsLayer</span> <span class="token punctuation">{</span> alpha <span class="token operator">=</span> <span class="token number">0.5f</span> <span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>从界面效果来看，它们几乎一样。</p>
<p>但在<strong>动画</strong>、<strong>滚动联动</strong>、<strong>高频状态变化</strong>这类场景里，两者背后的性能路径并不完全相同。</p>
<p>真正的关键不是“哪个 API 更高级”，而是：</p>
<blockquote>
<p><strong>你在哪个阶段读取状态。</strong></p>
</blockquote>
<p>如果状态读取发生在 Composition 阶段，就可能触发更多重组工作。</p>
<p>如果状态读取被延后到 Draw 阶段，Compose 就有机会跳过 Composition 和 Layout，只做更轻量的绘制更新。</p>
<hr>
<h2 id="Compose-UI-的三大阶段"><a href="#Compose-UI-的三大阶段" class="headerlink" title="Compose UI 的三大阶段"></a>Compose UI 的三大阶段</h2><p>理解这件事，先要知道 Compose 的渲染流程主要分为三个阶段：</p>
<h3 id="1-Composition"><a href="#1-Composition" class="headerlink" title="1. Composition"></a>1. Composition</h3><p>决定“<strong>要显示什么</strong>”。<br>也就是执行 Composable，构建或更新 UI 树。</p>
<h3 id="2-Layout"><a href="#2-Layout" class="headerlink" title="2. Layout"></a>2. Layout</h3><p>决定“<strong>显示在哪里、多大</strong>”。<br>也就是测量和摆放组件。</p>
<h3 id="3-Draw"><a href="#3-Draw" class="headerlink" title="3. Draw"></a>3. Draw</h3><p>决定“<strong>最终画成什么样</strong>”。<br>包括透明度、缩放、旋转、裁剪等绘制相关操作。</p>
<p>如果一个变化只影响 Draw 阶段，通常会比影响 Layout 阶段更高效，因为整体需要做的工作更少。</p>
<hr>
<h2 id="真正的性能差异，来自“状态读取时机”"><a href="#真正的性能差异，来自“状态读取时机”" class="headerlink" title="真正的性能差异，来自“状态读取时机”"></a>真正的性能差异，来自“状态读取时机”</h2><p>很多文章会把这件事简单总结成：</p>
<ul>
<li><code>.alpha()</code> 会触发重组</li>
<li><code>.graphicsLayer {}</code> 不会触发重组</li>
</ul>
<p>这个说法<strong>不够准确</strong>。</p>
<p>更准确的说法是：</p>
<blockquote>
<p><strong>是否发生重组，取决于状态是在 Composition、Layout 还是 Draw 阶段被读取。</strong></p>
</blockquote>
<p><code>graphicsLayer</code> 的优势并不只是“它能做透明度和缩放”，而是它提供了一个很适合承载<strong>绘制阶段状态读取</strong>的入口。</p>
<p>也就是说，在适合的场景下，你可以把频繁变化的状态放到更靠后的阶段去处理，从而减少不必要的 Composition 和 Layout 开销。</p>
<hr>
<h2 id="为什么-graphicsLayer-常常更适合动画"><a href="#为什么-graphicsLayer-常常更适合动画" class="headerlink" title="为什么 graphicsLayer 常常更适合动画"></a>为什么 <code>graphicsLayer</code> 常常更适合动画</h2><p><code>Modifier.graphicsLayer</code> 主要影响的是绘制阶段，不会改变组件的测量尺寸和摆放位置。</p>
<p>这意味着下面这些变化，通常更适合放进 <code>graphicsLayer</code>：</p>
<ul>
<li><code>alpha</code></li>
<li><code>scaleX / scaleY</code></li>
<li><code>translationX / translationY</code></li>
<li><code>rotationZ</code></li>
</ul>
<p>对于高频更新的 UI，这通常更有利，比如：</p>
<ul>
<li>滚动时的视差效果</li>
<li>列表项渐隐渐现</li>
<li>卡片缩放</li>
<li>图标呼吸动画</li>
<li>拖拽过程中的位移或旋转</li>
</ul>
<p>因为这些效果本质上更多是在改变“怎么画”，而不是“怎么排版”。</p>
<hr>
<h2 id="graphicsLayer-还有一层意义：图形层隔离"><a href="#graphicsLayer-还有一层意义：图形层隔离" class="headerlink" title="graphicsLayer 还有一层意义：图形层隔离"></a><code>graphicsLayer</code> 还有一层意义：图形层隔离</h2><p><code>graphicsLayer</code> 不只是一个 modifier，它还会带来图形层的隔离效果。</p>
<p>你可以把它理解成：</p>
<ul>
<li>组件内容先被组织成一个独立绘制层</li>
<li>后续对这个层做透明度、缩放、旋转等操作</li>
<li>不必每次都重新“重画里面的内容”</li>
</ul>
<p>这也是它特别适合动画场景的原因之一。</p>
<p>不过要注意，<strong>有 layer 不代表一定更快</strong>。</p>
<p>如果只是静态 UI，或者变化频率很低，没必要为了“性能优化”而到处套 <code>graphicsLayer</code>。真正值得优化的，是那些：</p>
<ul>
<li>高频变化</li>
<li>容易掉帧的动画</li>
<li>滚动联动效果</li>
<li>明显存在性能瓶颈的界面</li>
</ul>
<hr>
<h2 id="一个更准确的对比方式"><a href="#一个更准确的对比方式" class="headerlink" title="一个更准确的对比方式"></a>一个更准确的对比方式</h2><table>
<thead>
<tr>
<th align="left">对比项</th>
<th align="left">普通值传递写法</th>
<th align="left"><code>graphicsLayer { ... }</code> / lambda-based 写法</th>
</tr>
</thead>
<tbody><tr>
<td align="left">状态读取位置</td>
<td align="left">常见于 Composition 阶段</td>
<td align="left">可延后到 Draw 阶段</td>
</tr>
<tr>
<td align="left">是否一定触发重组</td>
<td align="left">不一定，但更容易发生</td>
<td align="left">不一定完全没有，但更容易跳过 Composition</td>
</tr>
<tr>
<td align="left">是否影响布局</td>
<td align="left">取决于具体 modifier</td>
<td align="left"><code>graphicsLayer</code> 本身只影响绘制</td>
</tr>
<tr>
<td align="left">适合场景</td>
<td align="left">静态 UI、低频变化</td>
<td align="left">动画、滚动联动、高频视觉变化</td>
</tr>
</tbody></table>
<p>这里最重要的是一句话：</p>
<blockquote>
<p><strong>不是 <code>graphicsLayer</code> 天生“无敌”，而是它更适合处理那些只需要改变视觉表现、不需要重新布局的更新。</strong></p>
</blockquote>
<hr>
<h2 id="实战示例：把动画状态留到绘制阶段处理"><a href="#实战示例：把动画状态留到绘制阶段处理" class="headerlink" title="实战示例：把动画状态留到绘制阶段处理"></a>实战示例：把动画状态留到绘制阶段处理</h2><p>下面这个例子非常适合这个思路：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">val</span> animatedScale <span class="token keyword">by</span> <span class="token function">animateFloatAsState</span><span class="token punctuation">(</span>targetValue <span class="token operator">=</span> <span class="token number">1.2f</span><span class="token punctuation">)</span>

<span class="token function">Box</span><span class="token punctuation">(</span>
    Modifier
        <span class="token punctuation">.</span><span class="token function">size</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">.</span>dp<span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">background</span><span class="token punctuation">(</span>Color<span class="token punctuation">.</span>Magenta<span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">graphicsLayer</span> <span class="token punctuation">{</span>
            scaleX <span class="token operator">=</span> animatedScale
            scaleY <span class="token operator">=</span> animatedScale
            alpha <span class="token operator">=</span> <span class="token number">0.9f</span>
        <span class="token punctuation">}</span>
<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这种写法的好处是，<code>scaleX</code> 和 <code>scaleY</code> 这类纯视觉变化，更适合放在 <code>graphicsLayer</code> 中处理。因为它们不需要改变组件本身的测量结果和摆放位置。</p>
<p>如果你的动画只是让组件“看起来变大”，而不是让布局系统认为“它真的变大了”，这种方式通常更划算。</p>
<hr>
<h2 id="什么时候优先考虑-graphicsLayer"><a href="#什么时候优先考虑-graphicsLayer" class="headerlink" title="什么时候优先考虑 graphicsLayer"></a>什么时候优先考虑 <code>graphicsLayer</code></h2><p>你可以优先在下面这些场景考虑它：</p>
<h3 id="1-高频动画"><a href="#1-高频动画" class="headerlink" title="1. 高频动画"></a>1. 高频动画</h3><p>例如透明度、缩放、旋转、位移持续变化。<br>这类变化通常只影响视觉呈现，不需要反复参与布局。</p>
<h3 id="2-滚动驱动效果"><a href="#2-滚动驱动效果" class="headerlink" title="2. 滚动驱动效果"></a>2. 滚动驱动效果</h3><p>比如顶部标题渐隐、列表项视差、卡片缩放。<br>这类状态更新非常频繁，更适合尽量延后到 Draw 阶段处理。</p>
<h3 id="3-不想因为视觉变化触发布局连锁反应"><a href="#3-不想因为视觉变化触发布局连锁反应" class="headerlink" title="3. 不想因为视觉变化触发布局连锁反应"></a>3. 不想因为视觉变化触发布局连锁反应</h3><p>如果变化不会影响组件大小和位置，就尽量别让它进入 Layout。</p>
<hr>
<h2 id="什么时候不要滥用"><a href="#什么时候不要滥用" class="headerlink" title="什么时候不要滥用"></a>什么时候不要滥用</h2><p><code>graphicsLayer</code> 不是“看到动画就全上”的银弹。</p>
<p>下面这些情况要谨慎：</p>
<h3 id="1-变化本身会影响布局"><a href="#1-变化本身会影响布局" class="headerlink" title="1. 变化本身会影响布局"></a>1. 变化本身会影响布局</h3><p>如果你需要的不是“看起来变大”，而是“真的占更多空间”，那就不能只靠 Draw 阶段处理。</p>
<h3 id="2-页面本身没有性能问题"><a href="#2-页面本身没有性能问题" class="headerlink" title="2. 页面本身没有性能问题"></a>2. 页面本身没有性能问题</h3><p>优化应该服务于实际瓶颈。<br>如果页面没有掉帧、没有频繁重组、没有滚动抖动，过度使用 <code>graphicsLayer</code> 反而会增加理解成本。</p>
<h3 id="3-为了“听起来高级”而替换所有-modifier"><a href="#3-为了“听起来高级”而替换所有-modifier" class="headerlink" title="3. 为了“听起来高级”而替换所有 modifier"></a>3. 为了“听起来高级”而替换所有 modifier</h3><p>性能优化不是 API 崇拜。<br>重点是识别哪些状态变化只是视觉层面的，哪些真的会影响布局和结构。</p>
<hr>
<h2 id="一句话总结"><a href="#一句话总结" class="headerlink" title="一句话总结"></a>一句话总结</h2><p><code>graphicsLayer</code> 的核心价值，不是“它比 <code>.alpha()</code> 更酷”，而是：</p>
<blockquote>
<p><strong>它很适合承载那些只影响绘制结果的高频状态变化，让 Compose 有机会跳过更重的 Composition 和 Layout。</strong></p>
</blockquote>
<p>所以，当你在做这些效果时：</p>
<ul>
<li>渐隐渐现</li>
<li>缩放</li>
<li>旋转</li>
<li>平移</li>
<li>滚动联动动画</li>
</ul>
<p>优先想一件事：</p>
<p><strong>这次变化，是不是其实只需要改 Draw，而不必让整个 UI 重新参与组合和布局？</strong></p>
<p>如果答案是“对”，那 <code>graphicsLayer</code> 往往就是非常值得尝试的方案。</p>
<hr>
<h2 id="结尾"><a href="#结尾" class="headerlink" title="结尾"></a>结尾</h2><p>写 Compose 性能优化时，最容易踩的坑就是把结论说得太绝对。<br>真正值得记住的不是“某个 modifier 一定更快”，而是这条原则：</p>
<blockquote>
<p><strong>把状态读取尽量放到更晚的阶段，尤其是 Draw 阶段。</strong></p>
</blockquote>
<p>这才是很多 Compose 性能优化技巧背后的共同思路。</p>
]]></content>
      <categories>
        <category>Compose</category>
      </categories>
      <tags>
        <tag>Jetpack Compose</tag>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>Jetpack Compose 中 remember 和 rememberSaveable 的区别：一次 Tab 切换状态丢失排查</title>
    <url>/posts/2023/12/04/f68195/</url>
    <content><![CDATA[<p>在 Jetpack Compose 中，<code>remember {}</code> 和 <code>rememberSaveable {}</code> 都能“记住”状态。</p>
<p>但它们的保存范围并不一样。</p>
<p>这个差异在简单页面里不明显，一旦遇到 Tab 切换、页面离开后重新进入、Activity 重建等场景，就很容易出现状态被意外重置的问题。</p>
<p>这篇文章结合一次真实排查，说明：</p>
<p><strong><code>remember</code> 只保证当前组合生命周期内的状态复用；<code>rememberSaveable</code> 适合需要在离开页面后仍然恢复的可保存状态。</strong></p>
<hr>
<h2 id="一、问题现象"><a href="#一、问题现象" class="headerlink" title="一、问题现象"></a>一、问题现象</h2><p>页面里有两个主要部分：</p>
<ul>
<li><strong>Dashboard</strong>：根据滚动做缩放动画</li>
<li><strong>Asset List</strong>：可滚动列表</li>
</ul>
<p>最开始使用 <code>remember {}</code> 保存 <code>scale</code> 状态时，表现并不稳定：</p>
<ul>
<li>快速切换 Tab：看起来正常，<code>scale</code> 还能保持</li>
<li>在其他 Tab 停留一段时间再切回：<code>scale</code> 被重置，Dashboard 突然放大</li>
</ul>
<p><img src="/images/20231204/remember.gif" alt="Tab 切换后 scale 状态变化" title="remember"></p>
<p>改成 <code>rememberSaveable {}</code> 后，无论如何切换 Tab，返回时 Dashboard 都能保持之前的缩放状态。</p>
<p>相关代码如下：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">var</span> scale <span class="token keyword">by</span> rememberSaveable <span class="token punctuation">{</span>
    <span class="token function">mutableFloatStateOf</span><span class="token punctuation">(</span><span class="token number">1.0f</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>

<span class="token keyword">val</span> nestedScrollConnection <span class="token operator">=</span> remember <span class="token punctuation">{</span> <span class="token operator">..</span><span class="token punctuation">.</span> <span class="token punctuation">}</span>

<span class="token function">Box</span><span class="token punctuation">(</span>modifier <span class="token operator">=</span> modifier<span class="token punctuation">.</span><span class="token function">nestedScroll</span><span class="token punctuation">(</span>nestedScrollConnection<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">DashboardView</span><span class="token punctuation">(</span>
        modifier <span class="token operator">=</span> Modifier
            <span class="token punctuation">.</span><span class="token function">fillMaxWidth</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">scale</span><span class="token punctuation">(</span>scale<span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">graphicsLayer</span> <span class="token punctuation">{</span>
                translationY <span class="token operator">=</span> toolbarHeightRange<span class="token punctuation">.</span>last <span class="token operator">*</span> <span class="token punctuation">(</span>scale <span class="token operator">-</span> <span class="token number">1.0f</span><span class="token punctuation">)</span>
            <span class="token punctuation">}</span><span class="token punctuation">,</span>
        onEvent <span class="token operator">=</span> onEvent
    <span class="token punctuation">)</span>

    <span class="token function">LazyColumn</span><span class="token punctuation">(</span>
        modifier <span class="token operator">=</span> Modifier
            <span class="token punctuation">.</span><span class="token function">fillMaxSize</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">graphicsLayer</span> <span class="token punctuation">{</span>
                translationY <span class="token operator">=</span> toolbarState<span class="token punctuation">.</span>height <span class="token operator">+</span> toolbarState<span class="token punctuation">.</span>offset
            <span class="token punctuation">}</span>
            <span class="token punctuation">.</span><span class="token function">background</span><span class="token punctuation">(</span>MaterialTheme<span class="token punctuation">.</span>colorScheme<span class="token punctuation">.</span>background<span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">shadow</span><span class="token punctuation">(</span>
                elevation <span class="token operator">=</span> <span class="token number">3</span><span class="token punctuation">.</span>dp<span class="token punctuation">,</span>
                shape <span class="token operator">=</span> <span class="token function">RoundedCornerShape</span><span class="token punctuation">(</span>topEnd <span class="token operator">=</span> <span class="token number">20</span><span class="token punctuation">.</span>dp<span class="token punctuation">,</span> topStart <span class="token operator">=</span> <span class="token number">20</span><span class="token punctuation">.</span>dp<span class="token punctuation">)</span>
            <span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">pointerInput</span><span class="token punctuation">(</span>Unit<span class="token punctuation">)</span> <span class="token punctuation">{</span>
                <span class="token function">detectTapGestures</span><span class="token punctuation">(</span>onPress <span class="token operator">=</span> <span class="token punctuation">{</span> scope<span class="token punctuation">.</span>coroutineContext<span class="token punctuation">.</span><span class="token function">cancelChildren</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
            <span class="token punctuation">}</span><span class="token punctuation">,</span>
        verticalArrangement <span class="token operator">=</span> Arrangement<span class="token punctuation">.</span><span class="token function">spacedBy</span><span class="token punctuation">(</span><span class="token number">12</span><span class="token punctuation">.</span>dp<span class="token punctuation">)</span><span class="token punctuation">,</span>
        state <span class="token operator">=</span> listState
    <span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token function">items</span><span class="token punctuation">(</span>uiState<span class="token punctuation">.</span>tokens<span class="token punctuation">,</span> key <span class="token operator">=</span> <span class="token punctuation">{</span> it<span class="token punctuation">.</span>token<span class="token punctuation">.</span>id <span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
            <span class="token function">TokenItemView</span><span class="token punctuation">(</span>extraToken <span class="token operator">=</span> it<span class="token punctuation">,</span> onEvent <span class="token operator">=</span> onEvent<span class="token punctuation">)</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<hr>
<h2 id="二、先理解组合生命周期"><a href="#二、先理解组合生命周期" class="headerlink" title="二、先理解组合生命周期"></a>二、先理解组合生命周期</h2><p>根据 Compose 官方文档，Composable 的生命周期可以简单分为三段：</p>
<ul>
<li><strong>Enter Composition</strong>：进入组合</li>
<li><strong>Recomposition</strong>：重组</li>
<li><strong>Exit Composition</strong>：退出组合</li>
</ul>
<p>当页面因为 Tab 切换、导航或列表滑出等原因离开当前 UI 树时，对应 Composable 可能会退出组合。</p>
<p>再次回到页面时，它不是简单“继续刚才的状态”，而是重新进入组合。</p>
<p>这正是 <code>remember</code> 和 <code>rememberSaveable</code> 差异出现的地方。</p>
<hr>
<h2 id="三、remember-保存的是什么"><a href="#三、remember-保存的是什么" class="headerlink" title="三、remember {} 保存的是什么"></a>三、<code>remember {}</code> 保存的是什么</h2><p><code>remember {}</code> 的核心行为是：</p>
<p><strong>在当前组合中记住计算结果，重组时复用这个值。</strong></p>
<p>也就是说：</p>
<ul>
<li>初次进入组合：执行 block，计算并保存值</li>
<li>普通重组：直接返回保存的值，不重新执行 block</li>
<li>退出组合后再次进入：进入新的组合生命周期，block 会重新执行</li>
</ul>
<p>所以在这次问题中，如果页面离开 Tab 后被移出组合，再回来时：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">remember <span class="token punctuation">{</span> <span class="token function">mutableFloatStateOf</span><span class="token punctuation">(</span><span class="token number">1.0f</span><span class="token punctuation">)</span> <span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>会重新执行，<code>scale</code> 自然回到 <code>1.0f</code>。</p>
<p>快速切换时之所以“看起来正常”，只是因为组合可能还没有被回收，并不代表 <code>remember</code> 能跨页面稳定保存状态。</p>
<hr>
<h2 id="四、rememberSaveable-多做了什么"><a href="#四、rememberSaveable-多做了什么" class="headerlink" title="四、rememberSaveable {} 多做了什么"></a>四、<code>rememberSaveable {}</code> 多做了什么</h2><p><code>rememberSaveable {}</code> 和 <code>remember {}</code> 类似，也会在组合中保存状态。</p>
<p>不同的是，它会把可保存的值接入 Compose 的保存机制，基于 <code>SaveableStateRegistry</code> / Android <code>SavedInstanceState</code> 进行恢复。</p>
<p>它的要点是：</p>
<ul>
<li>默认使用 <code>autoSaver()</code> 保存 Bundle 支持的类型</li>
<li>支持 <code>Int</code>、<code>String</code>、<code>Float</code> 等基础类型</li>
<li>自定义类型需要提供 <code>Saver</code></li>
<li>可以应对 Activity 重建、进程恢复，以及部分离开后重新进入的状态恢复场景</li>
</ul>
<p>如果保存的是不支持自动保存的类型，通常会遇到类似错误：</p>
<pre class="line-numbers language-text" data-language="text"><code class="language-text">java.lang.IllegalArgumentException: MutableState containing ... cannot be saved using the current SaveableStateRegistry.
The default implementation only supports types which can be stored inside the Bundle.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre>

<p>在本文这个场景里，<code>scale</code> 是 <code>Float</code>，属于可保存类型，因此适合用 <code>rememberSaveable</code> 保存。</p>
<hr>
<h2 id="五、用日志验证"><a href="#五、用日志验证" class="headerlink" title="五、用日志验证"></a>五、用日志验证</h2><p>可以用下面这段代码验证 <code>remember</code> 在退出组合后会重新计算：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">var</span> scale <span class="token keyword">by</span> remember <span class="token punctuation">{</span>
    <span class="token function">println</span><span class="token punctuation">(</span><span class="token string-literal singleline"><span class="token string">"=== re-calculate"</span></span><span class="token punctuation">)</span>
    <span class="token function">mutableFloatStateOf</span><span class="token punctuation">(</span><span class="token number">1.0f</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>

<span class="token function">DisposableEffect</span><span class="token punctuation">(</span>key1 <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    onDispose <span class="token punctuation">{</span> <span class="token function">println</span><span class="token punctuation">(</span><span class="token string-literal singleline"><span class="token string">"=== onDispose"</span></span><span class="token punctuation">)</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p><code>DisposableEffect</code> 的 <code>onDispose</code> 会在退出组合时执行。</p>
<p>使用 <code>remember {}</code> 时：</p>
<ul>
<li>第一次进入页面：打印 <code>re-calculate</code></li>
<li>切到其他 Tab 停留后：打印 <code>onDispose</code></li>
<li>再切回来：再次打印 <code>re-calculate</code>，状态被重置</li>
</ul>
<p><img src="/images/20231204/remember_log.jpg" alt="remember 退出组合后重新计算" title="remember_log"></p>
<p>改用 <code>rememberSaveable {}</code> 后：</p>
<ul>
<li>第一次进入页面时打印 <code>re-calculate</code></li>
<li>再切 Tab 回来时不会重新计算</li>
<li><code>scale</code> 会从保存状态中恢复</li>
</ul>
<p><img src="/images/20231204/remember_log1.jpg" alt="rememberSaveable 恢复已保存状态" title="remember_log1"></p>
<hr>
<h2 id="六、离开屏幕不等于-Activity-重建"><a href="#六、离开屏幕不等于-Activity-重建" class="headerlink" title="六、离开屏幕不等于 Activity 重建"></a>六、离开屏幕不等于 Activity 重建</h2><p>这里还有一个容易混淆的点：</p>
<p><strong>Composable leaving the screen is not recreation.</strong></p>
<p>可组合项离开屏幕，不等于 Activity 或进程重建。</p>
<p>在 Compose 中：</p>
<ul>
<li>离开屏幕：Composable 退出组合，<code>onDispose</code> 执行</li>
<li>再进入屏幕：Composable 重新进入组合</li>
<li>Activity 重建：触发 Android 保存 / 恢复流程</li>
</ul>
<p>常见“离开屏幕”的场景包括：</p>
<ol>
<li>Navigation 跳转或 Tab 切换</li>
<li>LazyColumn item 滑出视口</li>
<li>条件渲染导致某段 UI 暂时不显示</li>
</ol>
<p>因此，状态应该放在哪里，不能只看“会不会重组”，还要看它是否需要跨越组合生命周期。</p>
<hr>
<h2 id="七、如何选择"><a href="#七、如何选择" class="headerlink" title="七、如何选择"></a>七、如何选择</h2><p>可以按下面的规则判断：</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>推荐</th>
</tr>
</thead>
<tbody><tr>
<td>只需要在普通重组中保持状态</td>
<td><code>remember {}</code></td>
</tr>
<tr>
<td>页面离开后再进入仍要恢复状态</td>
<td><code>rememberSaveable {}</code> 或上层状态持有者</td>
</tr>
<tr>
<td>Activity / 进程重建后要恢复状态</td>
<td><code>rememberSaveable {}</code>，必要时自定义 <code>Saver</code></td>
</tr>
<tr>
<td>状态属于业务数据或跨多个页面共享</td>
<td>ViewModel / Repository / 状态管理层</td>
</tr>
<tr>
<td>LazyList 滚动位置</td>
<td><code>rememberLazyListState</code> 等可保存状态 API</td>
</tr>
</tbody></table>
<p>对本文的 <code>scale</code> 来说，它是 UI 状态，类型简单，而且需要在离开页面后恢复，所以 <code>rememberSaveable</code> 是合理选择。</p>
<hr>
<h2 id="八、总结"><a href="#八、总结" class="headerlink" title="八、总结"></a>八、总结</h2><p><code>remember</code> 和 <code>rememberSaveable</code> 的区别，可以用一句话概括：</p>
<blockquote>
<p><strong><code>remember</code> 记住当前组合里的值，<code>rememberSaveable</code> 记住可以被保存和恢复的状态。</strong></p>
</blockquote>
<p>排查这类问题时，重点不要只看“有没有重组”，而要继续追问：</p>
<ul>
<li>这个 Composable 是否会退出组合？</li>
<li>状态是否需要在再次进入页面时恢复？</li>
<li>这个状态能不能被保存到 Bundle？</li>
<li>它应该属于 UI 局部状态，还是应该上移到 ViewModel？</li>
</ul>
<p>理解这几个问题后，就能更稳定地选择 <code>remember</code>、<code>rememberSaveable</code> 或更上层的状态持有方式，避免页面切换后状态突然丢失。</p>
]]></content>
      <categories>
        <category>Technical</category>
      </categories>
      <tags>
        <tag>Jetpack Compose</tag>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解 Jetpack Compose key：为什么动态列表需要稳定身份</title>
    <url>/posts/2026/02/28/ec0aeab/</url>
    <content><![CDATA[<p>在 Jetpack Compose 里，<code>key()</code> 是一个很重要、但又很容易被轻视的 API。</p>
<p>很多人只记得一句话：</p>
<blockquote>
<p>列表里最好加 key</p>
</blockquote>
<p>但如果继续追问，往往就说不清了：</p>
<ul>
<li><code>key()</code> 到底解决了什么问题？</li>
<li>不加会发生什么？</li>
<li>它和重组有什么关系？</li>
<li>它到底算不算性能优化？</li>
<li>它和 <code>LazyColumn</code> 的 <code>key</code>、<code>remember(key)</code> 又有什么区别？</li>
</ul>
<p>这篇文章想把这件事讲透。核心结论先放前面：</p>
<blockquote>
<p><strong><code>key()</code> 的本质，不是“加速渲染”，而是给一段组合内容一个稳定身份。</strong></p>
</blockquote>
<p>当列表发生插入、删除、重排时，这个身份能帮助 Compose 把“当前这段 UI”正确对应到“之前那段 UI”，从而避免状态错位，并减少不必要的重建。</p>
<hr>
<h2 id="一、Compose-默认如何判断“这是不是同一个节点”？"><a href="#一、Compose-默认如何判断“这是不是同一个节点”？" class="headerlink" title="一、Compose 默认如何判断“这是不是同一个节点”？"></a>一、Compose 默认如何判断“这是不是同一个节点”？</h2><p>Compose 在重组时，需要判断一段 Composable 内容和上一次相比是不是“同一个东西”。</p>
<p>默认情况下，Compose 会结合<strong>调用位置</strong>来识别组合中的实例。</p>
<p>你可以先把它粗略理解成：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">identity ≈ 调用位置 <span class="token operator">+</span> 执行顺序<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>这也是为什么下面这种代码在“静态顺序”下通常没问题：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">for</span> <span class="token punctuation">(</span>item <span class="token keyword">in</span> list<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">Text</span><span class="token punctuation">(</span>item<span class="token punctuation">.</span>name<span class="token punctuation">)</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>但一旦 <code>list</code> 的顺序发生变化，Compose 就未必还能知道：</p>
<blockquote>
<p>“现在第 0 个位置这个 <code>Text</code>，是不是之前那个 item 对应的 <code>Text</code>？”</p>
</blockquote>
<p>这时，就需要额外的信息来告诉 Compose：“它是谁”，而不只是“它现在排第几个”。</p>
<hr>
<h2 id="二、不加-key-，动态列表最容易出现什么问题？"><a href="#二、不加-key-，动态列表最容易出现什么问题？" class="headerlink" title="二、不加 key()，动态列表最容易出现什么问题？"></a>二、不加 <code>key()</code>，动态列表最容易出现什么问题？</h2><p>看一段常见代码：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">for</span> <span class="token punctuation">(</span>stock <span class="token keyword">in</span> stocks<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">StockRow</span><span class="token punctuation">(</span>stock<span class="token punctuation">)</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>如果 <code>stocks</code> 只是内容刷新、顺序不变，通常问题不大。<br>但如果它会发生这些变化：</p>
<ul>
<li>按价格重新排序</li>
<li>插入新项</li>
<li>删除旧项</li>
<li>拖拽重排</li>
</ul>
<p>那么 Compose 可能会按“位置”去理解这些 item，而不是按“业务身份”去理解它们。</p>
<p>这类情况下，常见现象包括：</p>
<ul>
<li><code>remember</code> 的状态跟着位置走，而不是跟着 item 走</li>
<li>动画中断或绑定到错误对象</li>
<li>某些副作用被取消后重新启动</li>
<li>原本可以复用的内容没有被正确复用</li>
</ul>
<p>所以，问题的根源不是“列表变了”，而是：</p>
<blockquote>
<p><strong>Compose 缺少一个稳定标识，无法确认“重排前后的这个节点其实是同一个 item”。</strong></p>
</blockquote>
<hr>
<h2 id="三、key-到底做了什么？"><a href="#三、key-到底做了什么？" class="headerlink" title="三、key() 到底做了什么？"></a>三、<code>key()</code> 到底做了什么？</h2><p><code>key()</code> 的作用，就是为一段组合内容显式提供身份标识。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">for</span> <span class="token punctuation">(</span>stock <span class="token keyword">in</span> stocks<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">key</span><span class="token punctuation">(</span>stock<span class="token punctuation">.</span>code<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token function">StockRow</span><span class="token punctuation">(</span>stock<span class="token punctuation">)</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>加上以后，Compose 在识别这段内容时，不再只依赖位置，还会参考你提供的 key。</p>
<p>可以把它理解成：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">identity ≈ 调用位置 <span class="token operator">+</span> 你提供的 key<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>这不是官方 API 文档里的逐字公式，而是对工作方式的简化理解。更准确地说，<code>key()</code> 会改变 Compose 为这段内容建立身份映射的方式，让它在重排时更容易找到“同一个实例”。</p>
<hr>
<h2 id="四、key-解决的核心问题"><a href="#四、key-解决的核心问题" class="headerlink" title="四、key() 解决的核心问题"></a>四、<code>key()</code> 解决的核心问题</h2><h3 id="1-避免-remember-状态错位"><a href="#1-避免-remember-状态错位" class="headerlink" title="1. 避免 remember 状态错位"></a>1. 避免 <code>remember</code> 状态错位</h3><p>这是最常见、也最值得重视的场景。</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">for</span> <span class="token punctuation">(</span>stock <span class="token keyword">in</span> stocks<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">var</span> expanded <span class="token keyword">by</span> remember <span class="token punctuation">{</span> <span class="token function">mutableStateOf</span><span class="token punctuation">(</span><span class="token boolean">false</span><span class="token punctuation">)</span> <span class="token punctuation">}</span>
    <span class="token function">StockRow</span><span class="token punctuation">(</span>
        stock <span class="token operator">=</span> stock<span class="token punctuation">,</span>
        expanded <span class="token operator">=</span> expanded<span class="token punctuation">,</span>
        onExpandClick <span class="token operator">=</span> <span class="token punctuation">{</span> expanded <span class="token operator">=</span> <span class="token operator">!</span>expanded <span class="token punctuation">}</span>
    <span class="token punctuation">)</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>如果列表重排，而你没有提供稳定 key，这个 <code>expanded</code> 很可能会跟着“位置”走。结果就是：</p>
<ul>
<li>A 的展开状态跑到 B 身上</li>
<li>原本展开的项突然变成别的项展开</li>
</ul>
<p>而加上 <code>key(stock.code)</code> 之后，这段状态就更有机会稳定地跟着对应的业务对象走。</p>
<p>正确写法：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">for</span> <span class="token punctuation">(</span>stock <span class="token keyword">in</span> stocks<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">key</span><span class="token punctuation">(</span>stock<span class="token punctuation">.</span>code<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">var</span> expanded <span class="token keyword">by</span> remember <span class="token punctuation">{</span> <span class="token function">mutableStateOf</span><span class="token punctuation">(</span><span class="token boolean">false</span><span class="token punctuation">)</span> <span class="token punctuation">}</span>
        <span class="token function">StockRow</span><span class="token punctuation">(</span>
            stock <span class="token operator">=</span> stock<span class="token punctuation">,</span>
            expanded <span class="token operator">=</span> expanded<span class="token punctuation">,</span>
            onExpandClick <span class="token operator">=</span> <span class="token punctuation">{</span> expanded <span class="token operator">=</span> <span class="token operator">!</span>expanded <span class="token punctuation">}</span>
        <span class="token punctuation">)</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<hr>
<h3 id="2-让重排更像“移动”，而不是“换人”"><a href="#2-让重排更像“移动”，而不是“换人”" class="headerlink" title="2. 让重排更像“移动”，而不是“换人”"></a>2. 让重排更像“移动”，而不是“换人”</h3><p>很多人会说：</p>
<ul>
<li>没有 key，Compose 会销毁并重建节点</li>
<li>有 key，Compose 只会移动节点</li>
</ul>
<p>这个方向是对的，但说得太绝对。</p>
<p>更准确的说法是：</p>
<blockquote>
<p><strong>提供稳定 key 后，Compose 在集合变化时更容易把旧实例和新位置正确匹配起来，因此可以更稳定地复用已有组合、状态和相关节点。</strong></p>
</blockquote>
<p>也就是说，<code>key()</code> 的价值不是保证“绝对不重建”，而是让 Compose 在插入、删除、重排时拥有更准确的身份信息，从而减少因为误判身份带来的无谓重建。</p>
<hr>
<h3 id="3-让列表性能更稳定"><a href="#3-让列表性能更稳定" class="headerlink" title="3. 让列表性能更稳定"></a>3. 让列表性能更稳定</h3><p>这里也需要一个很重要的措辞修正。</p>
<p><code>key()</code> 不是那种“加了以后所有界面都更快”的万能优化。<br>它更像是一个<strong>结构正确性 + 复用稳定性</strong>工具。</p>
<p>所以更准确的说法是：</p>
<blockquote>
<p><strong><code>key()</code> 优化的不是绘制速度本身，而是列表变化时的匹配和复用质量。</strong></p>
</blockquote>
<p>特别是在这些场景里更明显：</p>
<ul>
<li>动态排序列表</li>
<li>插入 / 删除频繁的列表</li>
<li>带动画的列表项</li>
<li>列表项内部有 <code>remember</code></li>
<li>依赖副作用或异步加载的列表项</li>
</ul>
<hr>
<h2 id="五、key-和-LazyColumn-里的-key-有什么区别？"><a href="#五、key-和-LazyColumn-里的-key-有什么区别？" class="headerlink" title="五、key() 和 LazyColumn 里的 key 有什么区别？"></a>五、<code>key()</code> 和 <code>LazyColumn</code> 里的 <code>key</code> 有什么区别？</h2><p>这是最容易混淆的地方之一。</p>
<h3 id="普通组合里的-key"><a href="#普通组合里的-key" class="headerlink" title="普通组合里的 key()"></a>普通组合里的 <code>key()</code></h3><pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token keyword">for</span> <span class="token punctuation">(</span>item <span class="token keyword">in</span> list<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">key</span><span class="token punctuation">(</span>item<span class="token punctuation">.</span>id<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token function">ItemRow</span><span class="token punctuation">(</span>item<span class="token punctuation">)</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这是一个组合作用域里的 API。它直接影响这段内容在 Composition 中的身份识别。</p>
<h3 id="LazyColumn-里的-key"><a href="#LazyColumn-里的-key" class="headerlink" title="LazyColumn 里的 key"></a><code>LazyColumn</code> 里的 <code>key</code></h3><pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin">LazyColumn <span class="token punctuation">{</span>
    <span class="token function">items</span><span class="token punctuation">(</span>
        items <span class="token operator">=</span> list<span class="token punctuation">,</span>
        key <span class="token operator">=</span> <span class="token punctuation">{</span> it<span class="token punctuation">.</span>id <span class="token punctuation">}</span>
    <span class="token punctuation">)</span> <span class="token punctuation">{</span> item <span class="token operator">-&gt;</span>
        <span class="token function">ItemRow</span><span class="token punctuation">(</span>item<span class="token punctuation">)</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>这是 lazy list 提供的 item key。</p>
<p>两者机制相关，但作用层级不同：</p>
<table>
<thead>
<tr>
<th>API</th>
<th>主要作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>key()</code></td>
<td>给一段组合内容指定身份</td>
</tr>
<tr>
<td><code>items(key = …)</code></td>
<td>给 lazy item 指定稳定身份，帮助列表在数据变化时正确复用和保留状态</td>
</tr>
</tbody></table>
<p>所以你可以把它们理解为：</p>
<blockquote>
<p><strong>本质思想一致，使用位置不同。</strong></p>
</blockquote>
<p>如果你在 <code>LazyColumn</code> 里展示列表，优先使用 <code>items(..., key = { ... })</code>。<br>如果你是在普通 <code>for</code> 循环、条件分支或手写组合结构里管理身份，则使用 <code>key()</code>。</p>
<hr>
<h2 id="六、它和-remember-key-完全不是一回事"><a href="#六、它和-remember-key-完全不是一回事" class="headerlink" title="六、它和 remember(key) 完全不是一回事"></a>六、它和 <code>remember(key)</code> 完全不是一回事</h2><p>很多人会把这两个写法混在一起：</p>
<pre class="line-numbers language-kotlin" data-language="kotlin"><code class="language-kotlin"><span class="token function">key</span><span class="token punctuation">(</span>item<span class="token punctuation">.</span>id<span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token operator">..</span><span class="token punctuation">.</span> <span class="token punctuation">}</span>

<span class="token function">remember</span><span class="token punctuation">(</span>item<span class="token punctuation">.</span>id<span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token operator">..</span><span class="token punctuation">.</span> <span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>

<p>它们解决的是两个完全不同的问题。</p>
<h3 id="key"><a href="#key" class="headerlink" title="key()"></a><code>key()</code></h3><p>改变的是<strong>这段组合内容的身份</strong></p>
<h3 id="remember-key"><a href="#remember-key" class="headerlink" title="remember(key)"></a><code>remember(key)</code></h3><p>改变的是<strong>remember 保存的值什么时候失效并重新计算</strong></p>
<p>所以：</p>
<ul>
<li><code>key()</code> 关心的是“这是谁”</li>
<li><code>remember(key)</code> 关心的是“这个值要不要重新算”</li>
</ul>
<p>一个偏结构身份，一个偏值缓存。不要互相替代。</p>
<hr>
<h2 id="七、什么时候应该优先考虑使用-key-？"><a href="#七、什么时候应该优先考虑使用-key-？" class="headerlink" title="七、什么时候应该优先考虑使用 key()？"></a>七、什么时候应该优先考虑使用 <code>key()</code>？</h2><p>下面这些场景，建议你认真检查是否需要 key：</p>
<h3 id="1-列表会重排"><a href="#1-列表会重排" class="headerlink" title="1. 列表会重排"></a>1. 列表会重排</h3><p>例如：</p>
<ul>
<li>按价格排序</li>
<li>按时间倒序</li>
<li>拖拽排序</li>
<li>服务端返回顺序变化</li>
</ul>
<p>这是 key 最典型的使用场景。</p>
<h3 id="2-列表项内部有-remember"><a href="#2-列表项内部有-remember" class="headerlink" title="2. 列表项内部有 remember"></a>2. 列表项内部有 <code>remember</code></h3><p>只要 item 内部有局部状态，就要特别警惕“状态跟位置走”的问题。</p>
<h3 id="3-列表项包含动画或副作用"><a href="#3-列表项包含动画或副作用" class="headerlink" title="3. 列表项包含动画或副作用"></a>3. 列表项包含动画或副作用</h3><p>对于带图片加载、网络请求、动画、计时器的 item，这会更明显。</p>
<h3 id="4-使用-LazyColumn、LazyRow、Pager-一类动态容器"><a href="#4-使用-LazyColumn、LazyRow、Pager-一类动态容器" class="headerlink" title="4. 使用 LazyColumn、LazyRow、Pager 一类动态容器"></a>4. 使用 <code>LazyColumn</code>、<code>LazyRow</code>、Pager 一类动态容器</h3><p>尤其是数据不是固定顺序时，更应该提供稳定 key。</p>
<hr>
<h2 id="八、什么时候没必要滥用？"><a href="#八、什么时候没必要滥用？" class="headerlink" title="八、什么时候没必要滥用？"></a>八、什么时候没必要滥用？</h2><p>不是所有地方都需要 <code>key()</code>。</p>
<p>下面这些情况通常不用太紧张：</p>
<ul>
<li>静态 UI</li>
<li>元素顺序固定不变</li>
<li>没有局部状态</li>
<li>没有插入、删除、重排</li>
<li>只是简单渲染一次文本或图片</li>
</ul>
<p>如果结构稳定，Compose 基于调用位置的默认识别通常已经足够。</p>
<hr>
<h2 id="九、写-key-时的两个注意点"><a href="#九、写-key-时的两个注意点" class="headerlink" title="九、写 key() 时的两个注意点"></a>九、写 <code>key()</code> 时的两个注意点</h2><h3 id="1-key-必须稳定"><a href="#1-key-必须稳定" class="headerlink" title="1. key 必须稳定"></a>1. key 必须稳定</h3><p>不要用每次都变的值，比如随机数、当前时间戳、<code>hashCode()</code> 不稳定对象等。<br>如果 key 不稳定，Compose 反而会把它们当成“全新节点”。这会破坏复用。</p>
<h3 id="2-key-应该唯一"><a href="#2-key-应该唯一" class="headerlink" title="2. key 应该唯一"></a>2. key 应该唯一</h3><p>至少在同一组兄弟节点范围内，它应该唯一。</p>
<p>最常见的安全选择是：</p>
<ul>
<li>数据库主键</li>
<li>服务端返回的唯一 id</li>
<li>业务上不会变化的 code / uuid</li>
</ul>
<hr>
<h2 id="十、一句话总结"><a href="#十、一句话总结" class="headerlink" title="十、一句话总结"></a>十、一句话总结</h2><blockquote>
<p><strong><code>key()</code> 的本质，是告诉 Compose：这段内容是谁，而不是它现在排在第几个。</strong></p>
</blockquote>
<p>它真正带来的价值不是“神奇提速”，而是：</p>
<ul>
<li>防止状态错位</li>
<li>提高列表重排时的复用准确性</li>
<li>减少因身份误判带来的额外重建</li>
<li>让动画、副作用、remember 状态更稳定</li>
</ul>
<p>如果你在做这些界面：</p>
<ul>
<li>股票 / Trading 列表</li>
<li>可折叠列表</li>
<li>动态排序列表</li>
<li>拖拽重排列表</li>
<li>带动画的 <code>LazyColumn</code></li>
<li>内部有 <code>remember</code> 状态的 item</li>
</ul>
<p>那么 <code>key</code> 往往不是“锦上添花”，而是保证结构正确性的基础设施。</p>
<hr>
<h2 id="结尾"><a href="#结尾" class="headerlink" title="结尾"></a>结尾</h2><p>理解 <code>key()</code> 的最好方式，不是把它当成“性能开关”，而是把它当成 Compose 身份系统的一部分。</p>
<p>当你开始从“位置”切换到“身份”去思考列表和重组时，你对 Compose 的理解就会更深入一层。</p>
<p>因为在声明式 UI 里，真正重要的问题从来不只是：</p>
<blockquote>
<p>要画什么</p>
</blockquote>
<p>而是：</p>
<blockquote>
<p><strong>当数据变化时，谁还是原来的那个谁。</strong></p>
</blockquote>
]]></content>
      <categories>
        <category>Compose</category>
      </categories>
      <tags>
        <tag>Jetpack Compose</tag>
        <tag>Android</tag>
        <tag>Compose</tag>
      </tags>
  </entry>
</search>
